# Consolidation-Pass 1 — DB Dry-Run Plan

Generated by `scripts/consolidation-db-sync.ts --plan` from `sessions/resolver-dossiers/consolidation-pass-1-merge-map.json`. **Read-only** — no DB writes performed by this run.

Scope: W40K-Entity-Set (Pässe 1..9, batches ssot-w40k-001..057)

Summary: 2 merges (15 candidate clusters, 13 no-merges, 0 flagged).

## Maintainer-Review-Gate Tier Split

**Mechanically unambiguous (2):** quick scan tier — phantom-row dedup, surface-form-vs-canonical, identical primaryFactionId/parent.
  - `vigilus_s` → `vigilus` (locations) — Phantom-Row mit name="Vigilus Core" und falscher Metadata (sector=tempestus, gx=520, gy=520) statt korrekt (sector=ultima, gx=730, gy=355). Keine Override-Datei verweist auf die Surface-Form "Vigilus Core".
  - `magister_sek` → `anakwanar_sek` (characters) — Beide Rows verweisen auf den Anarch der Sons of Sek (Gaunt-Ghosts-Sabbat-Worlds-Lore). Pass-1-Row anakwanar_sek (referenziert in batches/ssot-w40k-004.json für Salvation's Reach + The Anarch) wurde von Pass-9-Phase-3 für die Urdesh-Duology W40K-0549/0550 nicht erkannt; Pass 9 erzeugte stattdessen magister_sek mit identischer primaryFactionId='sons_of_sek'.

**Lore-deep / LLM-adjudicated (0):** read-carefully tier — title-vs-name, beiname, identity-with-context-overlap.
  - (none in this pass)

## Stage (i) Field-Retention column updates on keepers

- **characters.anakwanar_sek**
  - `notes` ← "Anarch of the Sons of Sek, antagonist of Matthew Farrer's Urdesh duology (W40K-0549 The Serpent and the Saint, W40K-0550 The Magister and the Martyr); cross-cluster appearance in Sabbat-Worlds-Crusade-Saga (Salvation's Reach, The Anarch). Consolidation-Pass 1 merge of magister_sek into anakwanar_sek." *(keeper.notes was null; mergee.notes carries Urdesh-duology context worth preserving)*

## Stage (ii) FK-Remap (`characters.primary_faction_id`)

- *(no FK-remap — no faction merges in this pass; `characters.primary_faction_id` is unaffected)*

## Stage (iii) In-transaction verification (BEFORE delete)

For each mergee ID below, the apply path runs the following SQL and ABORTS the transaction (rollback) if ANY of these returns a non-zero count:

- `SELECT COUNT(*) FROM work_locations WHERE location_id = 'vigilus_s'`
- `SELECT COUNT(*) FROM work_characters WHERE character_id = 'magister_sek'`

## Stage (iv) Deletes

- `DELETE FROM locations WHERE id = 'vigilus_s'`
- `DELETE FROM characters WHERE id = 'magister_sek'`

## Atomicity

All four stages run inside **one DB transaction**. If stage (iii) flags any non-zero count for any mergee, the transaction rolls back — no partial delete, no orphaned field-retention update.

## Post-Verify (after commit, outside this script)

After the apply commits, the runbook calls for:
- Re-running the same Stage-(iii) counts: all must read 0.
- `SELECT * FROM <table> WHERE id IN (mergees)` must return 0 rows.
- Each keeper DB row, projected on the field-retention columns, must equal the keeper JSON row.
- `DB.locations.tags ⊇ JSON.locations.tags` for every touched location keeper (Re-Apply maintains this via `updateLocationTags`; the script never sets `tags`).
