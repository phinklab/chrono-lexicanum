# Consolidation-Pass 2 — DB Dry-Run Plan

Generated by `scripts/consolidation-db-sync.ts --plan` from `sessions/resolver-dossiers/consolidation-pass-2-merge-map.json`. **Read-only** — no DB writes performed by this run.

Scope: Full-Corpus (W40K Pässe 1..9 + HH Pässe 10..15, batches ssot-w40k-001..057 + ssot-hh-001..030)

Summary: 2 merges (23 candidate clusters, 21 no-merges, 0 flagged).

## Maintainer-Review-Gate Tier Split

**Mechanically unambiguous (1):** quick scan tier — phantom-row dedup, surface-form-vs-canonical, identical primaryFactionId/parent.
  - `sharrowkyn` → `nykona_sharrowkyn` (characters) — Same-era cross-pass doublette: Resolver-Pass 11 created `sharrowkyn` (Phase 3 7c freq=1, Angel Exterminatus HH-0023 — Raven Guard sniper, Shattered Legions associate); Resolver-Pass 13 created `nykona_sharrowkyn` (Phase 3 7c freq=1 promote, Kryptos HH-0167 — Raven Guard Shadowmaster paired with Sabik Wayland + Sisypheum origin). Both novels are Graham McNeill, both rows have primaryFactionId='raven_guard', both reference the same Mor Deythan Raven-Guard sniper character in the Shattered-Legions/Sisypheum saga. Pass-11→Pass-13 cross-wave honorific/surname-prefix miss; mechanically eindeutige Doublette. Class-identical to Pass-1 magister_sek → anakwanar_sek (Pass-9-implementer missed Pass-1 anchor on honorific strip).

**Lore-deep / LLM-adjudicated (1):** read-carefully tier — title-vs-name, beiname, identity-with-context-overlap.
  - `merir_astelan` → `astelan` (characters) — Cross-era identity: HH-era Dark Angels chapter-master Merir Astelan (Pass-13 Phase 3 freq=1, Call of the Lion HH-0154 Astelan vs Belath compliance debate) is the same individual as the W40K-era Fallen Angel Astelan (Lexicanum lemma 'Merir Astelan'; Fallen-Angel-interrogations lore-cluster). The merir_astelan row's own notes explicitly document the cross-era continuity ('Cross-arc with the Fallen / Cypher / Lion arcs; his post-Heresy First-of-the-Fallen status is downstream from his Pass-13 surface'); the Pass-13 implementer recognized the identity but lacked the ADR-policy (Brief 100) to consolidate. Per Cross-Era-Identities ADR (W40K-canonical short-form as keeper, HH-era full name as alias — analogous to horus / lorgar / typhus / corax), keeper = astelan.

## Stage (i) Field-Retention column updates on keepers

- **characters.astelan**
  - `notes` ← "Cross-era identity: HH-era Dark Angels chapter-master Merir Astelan (Resolver-Pass 13 Phase 3, freq=1) debating compliance around Byzanthis in Call of the Lion (HH-0154, Astelan vs Belath compliance debate); cross-arc with the Fallen / Cypher / Lion arcs. Post-Heresy: First-of-the-Fallen renegade. primaryFactionId fallen_angels reflects the W40K-era canonical state (keeper-wins per consolidation-pass-2 field-retention; HH-era dark_angels affiliation captured here via cross-era note per ADR cross-era-identities). Consolidation-Pass 2 merge of merir_astelan (HH-era surface form) into astelan (W40K-canonical row)." *(keeper.notes was null; mergee.notes carries Pass-13 HH-era context worth preserving + cross-era narrative)*
- **characters.nykona_sharrowkyn**
  - `notes` ← "Resolver-Pass 13 Phase 3 (7c medium freq=1 Phase-3-promote per dossier recommendation — Raven Guard Shadowmaster debut, cross-arc with the Sisypheum arc + future Shattered-Legions novellas): Raven Guard Shadowmaster debut paired with Sabik Wayland; Kryptos (HH-0167, Graham McNeill — Sharrowkyn / Wayland + Sisypheum origin). Earlier Resolver-Pass 11 Phase 3 surface (7c curated freq=1 lore-iconic): Raven Guard sniper, Shattered Legions associate, Angel Exterminatus (HH-0023). primaryFactionId raven_guard. Consolidation-Pass 2 merge of sharrowkyn (surname-only short-form, Pass-11 surface) into nykona_sharrowkyn (full canonical name, Pass-13 surface)." *(both rows have unique Pass-context notes worth preserving — keeper's Pass-13 Kryptos/Sisypheum notes enriched with mergee's Pass-11 Angel-Exterminatus anchor)*

## Stage (ii) FK-Remap (`characters.primary_faction_id`)

- *(no FK-remap — no faction merges in this pass; `characters.primary_faction_id` is unaffected)*

## Stage (iii) In-transaction verification (BEFORE delete)

For each mergee ID below, the apply path runs the following SQL and ABORTS the transaction (rollback) if ANY of these returns a non-zero count:

- `SELECT COUNT(*) FROM work_characters WHERE character_id = 'merir_astelan'`
- `SELECT COUNT(*) FROM work_characters WHERE character_id = 'sharrowkyn'`

## Stage (iv) Deletes

- `DELETE FROM characters WHERE id = 'merir_astelan'`
- `DELETE FROM characters WHERE id = 'sharrowkyn'`

## Atomicity

All four stages run inside **one DB transaction**. If stage (iii) flags any non-zero count for any mergee, the transaction rolls back — no partial delete, no orphaned field-retention update.

## Post-Verify (after commit, outside this script)

After the apply commits, the runbook calls for:
- Re-running the same Stage-(iii) counts: all must read 0.
- `SELECT * FROM <table> WHERE id IN (mergees)` must return 0 rows.
- Each keeper DB row, projected on the field-retention columns, must equal the keeper JSON row.
- `DB.locations.tags ⊇ JSON.locations.tags` for every touched location keeper (Re-Apply maintains this via `updateLocationTags`; the script never sets `tags`).
