# Sessions

This folder is the project's history. Every meaningful unit of work — an architect brief from Cowork, an implementation report from Claude Code — is one Markdown file here.

For the format, naming convention, status lifecycle, and full rules, see [`docs/agents/SESSIONS.md`](../docs/agents/SESSIONS.md).

## Quick reference

- **Naming:** `YYYY-MM-DD-NNN-{arch|impl}-{slug}.md` — e.g. `2026-04-28-002-arch-phase-1-handoff.md`
- **`NNN`** is monotonically increasing across the whole project (not reset daily)
- **Templates** live in `_templates/` — copy when starting a new session
- **Drafts** go in `_drafts/` (gitignored)
- **Archive:** when a phase wraps, move completed sessions to `archive/YYYY-MM/`

## Infrastructure log

Small infra changes that don't justify a session. Newest first.

- **2026-05-05** — **Phase 3 Stufe 3e Batch 1 grün gelaufen** (Report 044, Brief 044 → status `implemented`). Bücher 41–90, Dry-Run, Diff `ingest/.last-run/backfill-20260505-2247.diff.json` committed: 50 added · 0 updated/skipped · 15 field_conflicts · 47 errors · 18 llm_flags · Cost **$5.88** mittig im Brief-Range $4–8. Resume-Mechanik live verifiziert: zwei saubere Wiederaufsetzpunkte über drei Bash-Sessions hinweg (Index 43 → 44 und 67 → 68), hard-demo-Stdout-Zeile aus `scripts/ingest-backfill.ts:221–223` zitiert. SIGINT-Vehikel auf Windows-`npm.cmd`-Wrapper-Hosts nicht erreichbar (Soft-Demonstration via per-book State-Save genutzt — Brief erlaubt das explizit). Befunde gegen 042-Re-Test-Baseline: `value_outside_vocabulary` 6× mit drei distinct-Werten (`duty` ×4, `vengeance`, `fate`); `proposed_new_facet` 0× (Haiku flagt thematisch konsistent als `value_outside_vocabulary`, nicht explizit „propose"); `author_mismatch` 0/50 (Haiku-Schwäche bei Multi-Author-Anthologien wie 042 bestätigt — Side-Channel: Hardcover-Crawler-`errors` decken 10× author mismatch ab); Synopsen-Drift-Verbesserung (90% in 100–150 W vs. 042's 80%, kein Underflow mehr, max nur 157 W). Errors-Pro-Buch-Rate stabil (0.94 vs. 042 0.9), aber Bucket-Verschiebung: OL no-hits +68%, Hardcover author-mismatch −56%, neuer Bucket „no Hardcover hits for title" 4×. Dashboard rendert die neue Card sauber als jüngste oben mit 6 Cards total, Drill-down 50/50 Accordions.
- **2026-05-04** — **Phase 3.5 Ingestion-Dashboard als Brief 041 geöffnet.** Read-only Dashboard auf Route `/ingest`, Server Component, liest die committed Diff-Files aus `ingest/.last-run/*.diff.json`. Summary-Cards pro Lauf (chronologisch, neuester zuerst) mit Counters/Modell/Cost/Discovery-Roster-Size, plus per-Buch Drill-down mit allen DiffFile-Feldern (sources, fieldOrigins, hardcover-audit, llm-payload, flags). Datenquelle ist Filesystem (kein DB-Persist, kein Live-Polling) — Updates sichtbar nach `git push` + Vercel-Deploy. Public route, keine Auth. Folge-Effekt der 3e-Strategie-Anpassung (siehe nächster Eintrag): da Phase 3e jetzt in 50–100-Buch-Batches statt einem 800-Buch-Übernachtlauf läuft, wird der Dashboard nach jedem Batch zur Quality-Gate.
- **2026-05-04** — **Phase 3e Strategie-Anpassung: 50–100-Buch-Batches statt 800-Buch-Übernachtlauf.** Philipp will den PC nicht 38h durchlaufen lassen und will nach jedem Batch sehen ob alles ok ist. Operationelle Änderung, kein Code-Change — das CLI hat `--limit N --offset M` schon, State-Resume schützt vor Neuanfang. Geschätzt 8–16 Sessions über mehrere Wochen für die ~800 Bücher. Voraussetzung: Brief 041 (Dashboard) muss merged sein, sonst ist „nach jedem Batch sehen" raw-JSON-grep.
- **2026-05-04** — **Phase 3 Stufe 3c — Modell-Switch auf Haiku 4.5 + Prompt-Härtung als Mini-Brief 040 geöffnet.** Test-Gate-Entscheidung (siehe 039 + Addendum) zugunsten Haiku 4.5: ~$88 Voll-Lauf-Estimate (3.2× günstiger als Sonnet) + 95% Format-Coverage. Brief 040 schließt 3c sauber ab mit zwei System-Prompt-Härtungen (Vokabular-ID-Form gegen die `tone_grimdark`-Falle; Format-Required-Verhalten) + Default-Modell-Switch im Code, plus einem Re-Test-Lauf auf Bücher 21–40 zur Verifikation der Vokabular-Compliance (Ziel ≤2 invalid Facet-IDs vs. 19 im Original-Haiku-Lauf). Hybrid-Strategie Sonnet-Re-Run für Anthologien wurde verworfen, ebenso Chat-only-Pfad (50–80h Eigenarbeit ersetzt nicht $88) — stattdessen **gezielter Hand-Check** post-3e: nur Prestige-Reihen (HH 1–54, Eisenhorn, Ravenor, Gaunt's Ghosts; ~150 Bücher) + Bücher mit auffälligen Cowork-Triage-Flags (~120 von 800). Restliche ~530 Hintergrund-Bücher bekommen Haiku-Output as-is. Workflow nach 3e: 3e Voll-Lauf (Haiku) → Cowork-Flag-Triage + Philipps gezielte externe Hand-Reviews parallel → 3d Apply-Step mit Override-Korrekturen aus dem Hand-Check.
- **2026-05-03** — **Phase 3 Stufe 3c implementiert + Haiku-Vergleichs-Lauf** (Report 039 + Addendum, Brief 038 → status `implemented`). LLM-Anreicherungs-Schicht (Sonnet 4.6 + Web-Search, mandatory ≥2 calls/Buch) dockt an die 3a/3b-Engine via Two-Pass-Merge in `processOne` an, ohne Engine-Refactor. Schema-Migration `0006_illegal_sharon_carter.sql` (rating numeric(3,2) + rating_source varchar(32) + rating_count integer auf book_details, alle nullable) angewendet. Modul `src/lib/ingestion/llm/` mit 6 Files (enrich/prompt/parse/context/cache/rating); FS-Cache unter `ingest/.llm-cache/<slug>.json` mit Prompt-Version-Hash. **Zwei Test-Diffs committed: Sonnet 4.6 für Bücher 1–20 (`backfill-20260503-2037.diff.json`, $7.00, 21 Flags) + Haiku 4.5 für Bücher 21–40 (`backfill-20260503-2130.diff.json`, $2.21, 24 Flags) — letzter mit `--offset` CLI-Flag (3c-Add) und per-Buch `rawLlmPayload.model`-Tracking.** Befunde: Sonnet-Real-Cost $7 (Brief erwartete $1.50–4) → 800-Buch-Voll-Lauf ~$280; **Haiku $2.21 → ~$88 für 800 Bücher**, also 3.2× günstiger und im Brief-Original-Budget. Format-Coverage Haiku 95% vs Sonnet 40% (Sonnet konservativer). Vokabular-Compliance Haiku-Schwäche: 19 `value_outside_vocabulary`-Verstöße (Haiku kombiniert Kategorie-Prefix mit Wert wie `tone_grimdark` statt `grimdark` — fixbar via System-Prompt-Tweak). Plausibility-Cross-Check tiefer bei Sonnet (`data_conflict×9, author_mismatch×3` vs Haiku `×1, ×0`); Haiku verpasst Multi-Author-Anthologie-Glitches. Synopsen-Qualität auf vergleichbarem Niveau (Haiku 102–150 W alle in Range; Sonnet 122–159 W mit 3 leicht über). Test-Gate-Entscheidung gehört Philipp + Cowork.
- **2026-05-03** — **Phase 3 Stufe 3b implementiert** (Report 037, Brief 036 → status `implemented`). Open Library + Hardcover docken an die 3a-Engine **ohne Refactor** an, Schema bekommt `book_format` + `book_availability` Enums + 4 nullable `book_details`-Spalten via `0005_solid_giant_man.sql`, Wikipedia-Discovery erweitert auf 4 Pages (Hauptliste + HH-novels + Siege_of_Terra + Eisenhorn) mit neuem Wikitable-Parser. Discovered: 701 unique (700-900-Band ✓), `discoveryDuplicates`-Audit-Slot zeigt 96 cross-page Einträge. Test-Diff `ingest/.last-run/backfill-20260503-1328.diff.json` committed: 7 added (6 davon mit Hardcover-Tags 6-27 + averageRating 3.18-4.00) + 3 skipped_manual + 1 field_conflict auf releaseYear (OL-reissue vs WP/Lex-original; WP gewinnt). **Hardcover-Schema empirisch verifiziert** im Mid-Session-Update nachdem Philipp einen funktionierenden Token gesetzt hat: Hasura-typisch, `_eq` als einziger erlaubter Title-Operator (`_ilike`/`_iregex` Server-Permissions-blocked), `cached_tags` als Object-of-Arrays mit Genre/Mood/Content Warning/Tag-Kategorien.
- **2026-05-03** — **Phase 3 Stufe 3a auf main gelandet** (PR `feat/phase-3a-bulk-backfill-skeleton` → main, 3 Commits `0c5c18f` Brief 034 + ROADMAP-Reshape + 033-Archive, `a61fd4f` 3a-Code, `501749d` Report 035). Multi-Source-Merge-Engine läuft, Wikipedia-Discovery liefert 683 unique Bücher aus der Hauptliste, Lexicanum-Crawler via curl-shell-out (Cloudflare-TLS-Block auf Node-native fetch), Manual-Protection greift auch beim id-Suffix-Slug-Format der 26 Bestände. Test-Diff `ingest/.last-run/backfill-20260503-1115.diff.json` committed als Proof-of-Run. Phase 3b (Aux-Sources Open Library + Hardcover, format/availability-Schema, Discovery-Sub-Listen) ist als Brief 036 offen, schreibt weiterhin nichts in die DB.
- **2026-05-02** — **Phase-3-Strategie-Schwenk nach Cowork-Brainstorming.** Brief 033 (Daily-Drift-Crawler nach CCs 032-Empfehlung) wurde **vor Handover an CC zurückgezogen**, status `archived` mit Retraction-Banner. Im Cowork-Chat hat Philipp die fundamentalen Annahmen hinterfragt; gemeinsam neu durchdacht: Endziel ist **alle ~600–900 WH40k-Romane**, nicht „Drift bei 26". Daraus folgt Bulk-Backfill (lokal über Nacht, resumable State) + Maintenance-Crawler (monatlich, GH Actions); Wikipedia-Master-Liste als Discovery-Quelle; **Multi-Source-Merge per Field-by-Field-Source-Priority** (deterministisch, pro Schema-Feld eine kanonische Quelle); LLM-Anreicherungs-Schicht (Anthropic Sonnet 4.6 + Web-Search) für Synopsen-Paraphrase, Soft-Facet-Klassifikation und Plausibilitäts-Cross-Check (~$20–50 einmalig). Brief 034 schneidet Phase 3a aus dieser Vision: Wikipedia-Discovery + Lexicanum-Crawler + Multi-Source-Engine + Daten-Korrekturen, Dry-Run-only. Test-Modus `--limit 40` für Philipps Quality-Check vor Voll-Lauf. Phasen 3b–3f folgen sukzessiv.
- **2026-05-02** — **Phase 3 kickoff: Brainstorm geschlossen.** Brief 031 (Phase-3-Ingestion-Brainstorm) → Report 032 (PR #15, commit `0565da5`, status `complete`, 8 Achsen + opinionated Empfehlung). 032 lieferte wertvolle Faktenrecherche (Goodreads-API tot seit 2020, Lexicanum hat öffentliche MediaWiki-API mit 5s Crawl-Delay, BL hat Cloudflare-Verdacht, Open Library + Hardcover.app als Goodreads-Ersatz) und Schema-Korrektur (`source_kind`/`confidence` liegen auf `works`, nicht `external_links` — Brief 031 hatte das irreführend formuliert). Architektur-Empfehlung wurde nach Cowork-Brainstorming durch eine andere Strategie ersetzt (siehe darüber).
- **2026-05-02** — **Phase 2 (Chronicle Foundation) closed.** Verlauf: Stufe 2a slim (008+011) → Timeline-Polish (012+013) → Phase-1.5-Build-Hygiene (014+015) → CI-Cleanup (016+017) → Schema-Foundation 2a (019+020) → Rich-Seed 2b (021+022) → Era-Anchor 2c.0 (023+024) → DetailPanel + Deep-Link 2c.1 (025+026) → Hygiene-Pack 2c.2 (027+028) → FilterRail-MVP 2a.2 (029+030). 21 Sessions wandern in `archive/2026-04/` (008, 011–013) und `archive/2026-05/` (014–030). 018 ist als „superseded" mit drin (Plan-Wechsel zu DB-First hat es ersetzt). Active-threads-Tabelle ist beim Phasen-Schnitt leer; Phase 3 (Daten-Ingestion-Pipeline) startet als Brainstorm-/Research-Brief.
- **2026-05-02** — **Plan-Reshuffle** (siehe ROADMAP.md, "Plan-Reshuffle 2026-05-02" oben in der Datei). Auf Philipps Wunsch: Phase 2 schließt mit dem Minimal-FilterRail (Brief 029); EntryRail (vormals 2a.1) gestrichen, weil Funktion vollständig vom Ask-the-Archive-Trichter in Phase 5 abgedeckt wird; Phase 3 ↔ 4 getauscht (Daten-Ingestion vorgezogen, weil paralleles Scrapen ab jetzt im Hintergrund laufen soll, während Feature-Arbeit weiterläuft); neue Phase 4 = Discovery-Layer (Timeline-Reshape cineastisch + DB-/Sortier-Seite + Detail-Seiten + persönliche Bibliothek — fusioniert die alte Detail-Seiten-Phase mit Philipps Plan); Cartographer + Ask the Archive nach Phase 5 verschoben (vorher unter Phase 2). Cover-Ständer-Idee für Audio-Hörer in Ideas-Backlog. Strategie: Datenbank wird parallel gefüllt, während Features entstehen.
- **2026-05-01** — CI-Workflow Check-Naming stabilisiert (sessions 016/017). `.github/workflows/ci.yml` läuft nur noch auf `pull_request:` — der `(pull_request)`-Suffix, den GitHub bei Multi-Trigger-Workflows an den Check-Run-Namen hängt, ist damit weg. Required-Status-Check `ci / lint-and-typecheck` ist im Ruleset wieder ohne Suffix einhängbar (Anleitung im Report 017). Trade-off: kein automatischer CI-Lauf mehr auf `main` post-merge — Vercel deploy ist die zweite Sicherung.
- **2026-05-01** — Repo transferred from `wptnoire/chrono-lexicanum` to `phinklab/chrono-lexicanum` (new GitHub Team org, needed because Rulesets don't enforce on private repos under the Free plan). New canonical URL: <https://github.com/phinklab/chrono-lexicanum>. GitHub redirects old URLs permanently; references to `wptnoire/...` in archived session logs are left as-is (historical record).

> Cosmetic UI polish items live in [`docs/ui-backlog.md`](../docs/ui-backlog.md) and are cleared in batched cleanup sessions, not folded into the next brief.

## Carry-over for the next architect brief

Items decided / surfaced between sessions that the **next** architect brief MUST address. Append here when you spot something at the end of a session that doesn't deserve its own brief but must not be forgotten. Cowork prunes items here once they've been folded into a brief or otherwise resolved.

- **Hand-Check-Workflow-Brief nach voller Vergleichs-Matrix (jetzt Brief 047+).** Mit dem Modell-Achse-Split (045 = Sonnet, 046 = Opus + Aggregat) verschiebt sich der Hand-Check-Brief auf 047 oder später. Sequenz: Batch 1 (044, ✓) → 045 Sonnet-Quadrant (open) → 046 Opus-Quadrant + Cross-Aggregat (pending) → 047 Hand-Check + Override-Schema. Wenn der Vergleich zeigt: ein größeres Modell deutlich besser bei Author-Mismatch/Tagging-Coverage → 047 definiert den Hand-Check-Workflow für ~150 Prestige-Bücher mit dem ausgewählten Modell + Override-File-Schema + Apply-Read-Order. Wenn Modelle ähnlich → 047 kürzt den Hand-Check und fokussiert auf Vokabular-Erweiterung (`duty` mit 5/70 reproduzierbar) + 3d-Apply-Step.
- **Pipeline-Verbesserungs-Mini-Brief: Synopsis-zu-Tag-Cross-Check-Pass.** Aus 044-Befund: `mark-of-calth` (042 Re-Test) hat Synopsis die explizit „Word Bearers", „Roboute Guilliman's Ultramarines" und „Daemonic" erwähnt, aber Haikus `facetIds` enthalten keine Faktion-Tags (`chaos`, `word_bearers`, `ultramarines` fehlen). Pattern: thematische Tags okay, Faktions-/Charakter-Tags-Coverage lückenhaft. Möglicher Pipeline-Fix: zweiter Validierungs-Pass nach Synopsis-Erstellung — String-Match der Synopsis gegen das Faktionen-/Charaktere-Vokabular, fehlende Treffer als zusätzliche `facetIds`-Vorschläge oder als `proposed_new_tag`-Flags. Brief 045 testet als Nebenbefund, ob Sonnet-Pipeline (B) das Problem aus den selben Inputs auch hat (= Modell-Schwäche) oder nicht (= dann ist die Pipeline-Architektur nicht das Problem). Wenn nach 045 klar ist „auch Sonnet-Pipeline lässt die Faktionen weg" → der Synopsis-Cross-Check-Pass ist ein eigener Mini-Brief vor 3d. Wenn „Sonnet-Pipeline fängt es" → die Pipeline ist OK, das Modell ist der Hebel.
- **Vokabular-Erweiterungs-Mini-Brief Material aus 044.** `value_outside_vocabulary` über 70 Bücher kumulativ (042 + 044): `duty` × 5 (`praetorian-of-dorn`, `the-master-of-mankind`, `wolfsbane`, `saturnine`, `blood-of-the-emperor`), `vengeance` × 1 (`shattered-legions`), `fate` × 1 (`ruinstorm`). `duty` ist klarer Promotion-Kandidat, die anderen zwei brauchen mehr Datenpunkte. Entscheidungs-Aufnahme spätestens in Brief 046 oder als eigener Mini-Brief, falls Brief-045-Vergleich diese Werte als „sollte ins Vokabular" markiert.
- **`secondary_era_ids text[]` für Multi-Era-Sichtbarkeit.** Report 024 § „For next session" — `id01 The Infinite and the Divine` (M35–M41) wäre der offensichtliche Driver, weil es plausibel auch in `age_apostasy`/`long_war` lebt. UX-Konsequenz ist offen (zählt zweimal in den Era-Counts? lädt EraDetail zweimal?), darum kein eigener Brief jetzt. Wahrscheinlicher Aufgreif-Punkt: Phase-4-Timeline-Reshape (cineastische Era-Erkundung, scaling für viele Bücher) — dort wird Multi-Era-Sichtbarkeit ein konkretes Designthema.
- **Redirect-Mechanismus: HTTP 307 vs. meta-refresh nach `loading.tsx`-Add (PR #11 / Stufe 2c.1).** Mit `src/app/timeline/loading.tsx` (instant Skeleton beim Hub→Timeline-Click) hat sich das Verhalten von `redirect()`-Calls in `page.tsx` geändert: Stream startet vor dem Redirect → Next emittet `<meta http-equiv="refresh" content="1;url=…">` plus RSC-Redirect-Direktive, statt sauberen HTTP 307. Browser folgen korrekt; JS-User kriegen den Redirect via Next-Router instant; **Direkthits auf Redirect-URLs (geteilte Links, Legacy `?era=M30`) bekommen 1 Sekunde Skeleton-Blink vor Meta-Refresh**. Sauberere Alternative: Redirect-Logik in `middleware.ts` ziehen (läuft vor Page-Render, gibt 307 sofort zurück). Trade-off: ~80 Zeilen `middleware.ts` mit 1–2 DB-Queries vor jedem `/timeline`-Hit. Mit Philipp am 2026-05-02 bestätigt: bleibt vorerst hier liegen — Internal-Nav ist unaffected, Direkthits sind selten genug. Eigener Brief wenn der Schmerz konkret wird.
- **3c `no_rating_found`-Flag-Misuse (Sonnet-spezifisch, mit Haiku-Switch dormant).** 4 Bücher im Sonnet-Original-Lauf hatten `rating + ratingSource` gesetzt UND einen `no_rating_found`-Flag — der Sonnet nutzt den Flag als Audit-Erklärung für Source-Priority-Fallback. Haiku zeigte das Verhalten nicht (0× im Vergleichs-Lauf). Mit dem Modell-Switch in 040 ist das Symptom weg; bleibt relevant nur wenn Sonnet später ad-hoc per `INGEST_LLM_MODEL`-Override für einzelne Bücher eingesetzt wird. Mini-Brief-Pfad falls jemals nötig: Flag-Kind-Union um `rating_source_fallback` erweitern, oder System-Prompt schärfen.
- **`series_total_mismatch`-Flag bei Voll-Lauf beobachten.** Im Original-Haiku-Lauf (Bücher 21–40, alles HH-Phase-2) wurde der Flag 0× emittiert — entweder weil HH-Mid-Phase keine Series-Total-Anomalien hat, oder weil das Flag-Constraint im System-Prompt zu eng formuliert ist. Im 3e Voll-Lauf (alle 800 Bücher inkl. Primarchs/Tales-of-Heresy/etc.) sollten echte Mismatches sichtbar werden. Wenn der Voll-Lauf 0 Mismatches zeigt aber wir wissen dass es welche gibt: Prompt-Tweak-Mini-Brief; sonst nichts.
- **Mögliches Pipeline-Cost-Tuning Mini-Brief** (`max_uses` runter von 6 auf 3, da Haiku im Vergleichs-Lauf nur 3.4 Searches/Buch braucht; ggf. System-Prompt-Trim, Plot-Context kürzen, Web-Search-Optional-Mode für Bücher mit reichem Plot-Kontext). 042 hat das schon als „eher nicht der Mühe wert" eingestuft (Cost ist 3× unter Sonnet, weit unter Brief-Original-Budget). Nur öffnen wenn 044 oder spätere 3e-Batches überraschend hohe Costs zeigen. Realistisch: ~10–30% zusätzliche Ersparnis möglich, also $20–25 Reduktion auf den $88-Voll-Lauf-Estimate.
- **Phase-3-Brief-Reminders aus Reports 035 + 037 + 039 für kommende Bricks.** Sub-Items, die jeweils in den Brief der relevanten Stufe gehören:
  - **3d (Apply-Step):** Author-FK-Resolution für `work_persons`-Junction inkl. Auto-Create-on-New-Person (CC hat `authorNames: string[]` raw geparkt; FK-Resolution ist Apply-Job). Plus FK-Resolution für `work_facets`-Junction aus den 3c LLM-`facetIds`. Plus ALTER TYPE für `sourceKind`-DB-Enum: `open_library`, `hardcover`, `llm` werden im Pipeline-`SourceName`-Type schon verwendet, im DB-Enum aber erst beim ersten echten Write (= 3d) gebraucht. Plus UNIQUE INDEX `external_links (work_id, kind, service_id)` damit Wikipedia/Lexicanum/Open-Library-URLs nicht doppelt landen. Plus `junctionsLocked: true`-Flag auf `works` damit zukünftige Crawler-Läufe Editorial-Pflege nicht überschreiben.
  - **3d (Apply-Step) — `loadTimeline`-Trim**: post-2a.2 nur noch von der Overview-Ribbon für per-Era-Counts konsumiert — könnte auf `SELECT primaryEraId, COUNT(*) GROUP BY primaryEraId` schmälern. Bleibt für 3d-Migration-Sammlung oder spätestens wenn die DB nach 3e auf 800 Bücher anwächst. (Index `book_details_primary_era_idx` wurde in 3b mitgenommen — siehe Report 037.)
  - **3d (Apply-Step) — `llm_flags`-Triage-Workflow.** Mit 21 Flags in 20 Büchern (3c-Test) ist Triage real und konkret: drei Kategorien empfohlen — auto-applied (`year_glitch` + `data_conflict` mit `sources.length >= 2` und Reasoning enthält Edition/Reissue-Erklärung), Cowork-Review (`proposed_new_facet`, `author_mismatch` wo Multi-Author), ignoriert (`insufficient_web_search`, `no_storefronts_found`, `no_rating_found`, `value_outside_vocabulary`). Plus: bei Voll-Lauf-Skala (800 Bücher) wird die Flag-Anzahl ~800–1500 erreichen — Triage muss UI-/CSV-/Markdown-export-fähig sein für sequenzielles Abarbeiten.
  - **3d (Apply-Step) — `rawLlmPayload`-FK-Resolution.** AddedEntry/UpdatedEntry/SkippedManualEntry tragen `rawLlmPayload?: { facetIds, discoveredLinks }`. Apply muss: facetIds gegen `facet_values.id` resolven (existieren alle, weil parse.ts strict-validiert hat) → `work_facets`-Junction; discoveredLinks gegen `services.id` resolven (Service-Hint matcht typischerweise eine ID; unbekannte Hints landen als „unknown service"-error im Apply-Diff) → `external_links`-Insert mit `kind` aus dem LLM-Payload. SkippedManualEntry-`rawLlmPayload` wird ignoriert (manual wins).
  - **3e (Backfill-Day) — Hardcover-Title-Variation.** Server blockt `_ilike`/`_iregex`, also matcht der 3b-Crawler nur exakte Title-Strings. Funktioniert für 6 von 7 added entries im 10er-Test. Bei subtilen Unterschieden (z.B. „The Solar War" vs „Solar War", „Eisenhorn: Xenos" vs „Xenos") landet ein „no hits"-Reason. Mitigation für 3e: Title-Variationen-Liste pro Buch probieren oder eine separate Such-Funktion in Hardcover entdecken (`books_trending`/`activity_feed` waren im Schema; eine RPC-Funktion könnte aber existieren). Heute kein Blocker.
  - **3f (Maintenance-Crawler):** `curl` muss im GH-Actions-Workflow-Container verfügbar sein (`ubuntu-latest` hat es by-default; explizit dokumentieren als Runtime-Voraussetzung der Lexicanum-Crawler-Wahl aus 3a).
  - **distant:** Lexicanum-`apiSearchFallback()` als zweiter Discovery-Pfad — heute ist URL-Probing der einzige Mechanismus, weil Cloudflare den `api.php`-Endpunkt blockiert. Wenn jemand jemals den Cloudflare-TLS-Fingerprint sauber umgeht (`cycletls`/`curl-impersonate-bun` o.ä.), sollte `discoverLexicanumArticle` fallback-fähig sein. Bis dahin nur als „watch this space".
  - **distant:** Format/Availability-Heuristik aus Open Library mit per-Edition-Fetch + Sprach-Filter (`languages: [{ key: "/languages/eng" }]`) wäre eine günstige Vorfilterung wenn 3c LLM-Kosten zu hoch werden. In 3b absichtlich weggelassen, in 3c durch LLM-Klassifikation gelöst — falls 3c-Test-Lauf zeigt dass Format/Availability auch ohne LLM gut bestimmbar wären, könnte das eine 3.5+-Optimierung sein.
  - **distant:** Engine-Friktions-Befunde aus Report 037 (`SourceName`-Union vs Plugin-Pattern, kein shared HTTP-Throttle, `AddedEntry.rawHardcoverPayload` vs generisches `auditPayloads: Record<SourceName, unknown>`). Wenn 3c eine zweite Quellen-spezifische Audit-Slot-Form braucht, ist das der Zeitpunkt für den `auditPayloads`-Refactor — sonst Phase-3.5+.

## Active threads

Open and recently-closed sessions, newest first. Cowork updates this list when it writes or reads a session. Older sessions stay in this folder until a phase ends, then move to `archive/`.

| Session | Role | Status | Topic |
|---|---|---|---|
| [2026-05-05-045](2026-05-05-045-arch-cc-vs-pipeline-comparison.md) | architect | open | **Sonnet-Quadrant der Vergleichsmatrix (Sonnet × {Pipeline, Web}, 6 Slugs).** Erste Hälfte des Modell-Achse-Splits: Variante B (Sonnet+Pipeline-Inputs 1:1, kein Web) und Variante C (Sonnet+„klug"+freie WebSearch). 6 Slugs: `shattered-legions`, `sons-of-the-emperor` (Anthologien für Author-Mismatch), `the-master-of-mankind` (Vokabular-Drift `duty`), `garro` (data_conflict pageCount), `the-solar-war` (Synopsen-Drift), `mark-of-calth` (Tagging-Failure: Synopsis erwähnt Word Bearers/Ultramarines/Chaos, Haiku-facetIds enthalten keine Faktion-Tags). Source-Diffs: 5 Slugs aus `backfill-20260505-2247` (044), `mark-of-calth` aus `backfill-20260503-2308` (042 Re-Test, gleiche Pipeline-Konfig). **Per-Buch-Cost-Tracking** mit `costEstimate`-Feld pro Slug + 1-Zeilen-Status an Philipp im Chat + Hard-Stop wenn `cumulativeLimitPercent + nextEstimate > 80`. Output-Files: `ingest/.compare/045-sonnet-pipeline.json` und `045-sonnet-web.json`, plus Innerhalb-Sonnet-Befunde im Report. **Opus-Pendant kommt in Brief 046** nach Sonnet-Resultat — wenn Sonnet-Web sich als „klug genug" erweist, kann 046 reduziert/gestrichen werden. **Schreibt nichts in die DB.** |
| [2026-05-05-046] | architect | pending | **Opus-Quadrant der Vergleichsmatrix (Opus × {Pipeline, Web}, gleiche 6 Slugs) + Cross-Aggregat aller 5 Varianten.** Wartet auf 045-Resultat. Wenn Sonnet-Web in 045 deutliche Lücken zeigt (vor allem Tagging-Coverage auf `mark-of-calth` oder Vokabular-Resolution): 046 macht Opus-Varianten D + E auf den selben 6 Slugs + Cross-Aggregat. Wenn Sonnet-Web schon „klug genug" ist: 046 reduziert auf z.B. nur Opus-Pipeline (D) oder wird ganz gestrichen + Cross-Aggregat zieht von vier statt fünf Varianten. Abhängigkeit: Subscription-Verbrauchs-Schätzung aus 045 (Variante C) × 5 ergibt realistisches Opus-Web-Budget. **Brief noch nicht geschrieben.** |
| [2026-05-05-044](2026-05-05-044-impl-phase3e-batch-1.md) | implementer | complete | **Phase 3 Stufe 3e — Batch 1 Implementer Report** (Antwort auf 044). 50er-Batch grün durchgelaufen mit drei sequentiellen Läufen + zwei Resume-Punkten. Diff `backfill-20260505-2247.diff.json`: 50 added · 15 field_conflicts · 47 errors · 18 llm_flags · Cost **$5.88** (im Range $4–8). Hard-demo der Resume-Mechanik aus Lauf-2-Stdout zitiert; SIGINT auf Windows-`npm.cmd` nicht greifbar, Soft-Demo via per-book State-Save. Open Questions vom Brief beantwortet: `proposed_new_facet` 0×, stattdessen `value_outside_vocabulary` 6× (3 distinct: `duty`/`vengeance`/`fate`); `author_mismatch` 0/50 (Haiku-Schwäche bestätigt); Synopsen-Drift 90% in Range (Verbesserung gegen 042 80%); Errors proportional zu 042 (0.94/Buch vs 0.9/Buch). Pipeline-Code unverändert. |
| [2026-05-05-044](2026-05-05-044-arch-phase3e-batch-1.md) | architect | implemented | **Phase 3 Stufe 3e — Batch 1 (Bücher 41–90, Dry-Run).** Beantwortet von 044-impl. Erster produktiver Schritt der batched-3e-Strategie: 50er-Batch laufen lassen (`--limit 50 --offset 40`, Bücher 1–40 sind durch 039+042 schon getestet), Resume-Mechanik aus 3a live verifizieren (mind. 1× Ctrl-C-Test, Form CC's Wahl), Diff committen, im Dashboard inspizierbar. Cost-Erwartung $5–7. **Hand-Check, Override-File-Schema, DB-Apply explizit out of scope** — nach Batch 1 macht Philipp Hand-Check in Claude.ai, daraus dann Brief 045 (CC-Vergleich) und 046 (Hand-Check + Override-Schema). Open Questions an CC: Flag-Verteilung auf 50, Synopsen-Drift, Author-Mismatch-Trefferrate, Resume-State-Form. **Schreibt nichts in die DB.** |
| [2026-05-04-043](2026-05-04-043-impl-ingestion-dashboard.md) | implementer | complete | **Phase 3.5 — Ingestion-Dashboard Implementer Report** (Antwort auf 041). `/ingest` (Listen) + `/ingest/[runId]` (Drill-down) als Server Components mit SSG via `generateStaticParams`. Diff-Reader unter `src/lib/ingestion/diff-reader.ts` mit Path-Traversal-Guard. Native `<details>`/`<summary>`-Accordion → null Client-JS, keine Hydration. Alle 5 committed Diffs (3a/3b/3c-Sonnet/3c-Haiku/040-Re-Test) rendern grün, Cost-Vergleich Sonnet $7.00 vs Haiku $2.21/$2.24 prominent in Card-Header. ~470 CSS-Zeilen unter `ingest-*`-Namespace ergänzt. Kein neuer Dependency, alle existing Routen unaffected. |
| [2026-05-04-042](2026-05-04-042-impl-phase3c-haiku-switch.md) | implementer | complete | **Phase 3 Stufe 3c — Haiku-Switch + Prompt-Härtung Implementer Report** (Antwort auf 040). Default-Modell auf `claude-haiku-4-5` umgestellt, zwei System-Prompt-Härtungen (Vokabular-ID-Form gegen Kategorie-Prefix-Glitch, Format-Required-Verhalten). Re-Test auf Bücher 21–40 grün: `value_outside_vocabulary` 19→**1** (95% Reduktion, der eine Verstoß ist echte Vokabular-Lücke `duty`, kein Prefix-Glitch), Format-Coverage **20/20=100%**, Cost $2.236 (im Range). Plausibility-Flags total 24→7. Synopsen-Drift 4/20 außerhalb 100–150W (Soft-Befund, kein Acceptance-Blocker). Tool-Schema unverändert. **3e Voll-Lauf kann starten.** |
| [2026-05-04-041](2026-05-04-041-arch-ingestion-dashboard.md) | architect | implemented | **Phase 3.5 — Ingestion-Dashboard (read-only Diff-Inspector).** Beantwortet von 043. Public Route `/ingest`, Server Component, liest die committed Diff-Files aus `ingest/.last-run/*.diff.json` und rendert chronologische Summary-Cards (neuester zuerst) mit per-Buch Drill-down. Datenquelle ist Filesystem, kein DB-Persist, kein Live-Polling — Updates sichtbar nach `git push` + Vercel-Deploy. Voraussetzung für die batched 3e-Strategie (Philipp inspiziert nach jedem 50–100-Buch-Batch ob alles passt, statt raw JSON grep'en). Brief hat **Design freedom**-Sektion: alle visuellen Entscheidungen (Layout, Typografie, Color-Coding, Drill-down-Mechanik, Routing-Form) gehören CC. Hard-constraints sind nur Datenquelle (Filesystem), Read-only, Server Component, DiffFile-Type aus `src/lib/ingestion/types.ts` als single source of truth. **Keine DB-Schreib-Ops, keine Apply-Trigger.** |
| [2026-05-04-040](2026-05-04-040-arch-phase3c-haiku-switch.md) | architect | implemented | **Phase 3 Stufe 3c — Modell-Switch auf Haiku 4.5 + Prompt-Härtung (Re-Test).** Beantwortet von 042. Mini-Brief, der den Test-Gate-Befund aus 039 + Addendum in eine Konfigurations-Entscheidung gießt: Default-Modell wird `claude-haiku-4-5` (3.2× günstiger als Sonnet, $88 statt $280 für 800-Buch-Voll-Lauf). Plus zwei System-Prompt-Härtungen: (1) Vokabular-ID-Form (bare value-IDs wie `grimdark`, nicht Kategorie-prefixed wie `tone_grimdark`) gegen Haikus 19/20 invalid Facet-IDs aus dem Original-Vergleichs-Lauf; (2) Format-Required-Verhalten (closest-match unter Unsicherheit, Restunsicherheit als `data_conflict`-Flag). Re-Test auf Bücher 21–40 (`--limit 20 --offset 20`) zur Verifikation; Acceptance: `value_outside_vocabulary` ≤ 2, Format-Coverage ≥ 90%, Cost ~$2.20 (Cache-Invalidation einmalig). Hybrid Sonnet-Re-Run für Anthologien wurde verworfen — Plausibility-Tiefe wird stattdessen extern via Philipps Hand-Check-Sessions in Claude.ai (separater Chat, 20er-Batches, kein API-Cost) abgedeckt. **Schreibt nichts in die DB.** |
| [2026-05-03-039](2026-05-03-039-impl-phase3c-llm-enrichment.md) | implementer | complete | **Phase 3 Stufe 3c — LLM-Anreicherungs-Schicht Implementer Report** (Antwort auf 038). Sonnet 4.6 + Web-Search-Anreicherung dockt an 3a/3b-Engine via Two-Pass-Merge in `processOne` an, Migration 0006 (rating-Felder) angewendet, neues Modul `src/lib/ingestion/llm/` mit 6 Files. Test-Lauf `--limit 20` grün produziert (15 added + 5 skipped_manual + 4 field_conflicts + 21 llm_flags). **Wichtigster Befund für Test-Gate: real-Cost $7.00 für 20 Bücher = $0.35/Buch, Faktor ~2× über Brief-Erwartung; 800-Buch-Voll-Lauf skaliert zu ~$280 statt $60–160. Hauptcost-Treiber sind Input-Tokens (1.74M = $5.21), nicht Web-Searches selbst ($1.12).** Format-Coverage 6/15 (LLM lässt optionale Felder zurückhaltend leer). |
| [2026-05-03-038](2026-05-03-038-arch-phase3c-llm-enrichment.md) | architect | implemented | **Phase 3 Stufe 3c — LLM-Anreicherungs-Schicht (Sonnet 4.6 + Web-Search **mandatory pro Buch**), 20-Buch-Test-Gate, Dry-Run.** Beantwortet von 039. Stufe 3/6 der Phase-3-Roadmap. Fünf Jobs in einem Prompt-Run pro Buch: Synopsen-Paraphrase (Wikipedia-/Lexicanum-Plot-Sektionen + verpflichtende Web-Search nach Reviews/Marketing als Input; 100–150 Wörter, lizenzsicher), Soft-Facet-Klassifikation auf das DB-kanonische Vokabular (strict-vocab), Format+Availability-Klassifikation mit verpflichtender Web-Search-Verifikation (Black Library Shop + Amazon + Audible), Plausibility-Cross-Check (Year-Glitches, Series-Total-Mismatches, Author-Mismatches → `llm_flags`-Audit-Slot, kein Auto-Apply), **Reader-Rating-Capture per Source-Priority `[amazon → goodreads → hardcover → audible]`, normalisiert auf 0–5-Skala — eine Quelle pro Buch, kein Average**. Plus Side-Effect: Storefront-URL-Capture als `discoveredLinks: { serviceHint, kind, url }[]` im Audit (für 3d-FK-Resolution gegen `services.json`). **Schema-Migration 0006:** `book_details.rating numeric(3,2)` + `rating_source varchar(32)` + `rating_count integer` (alle nullable). Strukturierter Output via `tool_use`. Cache unter `ingest/.llm-cache/<slug>.json`. Soft-fail wenn `ANTHROPIC_API_KEY` fehlt. Cost-Schätzung 20-Buch-Test ~$1.50–4.00, Voll-Lauf 800 Bücher ~$60–160; `llmCostSummary` im Diff macht's hart messbar. **Hard-Acceptance: ein einziger `--limit 20`-Test-Lauf, dann CC stoppt** — Philipp inspiziert die Diff, entscheidet „weiter zu 3d" oder „Cost zu hoch → Mini-Brief: Web-Search optional + ggf. Haiku". **Schreibt nichts in die DB.** |
| [2026-05-03-037](2026-05-03-037-impl-phase3b-aux-sources.md) | implementer | complete | **Phase 3 Stufe 3b — Aux-Sources Implementer Report** (Antwort auf 036). Open Library + Hardcover docken an die Engine ohne Refactor an. Schema-Migration `0005_solid_giant_man.sql` (2 Enums + 4 nullable Spalten + primaryEraIdx) angewendet, alle 26 Manuals + 7 Eras + 21 Series überleben. Wikipedia-Discovery jetzt 4 Pages (Hauptliste + HH-novels + Siege_of_Terra + Eisenhorn) via neuem Wikitable-Parser; 701 unique nach Sub-List-Precedence-Dedup, 96 cross-page Audits. Open Library liefert coverUrl/isbn13/isbn10/pageCount mit fieldOrigin sichtbar. Hardcover-Schema mid-session empirisch verifiziert: Hasura-typisch mit `_eq`-only-Title-Match (Server blockt `_ilike`); 6 von 7 added entries liefern Tags + averageRating als Audit-Slot. |
| [2026-05-03-036](2026-05-03-036-arch-phase3b-aux-sources.md) | architect | implemented | **Phase 3 Stufe 3b — Aux-Sources (Open Library + Hardcover) + Format/Availability-Schema + Discovery-Erweiterung, Dry-Run.** Beantwortet von 037. Stufe 2/6 der Phase-3-Roadmap. Zwei zusätzliche `SourceCrawler` (Open Library für Hard-Facts: Cover/ISBN/PageCount/Author-Lückenfüller; Hardcover für Soft-Facts: Tags/Rating als Audit-Slots) andocken in die existierende Engine **ohne Refactor** (Stress-Test der 3a-Architektur). Schema-Migration: `format` + `availability` Enums + `isbn10` + `pageCount` auf `book_details`. Wikipedia-Discovery um mind. 3 Sub-Listen erweitert. Hardcover-Token-Beschaffung ist Philipps Job (.env.local); soft-fail wenn Token fehlt. **Schreibt nichts in die DB.** |
| [2026-05-03-035](2026-05-03-035-impl-phase3a-bulk-backfill-skeleton.md) | implementer | complete | **Phase 3 Stufe 3a — Bulk-Backfill-Skeleton-Report** (Antwort auf 034). Skeleton steht und läuft grün: Wikipedia entdeckt 698→683 unique Bücher, Multi-Source-Merge-Engine kombiniert Wikipedia + Lexicanum, Comparator schützt 26 Manuals via title-normalize-Match, Test-Lauf mit `--limit 5` produziert sauberen Diff (`ingest/.last-run/backfill-20260503-1115.diff.json`: 4 added + 1 skipped_manual + 0 errors). **Wichtigste Abweichung:** Lexicanum's `api.php` ist Cloudflare-blockiert für Node-native fetch (TLS-Fingerprint, nicht UA) → shell-out zu `curl` als HTTP-Transport für `/wiki/`-Article-Pages, URL-Probing statt API-Search. |
| [2026-05-02-034](2026-05-02-034-arch-phase3a-bulk-backfill-skeleton.md) | architect | implemented | **Phase 3 Stufe 3a — Bulk-Backfill-Skeleton (Wikipedia-Discovery + Lexicanum-Crawler + Multi-Source-Engine, Dry-Run).** Erster atomarer Brick der 6-stufigen Phase-3-Roadmap (3a→3f). Wikipedia-Listen-Parser für Discovery von ~600–900 Romanen, Lexicanum-Crawler via MediaWiki-`api.php`, Multi-Source-Merge-Engine mit Field-by-Field-Source-Priority (extensible für 3b Open Library/Hardcover und 3c LLM), Manual-Protection-Comparator, Resumable-State, CLI mit `--limit N` für Test-Modus. Plus 7 Confidence-Aktivierungen aus 2b und 5 `series.totalPlanned`-Bugs. **Schreibt nichts in die DB.** Beantwortet von 035. |
| [2026-05-02-032](2026-05-02-032-impl-phase3-ingestion-brainstorm.md) | implementer | complete | Phase-3-Ingestion-Brainstorm-**Research-Report** (Antwort auf 031). 8 Achsen + opinionated Empfehlung. Faktenrecherche (Goodreads-API tot seit 2020, Lexicanum API verfügbar, BL Cloudflare-Verdacht) ist weiterhin Grundlage; Architektur-Empfehlung wurde nach Cowork-Brainstorming durch Bulk-Backfill-Modell ersetzt. |
| [2026-05-02-031](2026-05-02-031-arch-phase3-ingestion-brainstorm.md) | architect | implemented | Phase 3 kickoff — Ingestion-Pipeline-Brainstorm (Research-Brief). Beantwortet von 032. |

> Phase 2 wurde am 2026-05-02 geschlossen — die 21 Sessions (008, 011–030) sind nach `archive/2026-04/` (008, 011–013) bzw. `archive/2026-05/` (014–030) gewandert. Phase 3 läuft jetzt: 031/032 (Brainstorm) + 034/035 (3a Bulk-Backfill-Skeleton) + 036/037 (3b Aux-Sources) + 038/039 (3c LLM-Anreicherungs-Schicht) + 040/042 (3c Haiku-Switch + Prompt-Härtung, complete) + 041/043 (Phase 3.5 Ingestion-Dashboard, complete) + 044 (3e Batch 1, complete) + **045 (Sonnet-Quadrant der Vergleichsmatrix auf 6 Slugs, open)**. Nächste Bricks: Brief 046 (Opus-Quadrant + Cross-Aggregat, pending nach 045-Resultat) + Brief 047 (Hand-Check + Override-Schema oder Vokabular-Erweiterung, abhängig vom Aggregat). Brief 033 (Daily-Drift-Plan, zurückgezogen vor Handover) liegt in `archive/2026-05/` mit Retraction-Banner.

Archived sessions live in [`archive/2026-04/`](archive/2026-04/) — Phase 1 (bootstrap, handoff), Phase 1.1 (stack bumps), Phase 2.0 (CSS + Hub + chrome), Aquila redesign side-quest, Phase 2a slim (Timeline-Overview + EraDetail), Timeline-Polish — und [`archive/2026-05/`](archive/2026-05/) — Phase 1.5 (Build-Hygiene + CI), Phase 2a Schema-Foundation, Phase 2b Rich-Seed, Phase 2c.0 Era-Anchor, Phase 2c.1 DetailPanel + Deep-Link, Phase 2c.2 Hygiene-Pack, Phase 2a.2 FilterRail-MVP, Phase 3 Brief 033 (Daily-Drift, retracted before handover).
