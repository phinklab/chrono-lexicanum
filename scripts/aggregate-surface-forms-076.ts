/**
 * aggregate-surface-forms-076.ts — RETIRED (Brief 171 Teil B).
 *
 * A one-off Brief-076 Phase-0 helper that aggregated surface forms across
 * `manual-overrides-ssot-w40k-016..020` for resolver-pass-4 targeting. The
 * resolver batch world is now frozen provenance (the corpus lives in
 * scripts/seed-data/books/*.json). Retired; original logic is in git history.
 */
console.error(
  "[aggregate-surface-forms-076] RETIRED (Brief 171). One-off resolver dossier helper; batch world frozen.",
);
process.exit(1);
