/**
 * strip-unknown-facets-074.ts — RETIRED (Brief 171 Teil B).
 *
 * A one-off Brief-074 mutator that rewrote `manual-overrides-ssot-w40k-011..015`
 * to drop unknown facet ids. The batch files are now FROZEN provenance (the
 * corpus lives in scripts/seed-data/books/*.json), so a tool that WRITES them
 * would violate the freeze. Retired; the original logic is in git history.
 */
console.error(
  "[strip-unknown-facets-074] RETIRED (Brief 171). One-off batch mutator; batch files are frozen.",
);
process.exit(1);
