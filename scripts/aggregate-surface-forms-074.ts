/**
 * aggregate-surface-forms-074.ts — RETIRED (Brief 171 Teil B).
 *
 * A one-off Brief-074 helper that printed frequency-sorted surface forms across
 * `manual-overrides-ssot-w40k-011..015` to pick resolver targets. The resolver
 * batch world is now frozen provenance (the corpus lives in
 * scripts/seed-data/books/*.json). Retired; original logic is in git history.
 */
console.error(
  "[aggregate-surface-forms-074] RETIRED (Brief 171). One-off resolver dossier helper; batch world frozen.",
);
process.exit(1);
