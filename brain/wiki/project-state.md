---
title: Project state
type: overview
created: 2026-05-09
updated: 2026-06-01
# 2026-06-01 (Post-Merge-Koordinations-Pass post-PR-118 / Brief 104 alias-aware-drift): Brief 104 (`alias-aware-drift`, Product) gemergt (PR #118, `1142c82`) — geteiltes Alias-Modul `src/lib/aliases/index.ts` (SSOT = statische Imports der `scripts/seed-data/*-aliases.json` + Entity-JSONs, Resolver-Pattern, keine Kopie) + drei-Zustands-Drift (none / known-alias / suspicious), **entity-genau**, an allen drei Call-Sites (`queries.ts`, `buecher/page.tsx`, `buch/[slug]/audit/page.tsx`) durch `classifyDrift`/`tallyAxisDrift` geroutet (dedupliziert die drei `countResolvedDrift`/`isDrift`-Kopien). Known-alias = ruhige cyan-Klasse (`ALIAS n`-Chip + inline `· alias «Surface → Canonical»` + optionale `?audit=alias`-Pille, via geteilter `AuditPills` auch auf `/atlas/werke`). Such-Contract `resolveSurfaceForm(query) → AliasResolution[]` exponiert (normalisiert, achsen-übergreifend, Liste), **nicht** verdrahtet, Test-belegt (`scripts/test-aliases.ts` 15/0). **Headline: `drift_works` 380 → 0** — alle 500 Drift-Junctions über 380 Bücher sind registrierte Edition-Renames, null suspicious drift (größer als die ~133-Schätzung = nur Top-3-Cluster; volle Maps × 3 Achsen ≈ 500). Befund gemessen *und* logisch zwingend (Resolver speichert nur `(rawName,id)` mit `alias→id`). Kein Schema/DB/Resolver/Override-Touch; `typecheck`/`lint`/`test:resolver` 473/0 grün. **Cowork-Review: sauber.** **Edition-Rename-Alias-Cluster-Watch-Item (§ What's open) geschlossen** (korrigierte Prämisse: der Hebel saß in der App-Schicht, nicht in den Daten). Vier next-brief/maintainer-Folgen als konsolidiertes Watch-Item in § What's open: (a) `?audit=drift` dauerhaft leer → Drift-Pille/Brief-103-Sub-Sort behalten/umlabeln/zurückbauen; (b) Alias-Klasse always-on auf ~44 % der Bücher → ggf. detail-only/hinter `?audit=alias`; (c) `resolveSurfaceForm`-Verdrahtung = Brief 109 Step 3; (d) maintainer-Visual-Pass (CC nur curl-Smoke). **Keine OQ-Bewegung** — die vier Brief-104-§-Open-questions sind im Impl-Report beantwortet; OQ (3) + (13) bleiben offen. Doc-only auf `main`.
# 2026-06-01 (Cowork-Maintainer-Edit — Brief 106 zurückgezogen): Philipp hat Brief 106 (Hero-Upscaling + Ambient-Motion-Varianten-Lab, Product) als vorschnell erstellt + nicht ausreichend durchdacht **zurückgezogen** — `status: open → archived`, nie implementiert (kein Code, kein PR, keine Assets). Die Hero-Polish-Linie ist damit **nicht** mehr offen; wird sie später aufgegriffen, entsteht ein frischer Brief mit neuer NNN (der 106-Body bleibt als Ausgangsmaterial erhalten). Physischer Archive-Move (→ `sessions/archive/2026-05/`) reitet auf dem ausstehenden Sessions-Archive-Sweep, dessen Scope jetzt 106 mit einschließt. **Keine OQ-Bewegung** — Hero-Polish-Linie ohne numerierten-OQ-Bezug; OQ (3) + (13) bleiben offen. Doc-only auf `main`.
# 2026-05-30 (Post-Merge-Koordinations-Pass post-Brief-107 / Full-Rebuild-Restore-Wiring): Brief 107 (`full-rebuild-restore-wiring`, Batches) gemergt — **der Cowork-Review-Watch-Item aus dem Brief-105-Review ist geschlossen, Cluster A damit vollständig abgeschlossen**. Ein neuer dünner, confirm-gegateter `db:rebuild`-Orchestrator (`scripts/db-rebuild.sh`) verkettet `db:reset-for-ssot --confirm` → Voll-Korpus-Apply-Wellen (`bash run-phase4-apply.sh scripts/db-rebuild.config.json`, `applyRanges` W40K 1..57 + HH 1..30 = alle 859) → `apply:audiobook-narrators` (finaler Tail — resolvt über `works.external_book_id`, braucht die `works`) → `apply:audiobook-narrators --verify` (read-only). Damit stellt ein voller SSOT-Rebuild die 88 Audio-`work_persons`-Rows **deterministisch** wieder her — vorher löschte `db:reset-for-ssot` (TRUNCATE works CASCADE) sie still, weil keine Orchestrierung den Audio-Apply nachzog (`run-phase4-apply.sh` ruft nur `db:apply-override` = nur author/editor). Neue Artefakte: `scripts/db-rebuild.sh` (153 Z., Confirm-Gate + Fail-fast-`step()`-Runner, Pattern aus `db-reset-for-ssot.ts`/`run-phase4-apply.sh`), `scripts/db-rebuild.config.json` (eigene sprechende Voll-Korpus-Config, **NICHT** `consolidation-pass-2.config.json` zweckentfremdet), `sessions/db-rebuild-runbook.md`; `package.json` +`db:rebuild`; `apply-audiobook-narrators.ts` **rein additiv** um einen `--verify`-Modus erweitert (Apply-/Dry-Pfad unverändert). **Verify = exakte `(workId, personId, role)`-Mengen-Gleichheit gegen die Sidecar-Erwartung** (Codex-Review-Härtung: eine reine Gesamtzahl false-positivt, wenn ein Rollen-Überschuss ein Defizit maskiert — Negativ-Test bewiesen), Sidecar-abgeleitet (heute 88 = narrator 63 / co_narrator 12 / full_cast 13), **nicht literal** → der spätere 859er-Audiobook-Full-Sweep braucht keinen Verify-Edit. `apply-override.ts` / `run-phase4-apply.sh` / `db-reset-for-ssot.ts` Internals **unverändert** (der Orchestrator ruft sie nur); `db:seed` bestätigt nicht auf dem SSOT-Rebuild-Pfad, unangetastet. **Cowork-Review: vollständig + sauber, Verify stärker als gefordert.** Destruktiver End-to-End-Lauf bewusst **maintainer-owned** (nicht von CC gegen die Live-DB gefahren — sie steht bereits im Ziel-Endzustand); jede Komponente einzeln non-destruktiv grün (Confirm-Gate nackt → exit 1 ohne Truncate; Verify-OK read-only → exit 0; Masking-Negativtest → exit 1; `typecheck`/`lint`/`brain:lint --no-write` 0-blocking; `test:resolver` 473/0, `test:resolver-data`/`-coverage`, `test:apply-override-dry` out-of-range=0, `test:collection-refs` 10/0, `test:audiobook-narrators` 12/0). Live-DB-Baseline read-only: `works=859`, `work_persons=873` (785 author/editor + 88 audio), `work_factions=2754`, `work_locations=1146`, `work_characters=2000`, `work_collections=196`. **Keine OQ-Bewegung** — Watch-Item ohne numerierten-OQ-Bezug; die vier Brief-107-§-Open-questions sind im Impl-Report beantwortet (bash-Geschwister; eigene `db-rebuild.config.json`; `--verify` am Worker; Reference-`persons` überleben den Reset verwaist + Audio-Apply legt sie idempotent FK-sicher an). Kein Schema-/DB-Touch durch den Rollup — doc-only auf `main` direkt.
# 2026-05-30 (Nachtrag post-PR-114 / Brief 105 UI-Pass gemergt — Cluster A komplett): PR #114 `c4d019c` („Brief 105 UI-Pass: Buy/Listen-Links + Hörbuch-Credits-Render", Product) ist gemergt — die UI-Hälfte von Cluster A. **Brief 105 jetzt `status: implemented`** (beide Pässe; CC hat im PR-114-Commit geflippt). Landete: `src/lib/store-links.ts` (geo-lokalisierendes, affiliate-bereites `buildStoreUrl` — Amazon/Audible-TLD-Swap + Black-Library-Suche; kein Tag, aktivierbar via `STORE_AFFILIATE_TAGS` ohne Call-Site-Änderung), `src/lib/store-region.ts` (server-seitige Region: `?store=` > `x-vercel-ip-country` > Accept-Language > US), `src/components/book/{BuyListenActions,AudioCredit,RegionSwitcher}.tsx` (AudioCredit in single/duet/ensemble/full-cast), `/buch/[slug]` Personen-Query **orderBy-Fix (L73)** + displayOrder + ISBN-Select + Rail unter dem Cover, `.env.example` (`STORE_AFFILIATE_TAGS` leer). **ISBN-Select adressiert den Short-Title-Store-Link-Befund aus dem Daten-Review** (Legion/Nemesis/Censure/… via ISBN statt mehrdeutiger Such-URL). Watch-Item „Brief-105-UI-Pass" damit erledigt; offene Cluster-A-Folge bleibt nur das **Full-Rebuild-Restore-Wiring** (eigener Brief). **Keine OQ-Bewegung.** (Mit-committed: die untracked arch-Briefe 104 + 106.)
# 2026-05-30 (Post-Merge-Koordinations-Pass post-PR-112 + post-PR-113 / Brief 105 Daten-Pass [Hörbuch-Credits] + Brief 096 Phasen G+H [Chronicle/Ask-Redesign]): Zwei Strang-PRs seit dem letzten Rollup gemergt. **PR #112 `c9b7c55` (Brief 105 Daten-Pass, branch `codex/ingest-batches-audiobook-credits`) — Cluster A, Daten-Hälfte.** Audiobook-Credits für **66 Bücher** (33 HH / 33 W40K) in einen committed Sidecar `scripts/seed-data/audiobook-narrators.json` gesourct (keyed by `externalBookId`, je Credit name/role/sourceUrl/checkedAt/confidence + optional note) und durabel auf `persons` + `work_persons` appliziert: **88 work_persons-Audio-Rollen-Rows** (narrator 63 / co_narrator 12 / full_cast 13), **29 neue Performer in `persons`**; Buch-Split single-narrator 58 / multi-narrator 5 / full-cast 3; 4 `no_audiobook`-Audit-Einträge (alle pre-2008-Titel — BL-Audio-Programm post-datiert sie). **Der P1-Durability-Fix ist in + live-bewiesen:** der Override-Apply-Delete (`scripts/apply-override.ts` ~L990) ist auf `inArray(role, ["author","editor"])` gescopt — eine Resolver/SSOT-Re-Apply einer audio-bearing Batch lässt narrator/co_narrator/full_cast jetzt unangetastet (ssot-hh-001-Re-Apply hält `{author:9, narrator:10, co_narrator:4}` identisch vor/nach; vor dem Fix hätte er alle 14 Audio-Rows gewischt). Neue Artefakte: `scripts/apply-audiobook-narrators.ts` (idempotenter Apply mit `--dry-run`/`--file`, voll-validierend vor jeder DB-Mutation, per-Buch scoped Delete-then-Insert in einer Transaktion, Auflösung über `works.external_book_id` UNIQUE), `src/lib/seed/persons.ts` (slugifyPerson + deriveNameSort aus apply-override extrahiert — Brief-077-`alignment.ts`-Pattern, garantiert Slug-Parität zwischen Author- und Audio-Pfad), `scripts/test-audiobook-narrators.ts` (DB-frei, 12/12). Quelle: per-Buch-Websuche → Amazon/Audible-Byline (gleiches Goodreads-Muster; Spot-Check ~18 Bücher vor Bulk). Storefront-Links + Render = Brief-105-**UI-Pass, noch offen** (Product). **Cowork-Review (diese Session): akkurat, stabil, sinnvoll.** Alle Coverage-Zahlen via `jq` unabhängig verifiziert + 3 Credits web-gegengeprüft (For-the-Emperor = Perring + Rawlins + Gregory, Butcher's-Nails-Cast, Nightbringer = Mackinnon — alle exakt); Durability-Delete ist der *einzige* `delete(workPersons)` in apply-override (Batch-Summary-Region nur lesend); Validierung lehnt doppelte (person, role) je Buch ab (PK-sicher). **Review-Befund → neues Watch-Item:** der Fix schützt nur die *routinemäßige* Re-Apply; ein voller Rebuild (`db:reset-for-ssot` TRUNCATE CASCADE / `db:seed`) löscht die Audio-Credits, und **keine** Orchestrierung (`run-phase4-apply*.sh` rufen nur `db:apply-override`) ruft `apply:audiobook-narrators` — der Schritt gehört in die Reset-/Rebuild-Sequenz, sonst gehen die Credits bei einem Rebuild still verloren. **PR #113 `b60b0fb` (Chronicle-Redesign + UI-Pass: Timeline-Port, Ask/Buecher/Map) — Brief-096 Phasen G (Chronicle/Timeline bespoke) + H (Ask-Funnel) Material aus dem `chrono-lexicanum-product`-Worktree, lokal-iterativ OHNE eigenen Impl-Report (096-Konvention).** Neue Chronicle-Komponenten-Suite `src/components/timeline/chronicle/*` (AccordionShell, BookNode, BookTooltip, ChronicleClient, LayoutLanes, LayoutSequence, OverviewRibbon, SegmentView, ZoomInShell, context), `src/lib/chronicle/roster.ts` + `src/lib/timeline.ts` + `src/lib/timelineAdapter.ts`, `src/app/timeline/page.tsx`-Rewrite (`loading.tsx` entfernt), Ask-Redesign `src/components/ask/*` + `src/app/ask/page.tsx`, `src/app/buecher/ScrollScrim.tsx`, Lab-Prototyp `public/lab/timeline-prototype/*`, `globals.css` +928. **Noch nicht Cowork-reviewt; eigene Sichtung + 096-Status-Entscheidung (open → implemented?) stehen aus — Watch-Item.** Counts: `work_persons` 785 → **873** (+88 Audio-Rollen); `persons` +29 Performer-Rows. Keine Schema-Migration, keine `external_links`-Rows (Storefront-Links sind UI-derived). **Keine OQ-Schließung, keine neue numerierte OQ** — beide PRs sind Strang-Arbeit ohne numerierten OQ-Bezug; OQ (3) + OQ (13) bleiben offen. **Neue Watch-Items in § What's open:** Full-Rebuild-Restore-Wiring, Brief-105-UI-Pass, PR-#113-Sichtung + 096-Status, 859er-Audiobook-Full-Sweep (~790 Bücher Langzahn), Audio-Drama-Cast-Tiefe + Spelling-Watch (Charlotte Page/Paige, Seán Barrett), Short-Title-Store-Link-Kollisionen (Legion/Nemesis/Censure/Krieg → ggf. `external_links`-Override später).
# 2026-05-28 (Post-Merge-Koordinations-Pass post-PR-110 + post-PR-111 / Brief 096 Phasen A–F + Brief 103 UI-Pass — Product-Strang gemergt): PR #110 (`Design-Direction UI: Map, Hub, Ask, Atlas-Inventory, MediaPlayer`, commit `59213c3`, 147 Files / +21 578 / -1 082) und PR #111 (Brief 103 UI-Pass `Audio-Drama-Daempfung + Drift-Tie-Group-Sub-Sort`, commit `b1b4746`, 5 Files / +312 / -10) sind beide gemergt. **Korpus bleibt datenkomplett + konsolidiert (859/859 = 565 W40K + 294 HH).** PR #110 ist die große Product-Strang-Konsolidierung aus dem `chrono-lexicanum-product`-Worktree mit drei zusammen geschnürten Linien: (a) **Brief 096 Phasen A–F** (impl-Report `2026-05-23-096-impl-design-direction.md`, vier Commits d37c590 / 492b352 / ed611c1 / a55679a) — globaler Shell + Hub (A+B), `/buecher`-Reskin (C), `/buch/[slug]` Bespoke (D), Stub-Shells für `/fraktion` / `/welt` / `/charakter` / `/ask` / `/map` (E), internal-tools Token-Aliasing-Verify (F); Cinzel / Cormorant Garamond / IBM Plex Mono via `next/font/google`, `SiteBackground` + `TopNav`, zwölf neue Chrono-Primitives unter `src/components/chrono/*`. (b) **Map-/Atlas-Inventories** — `/lib/galaxy/*` (coords/data/eras/share/stars/storage/themes/types — strukturierte Galaxy-Domäne aus dem Lab-Cartographer-Prototyp Brief 079), `/lib/atlas/*` (auth/decks/format/queries/types), `/lib/audio-tracks.ts` + `/lib/askPaths.ts`, `src/components/map/disc/*` + `src/components/map/editor/*` + `src/components/map/tweaks/*` + `src/proxy.ts`. (c) **Zwei Map-Polish-Followups** auf demselben Product-Branch — `2026-05-27-098-impl-map-dive-flicker-followup.md` (Dive-Curtain + delayed-view-switch + capped Preview-Zoom + Mechanicus-`SELECTING SEGMENTUM...`-Copy gegen Chrome-Flicker) und `2026-05-27-099-impl-map-transition-polish.md` (Reticle-Entfernung + 180→560ms settle-Window + Tint/Scanlines durch transitions stabil + `animsOn`-Gating für SMIL-Hazard-Zones in dived Segmentum). Brief 096 **bleibt `status: open`** — Phasen G (Timeline bespoke) + H (Ask 6-path funnel) sind im 096-impl explizit deferred, das Brief-Container-Vehikel trägt sie weiter im Product-Worktree (`lokal-iterativ`-Konvention aus dem Brief). PR #111 ist der **Brief-103-UI-Pass** mit Tight Diff im `/buecher`-Cockpit: im **Single-Gap-Modus** (`?audit=gap` als *einziger* Filter, AND-Semantik `auditFilters.length === 1 && auditFilters[0] === 'gap'` explizit, nicht naives `.includes('gap')`) werden Audio-Dramen auf `opacity:.4` gedämpft + mit kleinerem entsättigtem Faction-Dot (10px → 5px) + Mono-`Audio`-Tag im `__main` markiert (überlebt Mobile-Spaltenkollaps, volle Opacity auf `:hover`/`[open]`); URL-getriebene Toggle-Pille `?hideAudio=1` (kein localStorage, separater Param statt `audit=`-Pillen-Erweiterung — Anzeige-Toggle ≠ Audit-Filter) versteckt sie hart. Drift-Tie-Group flach `drift_count=2 / confidence=1.00` ist jetzt nach `driftScore = sum(globalDriftFreq(rawName))` über resolved drift-rawNames primär + Axis-Triple lex-DESC `(faction, location, character)` sekundär + `updatedAt DESC` tertiär sub-sortiert; pro Row rendert ein sichtbares `· drift «Surface-Form» ×N`-Signal die Top-Häufigkeits-Surface-Form. **Smoke-Cross-Check:** `?audit=gap&hideAudio=1` zeigt **325 Rows** = exakt die `audit:gap-candidates`-Helper-Zahl aus dem Daten-Pass (Daten-Pass-Helper-Skript und UI-Filter teilen dasselbe Prädikat `hasJunctionGap && format != audio_drama`). Brief 103 jetzt **`status: implemented`** (CC hat im PR-111-Commit geflippt — beide Hälften durch). **Wichtigster Befund aus UI-impl OQ 2:** die globale rawName-Häufigkeit identifiziert die fettesten Edition-Rename-Alias-Kandidaten — Imperial Guard ×77 → `astra_militarum`, Eldar ×34 → `aeldari`, Isstvan V ×26, Dark Eldar ×22 → `drukhari`, Mechanicum ×19, Horus Lupercal ×19 → `horus`, Sisters of Silence ×16, Adeptus Ministorum ×14, Knights-Errant ×13, Tau Empire ×12, Imperial Army ×12, Lucius ×11. Die Top-3 Edition-Renames (Imperial Guard / Eldar / Dark Eldar) sind reine Synonyme, **keine** Cross-Era-Identitäten — eine ADR-konforme Alias-Eintrag pro Form (`scripts/seed-data/faction-aliases.json` o.ä.) würde 77+34+22 = ~133 Junctions aus dem Drift-Bucket nehmen, ohne Resolver-Logik anzufassen. Klein-Diff-Brief-Kandidat mit hohem ROI; gehört vor jeden weiteren Audit-Cockpit-Refinement-Brief. **Disziplin-Befund:** die zwei Product-Strang-Followup-Files `2026-05-27-098-impl-map-dive-flicker-followup.md` + `2026-05-27-099-impl-map-transition-polish.md` reuse NNN=098 + NNN=099, die schon vom Batches-Strang vergeben sind (W40K-Konsolidierungs-Pass bzw. Sessions-Archiv-Sweep). `brain/wiki/workflows/sessions-format.md` spezifiziert NNN als **projektweit monoton**; die zwei Files müssen beim nächsten Sessions-Archive-Sweep umgenannt werden (Vorschlag: nächste freie NNN ab `104` und `105`, mit `parent`-Frontmatter-Anpassung in der zweiten Datei und Pfad-Rewrites in den `links:`-Listen falls referenziert). **Keine OQ-Schließung, keine neue numerierte OQ** — beide PRs sind UI-Strang ohne numerierten OQ-Bezug; alle sechs Brief-103-§-Open-questions sind jetzt komplett beantwortet (Q1+Q4 im Daten-impl, Q2+Q3+Q5+Q6 im UI-impl). Offene Queue unverändert: OQ (3) Hand-Check-Workflow, OQ (13) Crawl-Simplification-Sichtung. **Watch-Items neu / aktualisiert im § What's open:** (a) **Edition-Rename-Alias-Cluster-Brief** (Top-Empfehlung für den nächsten Architekten-Brief — kleiner Batches-Pass, hoher ROI), (b) **Sessions-Archive-Sweep + Numbering-Collision-Fix** (100/101/102/103/Pass-11..15-Dossiers/consolidation-pass-2-* + die zwei NNN=098/099-Reuse-Files; Brief-099-Style Hygiene-Pass), (c) **Brief-096 Phasen G + H** (Timeline bespoke + Ask 6-path funnel — bleiben `lokal-iterativ` im Product-Worktree), (d) **`/buch/[slug]/audit`-Detail-Refinement** (UI + Daten verzahnen), (e) **Audio-Drama-Dämpfung evtl. site-weit übertragbar** (heute nur Single-Gap-Cockpit, public `/buecher` könnte `book.format === 'audio_drama'` + `.catalogue-row__audio-tag` wiederverwenden), (f) **Cleanup-Pass auf Legacy-Tokens** (`--color-void / --color-aquila / --color-frost-*` post-096 möglicherweise unused — Code-Review-Sweep), (g) Maintainer-Excel-Sweep + Pelago-Tags-Review + Audit-Helper-Snapshot-Persistenz + Aggregator-Pass-3-Refinement (alle aus den post-PR-108/109-Pässen, unverändert).
# 2026-05-28 (Post-Merge-Koordinations-Pass post-PR-109 / Brief 103 Daten-Pass): PR #109 (Brief 103 Daten-Pass, branch `codex/ingest-batches-audit-pilot`) ist gemergt. **Korpus bleibt datenkomplett + konsolidiert (859/859 = 565 W40K + 294 HH).** Der Daten-Pass landet drei Dinge: (a) `npm run audit:gap-candidates` als read-only Triage-Helper über `works ⨝ book_details` mit `kind='book' AND format <> 'audio_drama' AND (factions=0 OR locations=0 OR characters=0)` — deterministischer Konsolen-Output + Markdown-Snapshot unter `ingest/.last-run/audit-gap-candidates.md`, idempotent, kein DB-Write; (b) zwei Pilot-Override-Backfills HH-0260 *Hunter's Moon* (Russ + Alpharius als `mentioned` im HH-026 Override, neuer `pelago`-Anchor in `locations.json` mit `tags=["space_wolves"]`) und HH-0270 *Iron Corpses* (neuer `koparnos`-Anchor in `characters.json` mit `primaryFactionId="iron_warriors"`, Override-File unverändert — Resolver greift den bestehenden POV-Eintrag, sobald der Anker steht); (c) Brief-Status-Flip `open → implemented` im selben PR (Brief 103 § Notes idempotent-Konvention — UI-Pass kann das später halten). **Counts:** characters `489 → 490` (+1 koparnos), locations `288 → 289` (+1 pelago), character-aliases `66`, NEW-Range gap_works im `verify-pass.ts`-Digest **18 → 16** (-2 = HH-0260 + HH-0270, exakt der Brief-Forecast), drift_works 25 (unverändert — Drift-Achse nicht angefasst), Smoke-Slugs HH-0260 `f=2/l=1/c=2` + HH-0270 `f=1/l=1/c=1` (Gap geschlossen). Out-of-Range-Tripwire = **147** — Artefakt der HH-only `applyRange` im Pass-15-Config gegen den post-Brief-102 Full-Corpus-DB-Stand; Brief-102 Cross-Domain-Boundary-Logik erwartet 0 nur bei Multi-Range-Configs wie `consolidation-pass-2.config.json`. Keine Regression durch diesen PR. Alle Tests grün (typecheck, lint, `brain:lint`, `test:resolver` 473/0, `test:resolver-data` 10/0 inkl. neuer coverage-smoke-Check über `w40k-001..057 + hh-001..030`, `test:apply-override-dry` `out-of-range=0/unknown-work=0`, `test:resolver-coverage` Data-Findings nur, `test:collection-refs` 10/0). **Wichtiger Befund — Forecast-Korrektur:** der `audit:gap-candidates`-Helper-Raw-Output zeigt **325 Kandidaten** (HH=73, W40K=252 — ~78 % W40K-konzentriert; Achsen-Breakdown factions=0 → 29, locations=0 → 228, characters=0 → 194), nicht ~10–20. Der Brief-Forecast "~10–20 echte fixable" bezieht sich auf die post-Triage-Subset (Bücher mit explizit benannten Lore-Ankern, die der Pass-Implementierer übersprungen hat). Die rohe Helper-Liste ist breiter — viele frühere W40K-Paperbacks (Cain-Reihe `W40K-0012..0019`, Necromunda-Reihe `W40K-0163..0180`, Warhammer-Horror `W40K-0492..0527`) mit `confidence=1.00` haben locations=0 oder characters=0, weil die ursprüngliche Lexicanum-/OL-Crawl-Pipeline für diese Domäne dünner aufgesammelt hat. Pro-Pilot-Buch-Backfill-Effort war ~5 min Entscheidung + ~1 min Edit + ~3 min Verify; Skalier-Schätzung für 10–20 echte Fixables = 2–4 h Maintainer-Arbeit. Brief 103 § Notes hat den Maintainer-Excel-Sweep explizit als "kein einmaliger Architektur-Brief" markiert; die Restliste läuft als laufender Sweep, nicht als Cowork-Job. **Was noch zu Brief 103 fehlt:** der **UI-Pass** (Audio-Drama-Dämpfung im Gap-Bucket + Drift-Tie-Group-Sub-Sortierung) — vollständig in Brief 103 spezifiziert, kein neuer Brief nötig, läuft als nächster CC-Pass im `chrono-lexicanum-product`-Worktree. Brief 103 selbst trägt `status: implemented` (CC hat geflippt im PR-109-Commit) — die Active-Threads-Tabelle weist den UI-Pass-Rest separat aus. Keine OQ-Schließung, keine neue numerierte OQ; offene Queue unverändert: OQ (3) Hand-Check-Workflow, OQ (13) Crawl-Simplification-Sichtung. Watch-Items im § What's open: (a) `audit:gap-candidates`-Restliste (325 → ~10–20 via Triage, laufender Maintainer-Sweep), (b) Pelago-Tags-Review (heute `["space_wolves"]`, optional `["space_wolves", "alpha_legion"]` analog Calth — Single-Line-Edit, Drift/Gap-Verhalten unverändert), (c) Audit-Helper-Snapshot-Persistenz (`ingest/.last-run/audit-gap-candidates.md` wird pro Lauf überschrieben; für Time-Series-Verlauf wäre `ingest/audit-snapshots/YYYY-MM-DD-gap-candidates.md` ein 5-Zeilen-Add, heute nicht nötig — `git log` über die `.last-run`-Datei zeigt den Verlauf), (d) UI-Pass von Brief 103 (nächster CC-Job, Product-Strang).
# 2026-05-27 (Post-Merge-Koordinations-Pass post-PR-108 / Brief 102 HH-Konsolidierungs-Pass): PR #108 (HH-Konsolidierungs-Pass / Brief 102 impl, branch `codex/ingest-batches-hh-consolidation-pass`, 7 Commits c01a795..037b618) ist gemergt. **Korpus bleibt datenkomplett (859/859) und ist jetzt konsolidiert über beide Domänen.** Pass 2 fand 23 Kandidaten-Cluster → **2 Character-Merges** (cross-era lore-deep `merir_astelan → astelan` als chronologisch-pre-ADR-Carryover aus Pass 13; same-era mechanical `sharrowkyn → nykona_sharrowkyn` als Pass-11×Pass-13-Doublette), 21 No-Merges, 0 Flagged. **Wichtigster Befund**: cross-era-anchor-breach Aggregator-Tripwire = **0 Treffer** über alle 18 pinned Cross-Era-Surface-Forms — **positive Brief-100-ADR-Validation** über sechs HH-Wellen. Drei HH-aware Aggregator-Klassen scharfgeschaltet (Slug-edit-distance auf locations / Cross-era-anchor-breach als Re-Annotation auf alias-coincidence / Primarch-stem auf characters): 1 false-positive locations (`barbarus↔tartarus`), 1 false-positive characters (`horus↔horus_aximand`), 0 true-positives in den HH-Layern — beide Pass-2-Merges via Pass-1-Base-Heuristiken gefunden. Counts: characters `491 → 489` (-2 mergees), character-aliases `64 → 66` (+2), work_factions `2752 → 2754` (+2 re-apply-Drift), work_locations `1144 → 1145` (+1 re-apply-Drift), work_characters `1992 → 1997` (+5 = re-pointed Junctions auf die zwei Keepers via neue Aliases). `EXPECTED_RANGES.characters.max 2200 → 2500` (~25% headroom über work_characters=1997, Pass-15-locations-Pattern). `verify-pass.ts`-Out-of-Range-Digest als Brief-102-Bolt-on gelandet (`if (verify)`-guarded, config-agnostisch); Pass-2-Lauf = **0** (positive Tripwire am datenkomplettem Korpus). Multi-Range-Loader-Normalisierung (`AggregatorConfig.applyRanges: ApplyRange[]`, Singleton bleibt schreibbar als Backward-Kompat, Hard-Error bei beidem); `run-phase4-apply.sh` expandiert über die Range-Liste. Alle Tests grün (typecheck, lint, `brain:lint`, `test:resolver` 473/0, `test:resolver-data` 10/0, `test:apply-override-dry`, `test:resolver-coverage`, `test:collection-refs` 10/0). **Brief-094-§-Cadence-Bogen geschlossen** — Pass 2 war der verpflichtende finale Konsolidierungs-Pass nach der HH-Domäne; künftige Konsolidierungs-Pässe sind ad-hoc, nicht mehr im Cadence-Default. Keine OQ-Schließung, keine neue numerierte OQ; offene Queue unverändert: OQ (3) Hand-Check-Workflow, OQ (13) Crawl-Simplification-Sichtung. Watch-Items aus dem Impl-Report (alle out-of-scope für diesen Pass, durable in § What's open): Audit-Cockpit-Drift/Gap-Follow-up (~148 drift_works + ~98 gap_works über die HH-Domain), Aggregator-Pass-3-Refinement (auto-no-merge-rationale für `tags:vessel↔planet` + `primarch-stem↔captain-named-after-primarch`), Codex-Review-Sicht auf den Pass-2-Cross-Era-Identity-Handhabungs-Diff.
# 2026-05-27 (Post-Merge-Koordinations-Pass post-PR-107 / Resolver-Loop 5 Wellen): PR #107 (Resolver-Loop: 5 wave(s) HH `ssot-hh-003..030`, Pässe 11–15 auf branch `codex/ingest-batches-resolver-loop-hh`) gemergt — erster echter headless Mehr-Wellen-Lauf des `run-resolver-loop.sh`-Drivers. **DB 585 → 859 = Korpus datenkomplett (565 W40K + 294 HH).** Fünf clean Two-Domain-Pässe in Folge (Pass 11: 60 Bücher HH-0021..HH-0080 / Pass 12: 60 HH-0081..HH-0140 / Pass 13: 60 HH-0141..HH-0200 / Pass 14: 50 HH-0201..HH-0250 / Pass 15: 44 HH-0251..HH-0294 — exakt der Brief-100-Forecast 60/60/60/50/44), null `## Needs decision`-Stops, null facetId-Strips. EXPECTED_RANGES.locations.max in Pass 15 1100 → 1500 (Pass-14-forecasted Re-Tune, ~24% post-bump headroom); factions cap `3200` (2754 ≈ 14% headroom) und characters cap `2200` (1992 ≈ 9% headroom — tightest bound) ohne Re-Tune. Counts post-Pass-15-Re-Apply: `works=859`, `work_factions=2752`, `work_locations=1144`, `work_characters=1992`, `work_collections=196` (+49 seit Pass 10 — deferred Anthology-Constituent-Edges idempotent materialisiert), `work_persons=785`, `work_facets=16845`. Reference factions `179 → 202` / locations `234 → 288` / characters `404 → 491`. Cross-Era-ADR über den vollen HH-Korpus operativ gehärtet — `factionsSkippedRedundant`-Bucket für Cross-Era-Hits leer, der Brief-100-§4-Entwurf trägt; ADR um „HH-domain hardened: 2026-05-27"-Halbsatz ergänzt. Resolver-Loop emittiert jetzt `all-complete`. **Nächster Architekten-Brief: HH-Konsolidierungs-Pass** (schlank — Maschinerie aus Brief 098 existiert; potenzieller Bündel-Kandidat: Brief 101 § Open questions `verify-pass.ts`-Out-of-Range-Count). Keine OQ-Schließung, keine neue numerierte OQ; offene Queue unverändert: OQ (3) Hand-Check-Workflow, OQ (13) Crawl-Simplification-Sichtung.
# 2026-05-26 (Post-Merge-Koordinations-Pass post-101 + Pass 10): PR #106 (Brief 101 Guard-Fix Reason-Split) und PR #105 (Pass 10 HH-Bootstrap-Apply) gemergt. DB jetzt 585/859 — HH-Bootstrap-Welle (20 HH-Bücher, HH-0001..HH-0020) in Postgres. Brief 101 hat die `apply-override-dry.ts`-Assertion auf `reason === "unknown-work"` eingeschränkt + Reason-Breakdown-Konsolenausgabe + drei Test-Cases (a/b/c) in `test-apply-override-collections.ts` (10/0). Pass 10 hat im JSON-Reference-Layer +6 factions / +10 locations / +60 characters / +9 aliases gelandet (4 Faction-, 3 Character-, 2 Cross-Era-Honor-Title — Luna Wolves→sons_of_horus, Mechanicum→mechanicus, Imperial Army→astra_militarum, Lucius→lucius_the_eternal, Ezekyle Abaddon→abaddon_the_despoiler); Phase 4a re-run gegen den gefixten Guard durchgelaufen (DB 565→585); Phase 4b Verify-Digest clean: Smoke-Slugs `tales-of-heresy` (7f/2l/9c) + `the-primarchs` (6f/1l/5c) präsent, Rating-Coverage 20/20 Goodreads, NEW-Range Audit 20 total_works / 15 drift_works (Cross-Era-Alias-Resolving wirkt erwartungsgemäß — Surface-Forms zeigen auf eine Canonical-Row, exakt der Brief-100-§4-Entwurf) / 2 gap_works (Data-Findings, kein 4a-Fehler) / 0 content_in_collection für die 3 HH-Anthologien (out-of-range, materialisiert auf Folge-Wellen). Cross-Era-ADR (`decisions/cross-era-identities.md`) hat in der ersten HH-Welle sauber getragen — eine kanonische Identität = eine Canonical-Row, Era-Surface-Forms als Aliases. Nächster Schritt: ~5 weitere HH-Resolver-Wellen via `run-resolver-loop.sh` (operativ, kein Brief), dann HH-Konsolidierungs-Folge-Brief.
sources:
  - ../../sessions/archive/2026-05/2026-05-08-047-arch-pipeline-hardening.md
  - ../../sessions/archive/2026-05/2026-05-08-047-impl-pipeline-hardening.md
  - ../../sessions/archive/2026-05/2026-05-08-048-arch-doc-refresh.md
  - ../../sessions/archive/2026-05/2026-05-08-048-impl-doc-refresh.md
  - ../../sessions/archive/2026-05/2026-05-08-049-arch-karpathy-brain-atlas-reset.md
  - ../../sessions/archive/2026-05/2026-05-09-049-impl-karpathy-brain-atlas-reset.md
  - ../../sessions/archive/2026-05/2026-05-09-054-arch-pipeline-v2-pilot.md
  - ../../sessions/archive/2026-05/2026-05-09-054-impl-pipeline-v2-pilot.md
  - ../../sessions/archive/2026-05/2026-05-09-055-arch-v2-voll-lauf-decision-gate.md
  - ../../sessions/archive/2026-05/2026-05-09-055-impl-v2-voll-lauf-decision-gate.md
  - ../../sessions/archive/2026-05/2026-05-10-056-arch-v2-pre-roster-fixes.md
  - ../../sessions/archive/2026-05/2026-05-10-056-impl-v2-pre-roster-fixes.md
  - ../../sessions/archive/2026-05/2026-05-10-057-arch-excel-roster-import.md
  - ../../sessions/archive/2026-05/2026-05-10-057-impl-excel-ssot-import.md
  - ../../sessions/archive/2026-05/2026-05-11-058-arch-v2-ssot-mode-first-batch.md
  - ../../sessions/archive/2026-05/2026-05-11-058-impl-v2-ssot-mode-first-batch.md
  - ../../sessions/archive/2026-05/2026-05-11-060-arch-ssot-w40k-001-db-apply.md
  - ../../sessions/archive/2026-05/2026-05-11-060-impl-ssot-w40k-001-db-apply.md
  - ../../sessions/2026-05-11-061-arch-ssot-loop.md
  - ../../sessions/archive/2026-05/2026-05-11-062-arch-apply-override-author-fix.md
  - ../../sessions/archive/2026-05/2026-05-11-062-impl-apply-override-author-fix.md
  - ../../sessions/archive/2026-05/2026-05-12-063-arch-resolver-50-books.md
  - ../../sessions/archive/2026-05/2026-05-12-063-impl-resolver-50-books.md
  - ../../sessions/archive/2026-05/2026-05-12-067-impl-resolver-apply-readiness.md
  - ../../sessions/archive/2026-05/2026-05-12-069-impl-resolver-apply-evidence.md
  - ../../sessions/archive/2026-05/2026-05-13-070-arch-faction-policy-hygiene.md
  - ../../sessions/archive/2026-05/2026-05-13-070-impl-faction-policy-hygiene.md
  - ../../sessions/archive/2026-05/2026-05-13-071-arch-loop-driver.md
  - ../../sessions/archive/2026-05/2026-05-13-071-impl-loop-driver.md
  - ../../sessions/archive/2026-05/2026-05-14-072-arch-resolver-batch-2.md
  - ../../sessions/archive/2026-05/2026-05-14-072-impl-resolver-batch-2.md
  - ../../sessions/archive/2026-05/2026-05-15-074-arch-resolver-batch-3.md
  - ../../sessions/archive/2026-05/2026-05-15-074-impl-resolver-batch-3.md
  - ../../sessions/archive/2026-05/2026-05-15-075-arch-cockpit-drift-sort-and-rating.md
  - ../../sessions/archive/2026-05/2026-05-15-075-impl-cockpit-drift-sort-and-rating.md
  - ../../sessions/archive/2026-05/2026-05-16-076-arch-resolver-batch-4-axis-sliced.md
  - ../../sessions/archive/2026-05/2026-05-16-076-impl-resolver-batch-4-axis-sliced.md
  - ../../sessions/archive/2026-05/2026-05-16-077-arch-grand-alignment-junction-hygiene.md
  - ../../sessions/archive/2026-05/2026-05-16-077-impl-grand-alignment-junction-hygiene.md
  - ../../sessions/archive/2026-05/2026-05-17-079-impl-lab-cartographer-prototype.md
  - ../../sessions/archive/2026-05/2026-05-17-080-arch-synopsis-guard-and-pilot.md
  - ../../sessions/archive/2026-05/2026-05-17-080-impl-synopsis-guard-and-pilot.md
  - ../../sessions/archive/2026-05/2026-05-17-081-arch-ssot-synopsis-backfill-005-019.md
  - ../../sessions/archive/2026-05/2026-05-18-081-impl-ssot-synopsis-backfill-005-019.md
  - ../../sessions/archive/2026-05/2026-05-17-082-arch-parallel-worktrees.md
  - ../../sessions/archive/2026-05/2026-05-17-083-impl-parallel-worktrees.md
  - ../../sessions/archive/2026-05/2026-05-19-084-arch-locations-axis-hygiene.md
  - ../../sessions/archive/2026-05/2026-05-19-084-impl-locations-axis-hygiene.md
  - ../../sessions/archive/2026-05/2026-05-20-086-arch-hardcover-hit-rate-pass-2.md
  - ../../sessions/archive/2026-05/2026-05-20-086-impl-hardcover-hit-rate-pass-2.md
  - ../../sessions/archive/2026-05/2026-05-20-087-arch-goodreads-rating-pipeline.md
  - ../../sessions/archive/2026-05/2026-05-20-087-impl-goodreads-rating-pipeline.md
  - ../../sessions/archive/2026-05/2026-05-21-088-arch-ssot-loop-lean.md
  - ../../sessions/archive/2026-05/2026-05-21-088-impl-ssot-loop-lean.md
  - ../../sessions/archive/2026-05/2026-05-21-089-arch-resolver-pass-5.md
  - ../../sessions/archive/2026-05/2026-05-21-089-impl-resolver-pass-5.md
  - ../../sessions/archive/2026-05/2026-05-21-090-arch-resolver-pass-lean.md
  - ../../sessions/archive/2026-05/2026-05-21-090-impl-resolver-pass-lean.md
  - ../../sessions/archive/2026-05/2026-05-22-092-impl-roster-hygiene.md
  - ../../sessions/archive/2026-05/2026-05-22-093-arch-resolver-pass-7.md
  - ../../sessions/resolver-dossiers/resolver-pass-7-impl-report.md
  - ../../sessions/archive/2026-05/2026-05-23-094-arch-resolver-loop.md
  - ../../sessions/archive/2026-05/2026-05-23-094-impl-resolver-loop.md
  - ../../sessions/archive/2026-05/2026-05-23-095-arch-rollup-ownership.md
  - ../../sessions/archive/2026-05/2026-05-23-095-impl-rollup-ownership.md
  - ../../sessions/2026-05-25-098-arch-w40k-consolidation-pass.md
  - ../../sessions/2026-05-25-098-impl-w40k-consolidation-pass.md
  - ../../sessions/2026-05-25-099-arch-sessions-archive-sweep.md
  - ../../sessions/2026-05-26-100-arch-resolver-hh.md
  - ../../sessions/2026-05-26-100-impl-resolver-hh.md
  - ../../sessions/resolver-pass-runbook.md
  - ../../sessions/resolver-loop-log.md
  - ../../sessions/resolver-dossiers/resolver-pass-11-impl-report.md
  - ../../sessions/resolver-dossiers/resolver-pass-12-impl-report.md
  - ../../sessions/resolver-dossiers/resolver-pass-13-impl-report.md
  - ../../sessions/resolver-dossiers/resolver-pass-14-impl-report.md
  - ../../sessions/resolver-dossiers/resolver-pass-15-impl-report.md
  - ../../sessions/2026-05-27-102-arch-hh-consolidation-pass.md
  - ../../sessions/2026-05-27-102-impl-hh-consolidation-pass.md
  - ../../sessions/resolver-dossiers/consolidation-pass-2-dossier.md
  - ../../sessions/resolver-dossiers/consolidation-pass-2-aggregator-output.md
  - ../../sessions/resolver-dossiers/consolidation-pass-2-dry-run-plan.md
  - ../../sessions/resolver-dossiers/consolidation-pass-2-db-snapshot.json
  - ../../sessions/resolver-dossiers/consolidation-pass-2-merge-map.json
  - ../../sessions/resolver-dossiers/consolidation-pass-2-reference-premerge-snapshot.json
  - ../../sessions/consolidation-pass-runbook.md
  - ../../scripts/consolidation-pass-2.config.json
  - ../../scripts/resolver-pass.config.json
  - ../../scripts/resolver-loop-detect.ts
  - ../../scripts/run-resolver-loop.sh
  - ../../sessions/ssot-loop-log.md
  - ../../scripts/apply-override-synopsis-lint.ts
  - ../../scripts/seed-data/synopsis-banned-patterns.json
  - ../../scripts/run-ssot-loop.sh
  - ../../scripts/run-resolver-pass.sh
  - ../../scripts/apply-override-skip.ts
  - ../../src/lib/seed/alignment.ts
  - ../raw/reviews/2026-05-09-codex-v2-pilot-review.md
  - ../../sessions/README.md
  - ../../ROADMAP.md
  - ../../sessions/2026-05-27-103-arch-audit-drift-gap-sweep.md
  - ../../sessions/2026-05-28-103-impl-data-audit-drift-gap-sweep.md
  - ../../scripts/audit-gap-candidates.ts
  - ../../ingest/.last-run/audit-gap-candidates.md
  - ../../scripts/seed-data/manual-overrides-ssot-hh-026.json
  - ../../scripts/seed-data/locations.json
  - ../../scripts/seed-data/characters.json
related:
  - ./pipeline-state.md
  - ./open-questions.md
  - ./roadmap.md
  - ./architecture.md
  - ./decisions/why-excel-ssot-not-crawl.md
  - ./decisions/faction-policy.md
  - ./decisions/why-sonnet-not-haiku.md
  - ./decisions/hardcover-to-goodreads-pivot.md
  - ./decisions/cross-era-identities.md
confidence: high
---

# Project state — 2026-05-30 (post-PR-112 + post-PR-113 / Brief 105 Daten-Pass [Hörbuch-Credits, Batches] + Brief 096 Phasen G+H [Chronicle/Ask-Redesign, Product] gemergt)

> The "where are we now" anchor. Cowork and Claude Code start every session here (after `brain/CLAUDE.md` and `wiki/index.md`).

## Phase

**Phase 3 — Bulk-Backfill-Pipeline (in flight, SSOT-Authority-Layer aktiv. Der Korpus ist datenkomplett *und* über beide Domänen konsolidiert: 859/859 = 565 W40K + 294 HH-Bücher in Postgres applied (post-PR-107, 2026-05-27); zwei Konsolidierungs-Pässe (Pass 1 W40K-only, Pass 2 W40K + HH Full-Corpus) gefahren — Brief-094-§-Cadence-Bogen geschlossen. **PR #108 (Brief 102 / HH-Konsolidierungs-Pass) ist gemergt (2026-05-27):** 23 Kandidaten-Cluster → 2 Character-Merges (`merir_astelan→astelan` cross-era lore-deep, `sharrowkyn→nykona_sharrowkyn` same-era mechanical), 21 No-Merges, 0 Flagged. Cross-era-anchor-breach Aggregator-Tripwire 0 Treffer über 18 pinned Surface-Forms — positive Brief-100-ADR-Validation. characters 491→489, character-aliases 64→66, work_characters 1992→1997 (re-pointed). EXPECTED_RANGES.characters.max 2200→2500. `verify-pass.ts`-Out-of-Range-Bolt-on landed (config-agnostisch, `if (verify)`-guarded); Pass-2-Lauf=0. Multi-Range-Loader-Normalisierung (`applyRanges: ApplyRange[]`, Singleton-Backward-Kompat). PR #107 (Resolver-Loop: 5 wave(s) HH `ssot-hh-003..030`, Pässe 11–15) hat zuvor den ersten echten headless Mehr-Wellen-Lauf des `run-resolver-loop.sh`-Drivers gemerged — fünf clean Two-Domain-Pässe in Folge brachten +274 HH-Bücher (60/60/60/50/44 exakt nach Brief-100-Forecast), null `## Needs decision`-Stops, null facetId-Strips. Der Resolver-Loop emittiert jetzt `all-complete` (W40K 565/565 + HH 294/294). Cross-Era-ADR über den vollen HH-Korpus operativ gehärtet — und post-Konsolidierung erneut validiert (eine Cross-Era-Doublette als Pre-ADR-Carryover consolidated, 0 Tripwire-Treffer im neuen Aggregator). Nächster Architekten-Brief: offen, drei Kandidaten — (a) Audit-Cockpit-Drift/Gap-Sweep (Data-Quality über kumulativ ~148 drift_works + ~98 gap_works), (b) Phase-3-Seal-Brief (formaler Bogen-Schluss + Phase-4-Öffnung in der Roadmap), (c) Aufmerksamkeit zurück zu Brief 096 (Product/UI, lokal-iterativ). Pre-PR-107 — Resolver-Pass 9 (`ssot-w40k-052..057`, PR #96) hat die finale W40K-Welle gemerged. Die W40K-Bootstrap-Resolver-Kadenz (Pässe 1–9) ist abgeschlossen, Brief 098 (W40K-Konsolidierungs-Pass) und Brief 099 (Sessions-Archiv-Sweep) sind gemergt. Brief 100 (HH-Resolver-Domain-Öffnung) ist implementiert + gemergt — der Headless-Resolver-Loop ist jetzt **zwei-domänen-fähig** (W40K + HH), drei externe Terminal-Zustände (`open-wave | idle | all-complete`), HH-Bootstrap-Welle als `ssot-hh-001..002` (20 Bücher, Pass 10) verbindlich, EXPECTED_RANGES für HH-Headroom angehoben (`factions.max=2500`, `locations.max=1100`, `characters.max=2200`); SSOT-Loop hat alle 30 HH-Batches kristallisiert (`ssot-hh-001..030`, 294 Bücher in `main`). **Pass 10 (HH-Bootstrap-Apply) und Brief 101 (Guard-Reason-Split) sind gemergt (2026-05-26):** Brief 101 (PR #106) hat den Brief-091 forward-ref Guard in `scripts/apply-override-dry.ts` auf `reason === "unknown-work"` eingeschränkt — `out-of-range`-Constituent-Edges (Constituent steht im Roster, ist nur noch nicht in der Apply-Range) sind jetzt informativ im Console-Reason-Breakdown sichtbar, nur `unknown-work` bleibt scharfer Tripwire. Pass 10 (PR #105) lief mit dem gefixten Guard durch: JSON-Reference-Schicht +6 factions / +10 locations / +60 characters / +9 aliases, **DB 565 → 585** (+20 HH-Bücher HH-0001..HH-0020), Verify-Digest clean (Smoke-Slugs `tales-of-heresy` 7f/2l/9c + `the-primarchs` 6f/1l/5c, Rating-Coverage 20/20 Goodreads, NEW-Range Audit 20 total / 15 drift_works = Cross-Era-Aliases wirken erwartungsgemäß / 2 gap_works = Data-Findings / 0 content_in_collection — die 3 HH-Anthologien tragen ihre 20 Constituent-Edges out-of-range, materialisieren auf Folge-Wellen über den idempotenten kumulativen Re-Apply). Nächster Schritt — operativ, kein Brief: ~5 weitere HH-Resolver-Wellen via `run-resolver-loop.sh` bis `all-complete` (~60/60/60/50/44 Bücher), dann Korpus 859/859 plus HH-Konsolidierungs-Folge-Brief (schlank — Maschinerie aus Brief 098 existiert). Goodreads-Rating als vierte Loop-Disziplin live).** Der TypeScript-Ingestion-Stack bleibt im Repo bestehen, aber die V2-LLM-Stage (`src/lib/ingestion/v2/llm/`) ist seit dem Brief-061-Standing-Loop **de-facto ausgemustert** — eine `claude -p`-Subsession (Default-Modell, momentan Opus) produziert die Override-Datei direkt. ADR dazu: [`./decisions/why-cc-direct-curation.md`](./decisions/why-cc-direct-curation.md) (geschrieben 2026-05-15 in der Post-074-Wiki-Hygiene-Session). Konsequenz: OQ1 (Sonnet-vs-Haiku) und OQ2-(c) (`chaos`-pov_side-Prompt-Härtung) sind in ihrer ursprünglichen Pipeline-Form moot, beide in [`./deferred-questions.md`](./deferred-questions.md) geparkt mit Promote-Triggern. Discovery-Stage ist seit 057 nicht mehr der Default-Eingang: die Maintainer-kuratierte Excel-SSOT ist die Roster-Quelle. ADR: [`./decisions/why-excel-ssot-not-crawl.md`](./decisions/why-excel-ssot-not-crawl.md).

Sequenz seit 057: 058 baute den SSOT-Modus + ersten 10er-Batch; 060 schrieb `ssot-w40k-001` in die DB; 061 lief als Standing-Loop bis `ssot-w40k-005` (50 Bücher); 062 fixte den Author-FK-Pfad; 063–069 brachten den Resolver-Layer für die ersten 50 Bücher; 070 schloss die Faction-Policy-/Hierarchie-Hygiene; 072 hat den Resolver-Pass 2 für die zweiten 50 Bücher abgeschlossen (`heretic_astartes`-Mid-Knoten, Aeldari-/Drukhari-Aliase, Cross-Batch-`applyCollections`-Refactor); 073 hat das Maintainer-Audit-Cockpit gelandet (`/buch/[slug]/audit` + `/buecher` Audit-Pillen); PR #54 (`4993e17`, 2026-05-15) hat den ersten produktiven Loop-Driver-Run gemerged (5 Iterationen `ssot-w40k-011..015`, 50 Override-Files auf `main`); PR #57 (`6ac4295`, 2026-05-15) hat Brief 074 geschlossen (Resolver-Pass 3 + Watson-Trilogy historical-canon-layer + Green-Tide-`collection-gaps.json`-Ledger + 15× `db:apply-override` = 150 W40K-Bücher in Postgres); PR #59 (`8806284`, 2026-05-15/16) hat Brief 075 (Cockpit-Drift-Frequenz-Sort + slim `/buch/[slug]`-Komma-Zeile + Hardcover-Rating-Backfill, 77/150 = 51.3 %) abgeschlossen. **PR #62 (`cfb0957`, 2026-05-16) hat Brief 076 als axis-sliced Resolver-Pass 4 gemerged** — fünf manuell gefahrene Subsessions (Preflight/Dossier → Factions only → Locations only → Characters only → Integration), per-Phase-Statusdateien unter `sessions/resolver-dossiers/`, Cross-Batch-Alias-Consolidation für Mad Donna/D'onne Ulanti, Ragnar Thunderfist, Kage/the Burned Man (single-row) plus Lord Helmawr (Split), Soul Drinkers mit `tone='primaris_reboot_coexistent'`, plus Driver-Deliverable `scripts/run-resolver-pass.sh` + `scripts/resolver-pass.config.json` als Pass-5-Template, plus Mini-Phase 5 (Public-Synopsis-Discipline ab `ssot-w40k-021` in 061 verankert + im `run-ssot-loop.sh`-Trigger-Heredoc). Re-Apply 001..020 brachte die DB auf 200 W40K-Bücher mit Counts `work_factions=1185`, `work_locations=417`, `work_characters=633`, `work_collections=56`. **PR #65 / Commits `7d482c2`+`4d8d57e` (2026-05-16, im Anschluss konsolidiert per PR #64 `3b5c664` Wiki-Hygiene) hat Brief 077 als Grand-Alignment-Junction-Hygiene gemerged:** Pipeline-Compliance-Lücke aus Brief 070 geschlossen, Skip-Helper `scripts/apply-override-skip.ts` (pure DI-Funktion) + Alignment-Util `src/lib/seed/alignment.ts` (aus `seed-resolver-extensions.ts` extrahiert, shared zwischen Apply-Override + Seed-Skript), Skip-Logik via `redundantWhenSubPresent: ["imperium", "chaos"]` in `faction-policy.json` + Drei-Bedingungen-Skip im Apply-Layer (`scripts/apply-override.ts`), geskippte Surface-Forms im neuen `factionsSkippedRedundant`-Bucket im `book_details.notes`-`---surfaceForms---`-Block. Re-Apply 001..020 putzt 165 redundante Junctions weg: `work_factions=1185 → 1020 (−165)`, `imperium 81 → 6 (−75)`, `chaos 133 → 43 (−90)`, andere Axes invariant. Faction-Granularity-Discipline ab `ssot-w40k-021` in 061 + Trigger-Heredoc; ADR-Section „Grand-Alignment-Junction-Skip" in [`./decisions/faction-policy.md`](./decisions/faction-policy.md) mit Revisit-Trigger für künftige Aeldari-Sub-Splits (Alignment-Equality → Parent-Chain-Umstellung nötig). Loop-Status: ⏸ Pause bei 200 W40K-Büchern — durch 076-Re-Apply abgearbeitet, der nächste Loop-Lauf für `ssot-w40k-021..025` startet ohne Skip-Flag, `bash scripts/run-ssot-loop.sh 5`. **PR #67 (`9b9a839`, 2026-05-17) hat Brief 080 (Public-Synopsis-Forward-Guard + Pilot-Rewrite Batch 020) gemerged** — Apply-Layer-Pre-Pass mit 32 Banned-Patterns wirft hart bevor `works.synopsis` geschrieben wird, plus 10 Batch-020-Synopsen als Voice-Korridor-Referenz (412–556 chars, mean 475). **Brief 081 (Public-Synopsis-Backfill `ssot-w40k-005..019`, Branch `codex/ingest-batches-synopsis-005-019` mit 16 Commits — 1× arch-Brief + 15 Rewrite-Iterationen + Closing-Report `2026-05-18-081-impl-…md`, noch nicht gepusht / kein PR) hat die 123 polluted Synopsen über 15 Batches in einem Standing-`/clear`-Loop sauber bekommen** — pro Iteration eine Override-Datei, Voice-Korridor 502/605/783 chars (min/mean/max), `npm.cmd run test:synopsis-lint` 14/0, `test:apply-override-dry` reports alle Batches `001..020` synopsis-clean („by label: none"), `lint`/`typecheck`/`brain:lint --no-write` grün am 019er-End. **PR #66 (`67e39dd`, 2026-05-17) hat einen Lab-Cartographer-Prototype unter `/lab/cartographer` als isolierte Iframe-Sandbox gelandet** (Direkt-Maintainer-Anweisung, kein arch-Brief; produktive `/map`-Route unverändert, kein DB-Touch, `robots: { index: false }`). **PR #68 (`f551a13`, 2026-05-17) hat Brief 082 als Parallel-Worktree-Bootstrap gemerged**: zwei dauerhafte Worktrees `chrono-lexicanum-product` (Branch `worktree/product-bootstrap`) + `chrono-lexicanum-batches` (Branch `worktree/batches-bootstrap`) parallel zum Original-Coordination-Worktree, Strang-Disziplin (Product/UI vs. Batch/Ingestion) in `CLAUDE.md` + `AGENTS.md` § „Parallel worktrees" verankert, `main` read-only für lokale Arbeit. Seit Brief 082: Brief 084 (Locations-Axis-Hygiene), Brief 086 (Hardcover→Goodreads-Rating-Pivot), Brief 087 (Goodreads-Rating als vierte Loop-Disziplin), Brief 088 (SSOT-Loop schlank-refactored), der Loop-Lauf `ssot-w40k-021..025`, Brief 089 (Resolver-Pass 5, 250 Bücher applied), Brief 090 (Resolver-Pass lean — brief-frei + runbook-getrieben, Phase-4 digest-only, Resolver-Cadence 50→100) der erste 100er-Loop-Lauf `ssot-w40k-026..035` und Resolver-Pass 6 (350 W40K-Bücher in Postgres applied), Brief 091 (Resolver-Pass-Phase-4-Split), Session 092 (Roster-Excel-Hygiene-Sweep), der SSOT-Loop-Lauf `ssot-w40k-036..045`, Resolver-Pass 7 (`ssot-w40k-036..045`, **450 W40K-Bücher applied**), Brief 094 (Resolver-Loop — Entkopplung + Headless-Automatisierung) und Brief 095 (Rollup-Ownership) — Detail in der „Latest pipeline state (post-095)"-Sektion unten.

Phases 1 (foundation), 1.1 (stack bumps), 1.5 (build/deploy hygiene), 2 (Chronicle / Timeline), and 3a–3c (pipeline skeleton + aux sources + LLM enrichment) are shipped. Phase 4 (Discovery-Layer) and Phase 5 (Cartographer + Ask the Archive) follow Phase 3. See [`./roadmap.md`](./roadmap.md) for the full phase plan.

Das Buch-Domain-Bild post-098: **565 W40K-Bücher** (`ssot-w40k-001..057`) liegen als `works`/`book_details`/Junctions in Postgres — **W40K ist datenkomplett**. Junction-Counts post-Pass-9-Re-Apply `001..057`: `work_factions=1903`, `work_locations=733`, `work_characters=1220`, `work_collections=147`, `work_persons=524`, `work_facets=11291`. Reference-Schicht post-Konsolidierung (Brief 098): `factions=173`, `locations=224` (−1), `characters=344` (−1), `facet_values=86`; Aliases faction 59 / location 16 / character 43 (je +1 aus den zwei Konsolidierungs-Merges). DB-Rating-Coverage ~556/565 (jede Welle holt ihr Goodreads-Rating per Page-Read; die ~9 Lücken sind 2025/26-Releases ohne aggregierte Wertung). Alle Public-Synopsen sauber gegen die 32 Banned-Patterns (Apply-Layer-Forward-Guard, Brief 080). `EXPECTED_RANGES.factions.max` in Pass 9 auf 2100 angehoben (HH-Headroom). Der **Excel-SSOT-Roster** mit 859 Büchern + 196 Collections liegt deterministisch unter [`scripts/seed-data/book-roster.json`](../../scripts/seed-data/book-roster.json). Operative Reihenfolge: die W40K-Bootstrap-Resolver-Kadenz (Pässe 1–9) ist abgeschlossen, [Brief 098, der W40K-Konsolidierungs-Pass](../../sessions/2026-05-25-098-arch-w40k-consolidation-pass.md), ist **gemergt** — der neue Pass-Typ ist gebaut und erstmals über das W40K-Entitäten-Set gelaufen (15 Kandidaten → 2 Merges, 0 geflaggt). [Brief 099, der Sessions-Archiv-Sweep](../../sessions/2026-05-25-099-arch-sessions-archive-sweep.md), ist ebenfalls **gemergt** — 53 geschlossene Session-Files nach `archive/2026-05/`, der `sessions/`-Root trägt nur noch offene Briefs + die lebenden Dateien. Der nächste große Daten-Strang ist die HH-Domäne (294 Bücher, eigener Resolver-Brief, sobald der SSOT-Loop sie kristallisiert hat). Die vier Loop-Disziplinen (Public-Synopsis 076/080, Faction-Granularity + Grand-Alignment-Skip 077, Locations-Granularity + Umbrella-Skip 084, Goodreads-Rating 087) waren über die ganzen `046..057`-Wellen aktiv.

## Branch

`origin/main` trägt seit dem post-PR-107-Koordinations-Pass den HH-Konsolidierungs-Strang (2026-05-27): **PR #108 — Brief 102 / HH-Konsolidierungs-Pass** (branch `codex/ingest-batches-hh-consolidation-pass`, 7 Commits c01a795 → 037b618: Phase 0 Maschinerie+Aggregator → Phase 1 Factions → Phase 2 Locations → Phase 3 Characters → Phase 4a Maintainer-Gate → Phase 4b DB-Sync apply + Tests + Bolt-on + Cap-Re-Tune). Brief 102 selbst mit `status: open → implemented` im Code-PR mit-portiert. Pre-PR-108 trug `main` den großen HH-Loop-Strang: **Resolver-Loop 5 Wellen** (PR #107, Pässe 11–15, branch `codex/ingest-batches-resolver-loop-hh`) hat die HH-Domäne datenkomplett gemacht — fünf clean Two-Domain-Pässe in Folge brachten +274 HH-Bücher HH-0021..HH-0294 (Pass 11: 60 / Pass 12: 60 / Pass 13: 60 / Pass 14: 50 / Pass 15: 44 — exakt der Brief-100-Forecast); DB 585 → 859, Cross-Era-ADR operativ gehärtet, EXPECTED_RANGES.locations.max in Pass 15 1100 → 1500 (Pass-14-forecasted Re-Tune). Diese Cowork-Session 2026-05-27 (Post-Merge-Koordinations-Pass post-PR-108 / Brief 102 HH-Konsolidierungs-Pass) läuft im Coordination-Worktree `C:\Users\Phil\chrono-lexicanum` direkt auf `main` (Doc-only-Commit nach PR-Policy 2026-05-25 — siehe `CLAUDE.md` § Git / „PR policy"). Brief 100 lag bis zum Pull der Session lokal als untracked-Datei rum (die Vor-Session hatte ihn geschrieben, das Commit lief aber nicht aus diesem Worktree); CC hat ihn im 100-impl-PR mit-committet, der Pull dieser Session hat ihn dann sauber als tracked file mit `status: implemented` in den Worktree gebracht. Worktree-Layout drei produktive Pfade: Coordination/main (`C:\Users\Phil\chrono-lexicanum`, `main` read-only), Product/UI (`C:\Users\Phil\chrono-lexicanum-product` — trägt aktuell das laufende Brief-096-Redesign, lokal-iterativ), Batches/Ingestion (`C:\Users\Phil\chrono-lexicanum-batches`); zusätzlich Altlast `C:\Users\Phil\chrono-batches-011-015` (bewusst unangetastet per Brief 082). **Brief 095 ist scharf:** `sessions/README.md` + `brain/**` werden ausschließlich aus dem Coordination-Worktree geschrieben. Disziplin-Punkt aus 072 bleibt gültig: vor jedem Brief-/Wiki-Commit `brain:lint -- --no-write` lokal grün ziehen.

**Git-Sandbox-Hinweis.** Die Cowork-/Sandbox-Seite fasst `git` nie selbst an — die Mount-Schicht (9P/drvfs) korrumpiert git-Metadaten (NUL-Byte-Korruption; die `.git/config` wurde am 2026-05-20 manuell repariert, Backup `.git/config.corrupt-backup-20260520`). Diese Prävention ist seit 2026-05-22 fest in [`../../CLAUDE.md`](../../CLAUDE.md) § Git verankert: Cowork editiert ausschließlich Working-Tree-Dateien über die Datei-Tools, erfragt den Git-Stand beim Maintainer, alle git-Operationen laufen Windows-nativ durch Philipp.

## What's running

- **App on Vercel.** Hub + `/timeline` + `/timeline/[era]` + `/buch/[slug]` (DetailPanel mit slim „Auch enthalten in:"-Komma-Zeile post-075) + `/buch/[slug]/audit` + `/buecher` (Catalogue mit Audit-Pillen + Drift-Frequenz-Sort post-075) + `/ingest` (Phase 3.5 read-only Diff-Inspector) + `/healthz`. Live: <https://chrono-lexicanum.vercel.app/>.
- **CI on GitHub Actions.** `lint-and-typecheck` + `brain:lint --no-write` Jobs on every PR. Vercel does production builds; CI does *not* run `next build`.
- **DB on Supabase.** Pooler (port 6543) URL in `.env.local` zeigt heute auf prod (kein Test-Branch). Migrations durch `0009_lucky_pete_wisdom.sql` sind applied: 0008 brachte SSOT-Schema/`work_collections`, 0009 machte `locations.gx/gy` nullable und fügte `raw_name` auf die drei Work-Junctions hinzu. Die Resolver-Pässe 7–15 + Konsolidierungs-Pässe 1–2 + Brief-103-Daten-Pass brauchten **keine** Schema-Migration. Reference-Seeds post-PR-109 / Brief 103 Daten-Pass: `factions=202` (unverändert seit Pass 15), `locations=289` (+1 in Brief 103 Daten-Pass — neuer `pelago`-Anchor in `locations.json` mit `tags=["space_wolves"]`), `characters=490` (+1 in Brief 103 — neuer `koparnos`-Anchor mit `primaryFactionId="iron_warriors"`); Faction-Aliases ~73 (unverändert), Location-Aliases ~25 (unverändert), Character-Aliases **66** (unverändert — Brief 103 hat keine neuen Aliases, nur zwei neue Canonical-Rows). `facet_values=86` (unverändert — Brief 103 ist Daten-Backfill, keine Facet-Promotion). **Junction-Counts post-PR-109 Re-Apply `001..057 + hh-001..030` + DB-Sync (859 works):** `work_factions=2754` (unverändert — Brief 103 hat keine neuen Faction-Junctions), `work_locations=1146` (+1 — HH-0260 trägt Pelago), `work_characters=1999` (+2 — HH-0260 trägt Russ + Alpharius als `mentioned`, HH-0270 trägt Koparnos als `pov` — netto +2 weil koparnos eine neue Canonical-Row ist; tatsächlicher `test:resolver-coverage`-Output zeigt `characters=2000/2526`), `work_collections=196` (unverändert), `work_persons=785` (post-PR-109; **post-PR-112 / Brief 105 Daten-Pass: 873**, +88 Audio-Rollen-Rows narrator/co_narrator/full_cast über 66 Bücher), `work_facets=16845` (unverändert). `book_details.rating` bei **~820/859** — 119 `ratingSource='hardcover'` + ~701 `goodreads`; ~39 Bücher ohne aggregierte Wertung (2025/26-Releases + Edge-Cases). `persons.json` JSON-Side unverändert 100 Rows, DB-Side via `ensurePersonsExist` 785 Author-Slugs (+29 Performer-Rows post-PR-112 / Brief 105 Audiobook-Apply — narrator/cast). EXPECTED_RANGES post-Pass-2: factions max `3200` (currently 2754 ≈ 14% headroom), locations max `1500` (currently 1145 ≈ 24% headroom — in Pass 15 von 1100 → 1500 angehoben), characters max **`2500`** (currently 1997 ≈ 25% headroom — in Pass 2 von 2200 → 2500 angehoben, Brief-102-Cap-Re-Tune; Brief-094-§-Cadence-Bogen geschlossen).
- **Excel-SSOT-Roster.** `scripts/seed-data/source/Warhammer_Books_SSOT.xlsx` (Maintainer-extern gepflegt) + Loader `scripts/import-ssot-roster.ts` produziert deterministisch `scripts/seed-data/book-roster.json` (859 RosterBooks + 196 RosterCollections). Die Pipeline konsumiert den Roster seit 058; **DB-Stand 859/859 = Korpus datenkomplett** (post-PR-107, 2026-05-27): `ssot-w40k-001..057` (565 W40K-Bücher post-Pass-9, W40K datenkomplett) plus `ssot-hh-001..030` (294 HH-Bücher post-Pass-10..15, HH datenkomplett). Alle 859 Override-Files sind kristallisiert *und* in der DB. Resolver-Loop emittiert `all-complete`. Nächster Schritt: **HH-Konsolidierungs-Pass** (Cowork-Folge-Brief; Maschinerie aus Brief 098 existiert).
- **Pipeline V1 in dry-run** (Default ohne `--pipeline=` Flag, bleibt für Reproduzierbarkeit alter Diffs). `npm run ingest:backfill -- --limit N --offset M`. Latest V1 committed: `ingest/.archive/v1/backfill-20260508-2101.diff.json` (9 books, post-047 hardening, archived in 056).
- **Pipeline V2 / SSOT Authority path.** `npm run ingest:backfill -- --pipeline=v2 --source=ssot --batch=ssot-w40k-00N` erzeugt die Diff-/Override-Basis; `scripts/apply-override.ts` schreibt curated overrides in die DB. Apply-Layer ist post-077 erweitert um den `decideFactionSkips()`-Aufruf (pure helper in `scripts/apply-override-skip.ts`, DI-Signatur — der Resolver bleibt Pure-Surface-Form-zu-ID), **post-080 zusätzlich um den `validateSynopses()`-Batch-Level-Pre-Pass** (pure helper in `scripts/apply-override-synopsis-lint.ts`, 32 Banned-Patterns in `scripts/seed-data/synopsis-banned-patterns.json`, hard-throw mit `externalBookId + slug + batch + label + position + 40-char snippet` bevor `works.synopsis` geschrieben wird). Resolver-Support liegt in `src/lib/resolver/`, `scripts/seed-data/{faction,location,character}-aliases.json`, `scripts/seed-data/characters.json`, `scripts/test-resolver*.ts` und `docs/resolver-apply-runbook.md`. Shared Alignment-Util `src/lib/seed/alignment.ts` (Brief-077-Extract aus `seed-resolver-extensions.ts`) wird in beiden Pfaden (apply-override + seed-resolver-extensions) benutzt. `scripts/apply-override-dry.ts` läuft den Synopsis-Lint als Report-Only-Pre-Pass (exit 0 auch bei Hits — Inspector statt Build-Fail).
- **Loop-Driver `scripts/run-ssot-loop.sh`** (Brief 071, schlank-refactored in Brief 088, vom Resolver entkoppelt in Brief 094): Headless-Wrapper, `claude -p`-Subsession pro Iteration. Eine Iteration liest genau drei Dateien — `sessions/ssot-loop-runbook.md` (selbst-enthaltene Iter-Spec) + `npm run loop:next`-Output (Detection-Helper `scripts/loop-next-batch.ts`: nächster Batch/Slice als JSON) + `scripts/seed-data/facet-catalog.json` — ~6k statt vormals ~55k+ Tokens. **Brief 094 hat die Resolver-Pause-Cadence vollständig entfernt** — `loop-next-batch.ts` trägt keine Cadence-Helper mehr, `Decision` kein `resolverPause`/`nextResolverPauseAt`; der SSOT-Loop läuft cadence-frei bis zum vollen Roster (859) durch. Default `ITERATIONS` = 10. Der historische `⏸`-Block bei 450 im Loop-Log ist nur noch ein advisory Marker, keine Halt-Bedingung. Vier Loop-Disziplinen sind im Runbook bzw. Apply-Layer verankert: Public-Synopsis (076/080, zusätzlich Apply-Layer-hard-enforced), Faction-Granularity (077), Locations-Granularity (084), Goodreads-Rating (087 — `WebFetch` in `--allowedTools`, Page-Read der Goodreads-Buchseite).
- **Parallel-Worktree-Konvention** (Brief 082): drei produktive Worktrees — Coordination (`C:\Users\Phil\chrono-lexicanum`, `main` read-only), Product/UI (`chrono-lexicanum-product`, Branch `worktree/product-bootstrap`, später `codex/product-{slug}`-Task-Branches für HUD/Map/Music-Player/Login/Settings), Batches/Ingestion (`chrono-lexicanum-batches`, Branch `worktree/batches-bootstrap`, später `codex/ingest-batches-{slug}`-Task-Branches für SSOT-Batches/Resolver-Passes). Disziplin in `CLAUDE.md` + `AGENTS.md` § „Parallel worktrees" verankert; Altlast-Worktree `chrono-batches-011-015` (Brief-074-Zyklus, Branch `ingest/batches-011-015`) bewusst unangetastet.
- **Lab-Cartographer-Prototype** (Brief 079, PR #66): Standalone Claude-Design-Galaxy-Map-Export aus dem lokalen gitignored `.scratch/map/Warhammer 40k Map/` als isolierte statische Sandbox unter `/lab/cartographer` (Server-Component-Iframe auf `/lab/cartographer-prototype/index.html?standalone=1`, `robots: { index: false, follow: false }`). Produktive `/map`-Route unverändert, kein DB-Touch, kein TSX-Port. Minimaler Root-Layout-Patch `<body suppressHydrationWarning>` für Browser-Extension-Body-Attribute. Direkt-Maintainer-Anweisung — kein paired Architect-Brief.
- **Headless Resolver-Loop (Brief 094 + 100).** Brief 094 hat den Resolver vom SSOT-Loop entkoppelt und die Maschinerie headless gemacht (vier Bausteine: Entkopplung, Wellen-Detektor `scripts/resolver-loop-detect.ts` als pure-core, brief-freies Runbook `sessions/resolver-pass-runbook.md`, Loop-Driver `scripts/run-resolver-loop.sh` mit Resume + Halt-Disziplin + State-File). **Brief 100 hat den Detektor + Wrapper + Verify-Trias auf zwei Domänen geöffnet:** `CrystallizedBatch` trägt `domain: "w40k" | "hh"`, `DetectInput` zählt Roster pro Domain (`w40kRosterCount` + `hhRosterCount`), `WaveDescriptor` trägt das vollständige `label`, `ApplyRange.domain` erweitert, `parseResolverLoopLog` matcht beide Domain-Heading-Formen, `loadInputs` matcht `manual-overrides-ssot-(w40k|hh)-(\d+)\.json` und erfasst die Domain pro Batch. `detectNextWave` durchläuft sequenziell W40K vor HH (Domain-Reihenfolge hartcodiert — HH-Cross-Era-Aliases hängen vom stabilen W40K-Reference-Layer ab), drei externe Terminal-Zustände `open-wave | idle | all-complete`; der W40K→HH-Übergang ist interner Branch-Point, materialisiert sich extern als `open-wave` mit der ersten HH-Welle, keine eigene Status-Variante. Der frühere externe Terminal `"w40k-complete"` ist entfernt. Loop-Wrapper `run-resolver-loop.sh` ist domain-agnostisch — die `w40k-complete)`-Switch-Case durch `all-complete)` ersetzt, der hardgecodete `WAVE="ssot-w40k-${WAVE}"`-Zusammenbau entfernt; der Wrapper konsumiert `wave.label` 1:1 aus dem Detektor-Output. **HH-Bootstrap-Wave-Sizing.** Neue Konstanten `HH_BOOTSTRAP_WAVE_TARGET=20` / `HH_BOOTSTRAP_WAVE_HARD_CAP=25` greifen **nur** bei `hhProgressBatch === 0` — die erste HH-Welle ist mechanisch `ssot-hh-001..002` (20 Bücher, Pass 10, Batch-003 würde gegen die 25er-Cap overflowen). Folge-HH-Wellen nutzen die regulären `WAVE_TARGET=50` / `WAVE_HARD_CAP=60`. Total HH ~6 Wellen (1 Bootstrap + 5 regulär `003..008 / 009..014 / 015..020 / 021..025 / 026..030`). **Pass-Counter global fortlaufend** — HH startet bei Pass 10 nach W40K-Pässen 1–9, kein eigener `hh-pass-1`-Namespace. **Verify-Trias zwei-domänen** (Variant A: statische `{domain, n}`-Tupel in `apply-override-dry.ts` / `test-resolver-coverage.ts` / `test-resolver-data-integrity.ts`, mit `as const satisfies ReadonlyArray<…>`-Schema-Validierung); HH-Tupel sind **noch nicht** pre-added — der Phase-4a-Trigger jeder zukünftigen HH-Welle fordert CC auf, die `{domain: "hh", n: "NNN"}`-Tupel der Welle zu appenden. EXPECTED_RANGES in `apply-override-dry.ts` für HH-Headroom angehoben: `factions.max 2100→2500`, `locations.max 800→1100`, `characters.max 1400→2200`; Minima unverändert (W40K-Floor-Schutz). Live-Smoke (gegen den realen Repo-Stand: W40K resolved durch Pass 9, 30/30 HH crystallized) liefert die erwartete Bootstrap-Welle `ssot-hh-001..002` mit `wave.pass=10` und `applyRange.domain="hh"`. Der erste echte headless Mehr-Wellen-Lauf des `run-resolver-loop.sh`-Drivers steht weiterhin aus — er ist jetzt der HH-Trial-Lauf (operativ, Philipp triggert). Pass 8 + 9 liefen supervised über `run-resolver-pass.sh`; HH-Wellen werden voraussichtlich headless durchlaufen.
- **Konsolidierungs-Pass (Brief 098).** Ein eigener, runbook-getriebener Pass-Typ (Geschwister des Resolver-Passes), der die über die Resolver-Wellen gewachsene Reference-Schicht global auf Cross-Wave-Dubletten prüft und merged — er arbeitet auf dem Entitäten-Set, nicht auf Büchern, ist also wellengrößen-unabhängig. Skripte (alle neu, kein In-Place-Umbau der Resolver-Skripte): `scripts/consolidation-aggregate.ts` (deterministischer, byte-identisch re-runnabler Dubletten-Kandidaten-Aggregator), `scripts/consolidation-db-snapshot.ts` (versionierter Prod-DB-Pre-Mutation-Snapshot), `scripts/consolidation-db-sync.ts` (Dry-Run-Plan + transaktionaler Apply: Field-Retention → FK-Remap → In-Tx-Verify → Delete), dedizierte `scripts/consolidation-pass.config.json`. Spec: `sessions/consolidation-pass-runbook.md` (schlank, brief-frei, achsen-sliced Default, 120k-Token-Grenze, Maintainer-Review-Gate). Erstlauf 2026-05-25 (Brief 098) über das W40K-Set: 15 Kandidaten → 2 Merges. Künftige Läufe (finaler Full-Corpus-Pass nach HH, Ad-hoc-Zwischenläufe) laufen brief-frei via Runbook — Session-Start-Callout in `CLAUDE.md` + `AGENTS.md`.
- **Atlas-Regen-Skript.** `npm run atlas:regen` writes a Postgres-mirror Obsidian vault to `~/chrono-atlas/` (Windows `C:\Users\Phil\chrono-atlas\`; override via `--out=<path>` or `ATLAS_PATH` env). 049-impl produced first proof-of-render (1 book + 1 faction + INDEX.md, DB-counts 26/29 verified). Manual trigger only; see [`./workflows/atlas-regen.md`](./workflows/atlas-regen.md).
- **Brain-Lint.** `npm run brain:lint` (11 Check-Kategorien post-070) + CI-Gate `brain:lint -- --no-write` post-053. Reports unter `brain/outputs/lint/YYYY-MM-DD.md`. Neue Kategorie „Faction policy" (070): warn auf parent-null Faction-Rows ohne Browse-Root-Status, error auf dangling `parent`-FK. Post-077-Beobachtung: `faction-policy.md` bei 108 body lines vs. 100 soft-budget — tolerabel weil die neue Skip-Section notwendig ist.
- **Faction-Policy.** [`./decisions/faction-policy.md`](./decisions/faction-policy.md) trennt Browse-Root (UI-Filter-Ebene) von Tree-Root; Browse-Root-Whitelist + `knownTopLevelExceptions` + `specialCases` leben in `scripts/seed-data/faction-policy.json`. Schema unverändert; `factions.parent_id` weiter Single-Parent. `seed-resolver-extensions` faction-Insert ist seit 070 ein Upsert auf JSON-Spalten, damit der Pre-Apply Parent-Hygiene-Check (Runbook) Reparents in prod-DB schieben kann. Post-077: neues Top-Level-Feld `redundantWhenSubPresent: ["imperium", "chaos"]` schaltet den Grand-Alignment-Junction-Skip im Apply-Layer scharf; `factions.json`-Row `imperium` trägt jetzt explizit `"alignment": "imperium"` (single source of truth statt `inferAlignmentFromTree`-Inferenz, plus Helper im shared `src/lib/seed/alignment.ts`).

## Latest pipeline state (post-PR-108 / Brief 102 HH-Konsolidierungs-Pass — Korpus datenkomplett + konsolidiert, DB 859/859)

PR #108 (HH-Konsolidierungs-Pass / Brief 102 impl, branch `codex/ingest-batches-hh-consolidation-pass`, 7 Commits c01a795..037b618) ist gemergt. Konsolidierungs-Pass 2 — Full-Corpus W40K 1..57 + HH 1..30 — fährt die Brief-098-Maschinerie (Aggregator + DB-Snapshot + DB-Sync + Runbook) ein zweites Mal über das datenkomplette Reference-Set. **Brief-094-§-Cadence-Bogen geschlossen** — der verpflichtende finale Konsolidierungs-Pass nach der HH-Domäne ist gefahren; künftige Konsolidierungs-Pässe sind ad-hoc (Polish-/Refinement-/neuer-Domain-getrieben), nicht mehr im Cadence-Default. Detail im [Brief-102](../../sessions/2026-05-27-102-arch-hh-consolidation-pass.md) + [102-impl](../../sessions/2026-05-27-102-impl-hh-consolidation-pass.md) + sechs Pass-2-Artefakten unter `sessions/resolver-dossiers/consolidation-pass-2-*` (dossier, aggregator-output, dry-run-plan, db-snapshot, merge-map, reference-premerge-snapshot).

**Ergebnis-Tier.** 23 Kandidaten-Cluster → **2 Merges, 21 No-Merges, 0 Flagged.** Beide Merges auf der Characters-Achse:

- **Merge 1 (Lore-Deep-Tier, cross-era):** `merir_astelan → astelan`. HH-era Dark-Angels-Captain (Pass-13 *Call of the Lion*) IST der W40K-era Fallen Angel Astelan. Chronologisch-pre-ADR-Carryover: Pass-13-Implementierer hatte Brief 100 + ADR noch nicht greifbar; die `merir_astelan`-Notes dokumentierten die Cross-Era-Continuität selbst. Keeper-Wahl `astelan` per Cross-Era-Identities-ADR-Pattern (W40K-canonical short-form analog `horus`/`lorgar`/`typhus`/`corax`). Field-Retention keeper-wins (`primary_faction_id=fallen_angels` retained, ADR-konform); HH-era pre-fall Affiliation via cross-era-coherent notes-Rewrite captured.
- **Merge 2 (Mechanical-Tier, same-era):** `sharrowkyn → nykona_sharrowkyn`. Pass-11 (*Angel Exterminatus* HH-0023) und Pass-13 (*Kryptos* HH-0167) haben unabhängig denselben Raven-Guard-Mor-Deythan-Charakter mit verschiedenen Surface-Forms angelegt — klassen-identisch zu Pass-1 `magister_sek → anakwanar_sek`. Keeper-Wahl `nykona_sharrowkyn` (Lexicanum-canonical-Form); notes-Merge (Pass-13 + Pass-11 Anchors).

**Wichtigster Befund: positive Brief-100-ADR-Validation über sechs HH-Wellen.** Cross-era-anchor-breach Aggregator-Tripwire (Re-Annotation auf alias-coincidence-Edges) = **0 Treffer** über alle 18 pinned Cross-Era-Surface-Forms (Luna Wolves, Imperial Army, Mechanicum, Ezekyle Abaddon, Kharn, Magnus, Lucius, Ahriman, Horus Lupercal, Calas Typhon, Corvus Corax, Lorgar Aurelian, Little Horus Aximand, Nassir Amit, Alexis Pollux, Dantioch, Maloghurst, Arvida, Aenoid Thiel). Das eine Cross-Era-Merge `merir_astelan→astelan` ist Disziplin-Drift im engeren Sinn (Pass-13-Implementierer ohne Pre-ADR-Greifbarkeit), kein laufender Drift-Trend. ADR `decisions/cross-era-identities.md` um „HH-domain post-consolidation hardened: 2026-05-27"-Halbsatz ergänzt.

**Drei HH-aware Aggregator-Klassen scharfgeschaltet.** (a) **Slug-edit-distance** auf locations (Wagner-Fischer Levenshtein; Schwellen `distance≤2 AND ratio≤0.25 AND min-len≥4` — deliberat so gewählt, dass `vigilus↔vigil` NICHT triggert; `isstvan↔istvaan` / `prospero↔prosperan` / `calth↔caltha` aber schon): 1 trigger (`barbarus↔tartarus`), 0 true-positives, 1 false-positive (Tags-Disjunktheit `["death_guard"]` vs `[]` als Adjudikations-Marker). (b) **Cross-era-anchor-breach** als Re-Annotation auf alias-coincidence-Edge (3 Zeilen Code statt eigener Edge-Klasse — semantisch äquivalent): 0 trigger = positive ADR-Validation. (c) **Primarch-stem** auf characters (22 endliche Stems hart-kodiert im Aggregator: horus, sanguinius, rogal dorn, lion eljonson, leman russ, lorgar, fulgrim, perturabo, corax, alpharius, omegon, konrad curze, night haunter, magnus, ferrus manus, mortarion, vulkan, roboute guilliman, guilliman, angron, jaghatai khan, jaghatai): 1 trigger (`horus↔horus_aximand`), 0 true-positives, 1 false-positive (Primarch ↔ Captain-named-after-Primarch, klassen-analog zu Planet ↔ Ship-named-after-Planet auf der Locations-Achse). Aggregat-Befund: **alle drei HH-Heuristik-Klassen sind Tripwires** (negative-validate gegen Drift), nicht True-Positive-Mining-Tools — beide Pass-2-Merges wurden von Pass-1-Base-Heuristiken (Jaccard + substring + shared-primaryFactionId) gefunden. False-negative-Befund auf cross-era-anchor-breach ist die wertvollste der drei Signal-Klassen.

**Counts post-Pass-2-Re-Apply + DB-Sync (859 works):**

- Reference: characters `491 → 489` (-2 mergees: merir_astelan + sharrowkyn gelöscht), character-aliases `64 → 66` (+2 neue Einträge); factions `202` / locations `288` / faction-aliases `73` / location-aliases `25` / facet_values `86` alle unverändert.
- DB: work_factions `2752 → 2754` (+2 re-apply-Drift über die 87 Batches), work_locations `1144 → 1145` (+1 re-apply-Drift), **work_characters `1992 → 1997`** (+5 — re-pointed Junctions auf die zwei Keepers via neue Aliases: `astelan` 5→6 = +1 von merir_astelan, `nykona_sharrowkyn` 1→3 = +2 von sharrowkyn). work_collections `196`, work_persons `785`, work_facets `16845` alle unverändert. Orphan refs 0, residue 0, keeper-notes korrekt.

**EXPECTED_RANGES.characters.max 2200 → 2500** (Brief-102-Cap-Re-Tune, ~25% headroom über work_characters=1997). Pass-15-locations-Pattern (1100→1500 ~24%) übertragen — bewusst 25% statt 20% gewählt, weil `characters.max` historisch der engste der drei Caps war und Pass 2 laut Brief 094 § Cadence der finale verpflichtende Konsolidierungs-Pass ist (ad-hoc-Folgelauf ungewiss timed). factions cap `3200` (2754 ≈ 14% headroom) und locations cap `1500` (1145 ≈ 24% headroom) unangetastet.

**`verify-pass.ts`-Out-of-Range-Digest-Bolt-on gelandet.** Brief-102-Bündel-Kandidat aus Brief 101 § Open questions: am Ende des `main()`-Body hinter einem `if (verify)`-Guard (config-agnostisch — Per-Wave-Resolver-Configs mit verify + Konsolidierungs-Pass-Configs ohne verify). SQL `LIKE ANY (ARRAY['W40K-%','HH-%'])` aus den Domain-Prefixes; zählt `work_collections`-Rows wo Constituent's `external_book_id` außerhalb der ApplyRanges-Union liegt. Pass-2-Lauf: `out_of_range_count=0` ✓ (Full-Corpus-Tripwire grün am datenkomplettem Korpus — exakt das Brief-101-Erfolgs-Pattern, das deferred-edge-Materialisierung über die HH-Wellen 11–15 vorhergesagt hat). Per-wave HH-only-Config-Lauf als Sanity-Check: `147` (W40K-Constituents außerhalb der HH-only-Range, informational für per-wave-configs).

**Multi-Range-Loader-Normalisierung — Singleton bleibt schreibbar, Liste ist canonical-Lese-Form.** `AggregatorConfig` um optionales `applyRanges: ApplyRange[]` erweitert; `normalizeApplyRanges()` wirft Hard-Error bei beidem (Mehrdeutigkeit als Konfig-Bug behandelt), normalisiert Singleton → `[r]` und exponiert `applyBatchIds()` + `assertValidRange()`. Pass-1-Config + per-wave-resolver-Configs schreiben weiterhin `applyRange` (Singleton); der Loader normalisiert intern. Konsumenten lesen ausschließlich `applyRanges`. `run-phase4-apply.sh` inline `apply_batches()`-Helper liest beide Formen, hart-failt bei beidem, expandiert über die Range-Liste; Shell-Struktur und CLI unverändert.

**Tests alle grün (sequentially executed):** `npm run typecheck` ok; `npm run lint` ok mit 1 pre-existing Warning (`no-page-custom-font` in `src/app/layout.tsx`); `npm run brain:lint -- --no-write` 0 blocking + 24 pre-existing Warnings (unverändertes Profil); `npm run test:apply-override-dry` ok (`out-of-range=0, unknown-work=0`); `npm run test:resolver` **473 passed / 0 failed** (+1 reformulated Merir-Astelan-Test von `direct match → merir_astelan` auf `alias-resolved → astelan`); `npm run test:resolver-data` 10/10; `npm run test:resolver-coverage` 10 ok / 0 fail; `npm run test:collection-refs` 10/10 inkl. Brief-101-Reason-Split-Cases.

**Was Pass 2 NICHT angefasst hat.** `scripts/seed-resolver-extensions.ts` (fully generic JSON-loader, unverändert), `scripts/apply-override-collections.ts` (Brief-101-Reason-Split unangetastet), `scripts/seed-facets.ts`, `scripts/db-counts.ts`, `scripts/seed-data/collection-gaps.json` (leer/unverändert), `scripts/seed-data/facet-catalog.json` (vokabular-sauber). Pass-1-Config + Pass-1-Artefakte bleiben byte-identisch ladbar — Pass-2-Artefakte leben unter separaten Pfaden (`consolidation-pass-2-*`).

**Watch-Items aus dem 102-impl For-next-session (alle out-of-scope für diesen Pass, durable in § What's open):**

- (i) **Aggregator-Refinement-Pass-3** (optional, langfristig): die false-positive-Klassen aus Pass-2 könnten als auto-no-merge-Regeln im Aggregator codiert werden — konkret `tags:vessel↔planet`-Pattern auf locations (3 false-positives in Pass 2) + `primarch-stem↔captain-named-after-primarch` auf characters (1 false-positive). Bevorzugte Form: `auto-no-merge-rationale: planet-ship-pattern`-Annotation im Aggregator-Output statt strikter Filterung (Cluster bleiben sichtbar, sind als „expected false-positive" gelabelt).
- (ii) **Audit-Cockpit-Drift/Gap-Follow-up** über die kumulativ ~148 drift_works + ~98 gap_works über die HH-Domain. Brief 102 erwähnt das explizit als Out-of-Scope; eigener Data-Quality-Brief sinnvoll mit klarem Scope (Drift = raw_name-vs-canonical-name-Mismatch / Surface-Form-Drift; Gap = work hat 0 Refs in mindestens einer Junction-Achse). Pass-15-Forecast.
- (iii) **`consolidation-aggregate.ts` HH-Heuristik-Doku im Code-Header** (Master-Block am Datei-Anfang analog Brief-090-Bestand-Header). Klein, eigene Folge-Session.
- (iv) **Codex-Review-Sicht auf Pass 2** analog zum Pass-1-Codex-Read-Pass-Review (Cross-Era-Identity-Handhabung: keeper-wins-on-conflicting-primaryFactionId mit notes-Rewrite). Sinnvoll als externe ADR-Anwendungs-Validation.



**Wave-Größen exakt nach Brief-100-Forecast (60/60/60/50/44).** Pass 11 (`ssot-hh-003..008`, 60 Bücher HH-0021..HH-0080) — der erste Cross-Era-Honor-Title-Anker-Schub (`Bjorn` als Pre-Dreadnought-Spine anticipated). Pass 12 (`ssot-hh-009..014`, 60, HH-0081..HH-0140) — die mid-HH-Mittellage mit Pharos-/Mournival-Material. Pass 13 (`ssot-hh-015..020`, 60, HH-0141..HH-0200) — der post-Mid-Heresy-Knubbel inkl. Anthology-Konstituenten HH-0150..HH-0156 / HH-0157..HH-0165 (Pass-10-deferred-Edges materialisieren idempotent). Pass 14 (`ssot-hh-021..025`, 50, HH-0201..HH-0250) — Post-Heresy-Tail mit Blackshields-Forecast. Pass 15 (`ssot-hh-026..030`, 44, HH-0251..HH-0294) — HH-audio-drama + artbook/scriptbook-Tail (ssot-hh-026..029 first-cycle Heresy-audio-drama-Bloc + mid-Heresy Legion-Vignette + Pharos / Sicarus / Tallarn + Endryd-Haar Blackshields-Trilogy; ssot-hh-030 4-Buch-Tail Collected Visions / Scripts Volume I+II / Visions of Heresy 2018 ed.).

**Counts post-Pass-15-Re-Apply `001..057 + hh-001..030` (859 works):**

- works **565 → 859** (+294 volle HH-Domäne über die fünf Pässe; davon Pass 11 +60, Pass 12 +60, Pass 13 +60, Pass 14 +50, Pass 15 +44).
- work_factions `1981 → 2752` (+771); work_locations `776 → 1144` (+368); work_characters `1325 → 1992` (+667); work_collections `147 → 196` (+49 — die 20 Pass-10-Anthology-Constituent-Edges sind idempotent materialisiert + reguläre neue Collection-Kanten aus den HH-Folge-Wellen); work_persons `541 → 785` (+244 HH-Author-Slugs via `ensurePersonsExist`; `persons.json` JSON-side unverändert 100); work_facets `11672 → 16845` (+5173).
- Reference: factions `179 → 202` (+23), locations `234 → 288` (+54), characters `404 → 491` (+87); `facet_values 86 → 86` — keine Facet-Promotion über die fünf Wellen.

**EXPECTED_RANGES — eine quantitative Re-Tune über fünf Pässe.** Pass 15 hat `locations.max 1100 → 1500` angehoben (exakt der Pass-14-forecasted Bump, ~24% post-bump headroom). Factions cap `3200` (currently 2754 ≈ 14% headroom) und characters cap `2200` (currently 1992 ≈ 9% headroom — der tightest bound nach Pass 15) ohne Re-Tune. Der characters-cap-Re-Tune-Kandidat ist der HH-Konsolidierungs-Pass.

**Verify-Digest Pass 15 clean (live DB matcht Apply-Digest exakt).** 5 Smoke-Slugs (HH-0260 *Hunter's Moon* 2f/0l/0c, HH-0270 *Iron Corpses* 1f/1l/0c, HH-0280 *Children of Sicarus* 2f/2l/1c, HH-0290 *Blackshields: The Broken Chain* 2f/0l/2c, HH-0294 *Visions of Heresy 2018 ed.* 20f/4l/3c — Artbook-Overview-Coverage-Signatur). Rating-Coverage 44/44 Goodreads für HH-0251..HH-0294. NEW-Range Audit-Replica `total_works=44`, `drift_works=25` (~57% — die drei Pass-15-Character-Aliases + cumulative Cross-Era-Anker landen `raw_name ≠ canonical.name` per Definition für legitimate Identity-Coherence-Gründe), `gap_works=18` (~41% — Audio-Drama-Format dominiert single-axis pieces; Data-Quality-Cycle-Material, kein 4a-Fehler), `content_in_collection=0` (erste HH-Welle mit Null NEW-Range-in-coll-Edges — die vier ssot-hh-030 Artbook/Scriptbook-Entries tragen kein `relatedBookIds`, Reference-Volumes statt Novel-Omnibi). OLD-Range HH-0001..HH-0250 cumulative `content_in_collection=49` — das Brief-100/101-deferred-edge-Pattern trägt sauber über die fünf Wellen.

**Cross-Era-ADR über den vollen HH-Korpus operativ gehärtet.** Über die fünf Wellen 11–15 trugen Cross-Era-Aliases robust — `factionsSkippedRedundant`-Bucket für Cross-Era-Hits leer (Aliases resolven auf eine Canonical-Row, kein Skip), keine echte Identitäts-Disambig getriggert (kein `## Needs decision`-Stop), Pass 15 hat drei Character-Alias-Cases sauber gelandet (`Aenoid Thiel → aeonid_thiel` Typo, `Bjorn the One-Handed → bjorn` Pre-Dreadnought-Honor-Title als Pass-11-anticipated Cross-Pass-Consolidation, `Lord Cypher → cypher` Cross-Era Heresy-era-Dark-Angels-title-character → post-Heresy Fallen-Lord-of-the-Fallen). ADR `decisions/cross-era-identities.md` um „HH-domain hardened: 2026-05-27"-Halbsatz ergänzt (Status-Zeile + Pass-15-Impl-Report in `sources:`).

**Deferred-Edge-Pattern (Brief 100/101) trägt — die Pass-10-Anthology-Constituent-Edges sind materialisiert.** Die 20 Pass-10-Edges aus den drei HH-Anthologien (HH-0020 → HH-0117..HH-0120, HH-0010 → HH-0150..HH-0156, HH-0016 → HH-0157..HH-0165) waren beim Pass-10-Apply out-of-range; Pass 11/12/13 haben die HH-0117..HH-0120 / HH-0150..HH-0156 / HH-0157..HH-0165-Constituents in den kumulativen Re-Apply gebracht; `applyCollections` hat die Kanten idempotent materialisiert. `work_collections` ist von 147 (Pass 10) auf 196 (Pass 15) gewachsen — die +49 Edges sind die Mischung aus den 20 deferred Pass-10-Edges plus die neuen HH-Anthology-Collection-Edges aus den Pässen 11–15 selbst. Exakt das Brief-101-Erfolgs-Pattern (Sanctus Reach / Damocles / Shield of Baal analog für HH). **Kein `collection-gaps.json`-Eintrag nötig** — der idempotente kumulative Re-Apply löst die Forward-Refs alleine.

**Watch-Items aus den Pass-15-impl-For-next-session (in § What's open verlinkt).** (i) Audit-Cockpit-Drift/Gap-Follow-up für die kumulativ ~148 drift_works + ~98 gap_works über die HH-Domäne (Data-Quality-Cycle, nicht Resolver-Pass). HH-0260 *Hunter's Moon* (`l=0, c=0`) und HH-0270 *Iron Corpses* (`c=0`) sind die wahrscheinlichsten Backfill-Kandidaten (audio-drama-format-single-axis); HH-0260 ist von Pass-15-Dossier §6 als `low_confidence:characters` bereits geflaggt. (ii) Characters-cap Re-Tune `2200 → ?` (currently 1992 ≈ 9% headroom — der tightest bound) — natürlicher Place im HH-Konsolidierungs-Pass alongside any merge movement.

**Was die fünf Pässe NICHT angefasst haben.** `scripts/seed-resolver-extensions.ts` (fully generic JSON-loader), `scripts/apply-override-collections.ts` (Brief-101-Reason-Split unangetastet), `scripts/db-counts.ts`, `scripts/seed-facets.ts`, `scripts/run-phase4-apply.sh` (stable apply-side tooling), `scripts/seed-data/collection-gaps.json` (deferred-edge-Pattern absorbiert alle Forward-Refs), `scripts/seed-data/facet-catalog.json` (vokabular-saubere Wellen). Lint hat über alle fünf Pässe **eine** pre-existing Warning (`@next/next/no-page-custom-font` in `src/app/layout.tsx:44` — Strang-fremd, Product-side, out of scope).

## Latest pipeline state (post-101 / Pass 10, HH-Bootstrap-Apply applied — DB 585/859)

Resolver-Pass 10 (`ssot-hh-001..002`, PR #105 auf branch `codex/ingest-batches-resolver-trial-hh`) hat als erste HH-Welle die DB von 565 → 585 W40K+HH-Bücher gebracht; Brief 101 (PR #106) hat im Vorfeld den Brief-091 forward-ref Guard reason-split-fähig gemacht. Detail in [Pass-10-Impl-Report](../../sessions/resolver-dossiers/resolver-pass-10-impl-report.md) + [Brief 101 arch](../../sessions/2026-05-26-101-arch-hh-forward-ref-guard-reason-split.md) + [101-impl](../../sessions/2026-05-26-101-impl-hh-forward-ref-guard-reason-split.md).

**Brief 101 — Guard-Reason-Split.** Cowork-Maintainer-Entscheidung 2026-05-26: aus drei vom Pass-10-Phase-4a-Halt angebotenen Pfaden Option 1 (Reason-Split) gewählt — Optionen 2 (domain-aware seal) und 3 (allowlist) explizit verworfen. Implementation tight: `scripts/apply-override-dry.ts:942-953` (Reason-Breakdown-Konsolenausgabe `by reason: out-of-range=N1, unknown-work=N2`, auch wenn beide 0) + `:979-1000` (Assertion auf `unresolvable.filter(u => u.reason === "unknown-work")` eingeschränkt, Kommentar-Block aktualisiert) + `scripts/test-apply-override-collections.ts:19-24, 162-218` (Import `UnresolvableConstituentRef`-Typ, lokale `guardAbortRefs`-Helper-Predicate, drei Cases a/b/c: out-of-range>0/unknown-work=0 → kein Abort, out-of-range=0/unknown-work>0 → Abort, beide>0 → Abort auf `unknown-work`-Subset only). `apply-override-collections.ts` unangetastet — die Reason-Klassifikation war bereits korrekt; die Änderung lebt rein konsumenten-seitig. `test:collection-refs` 10/0 (+3 von 7); volle Trias grün.

**Pass 10 — HH-Bootstrap-Apply.** Sechs Phasen completed (eine Commit pro Phase). Phase 0-3 haben die JSON-Reference-Schicht sauber wachsen lassen (Cross-Era-Disziplin aus 100-Runbook §4 sauber gehalten — Luna Wolves → `sons_of_horus`, Mechanicum → `mechanicus`, Imperial Army → `astra_militarum`, Lucius → `lucius_the_eternal`, Ezekyle Abaddon → `abaddon_the_despoiler`). Phase 4a hat in einem ersten Anlauf halt-disziplin-konform auf `## Needs decision` gestoppt (20 HH-Anthology→Constituent-Edges aus den drei HH-Anthologien — HH-0020 → HH-0117..HH-0120, HH-0010 → HH-0150..HH-0156, HH-0016 → HH-0157..HH-0165 — brachen den über-strengen Brief-091-Guard auseinander; exakt der Pass-10-Dossier-§7d-Stop-Trigger); Brief 101 hat den Guard gefixt; das Re-Run-Commit (`42a6fad`) ist auf der ersten grünen Iteration durchgelaufen: `test:apply-override-dry` reports `out-of-range=20, unknown-work=0`, `run-phase4-apply.sh` schreibt beide Batches in die DB. **Counts PRE→POST (idempotenter Re-Apply `001..057 + hh-001..002`):** `works 565 → 585` (+20), `work_factions 1903 → 1981` (+78), `work_locations 733 → 776` (+43), `work_characters 1220 → 1325` (+105), `work_collections 147 → 147` (unverändert — out-of-range Constituent-Edges materialisieren auf Folge-Wellen via cumulative idempotent re-apply), `work_persons 524 → 541` (+17), `work_facets 11291 → 11672` (+381). Reference: `factions 173 → 179`, `locations 224 → 234`, `characters 344 → 404`; Aliases faction 59→63, location 16→17, character 43→47. `facet_values 86→86` — keine Facet-Promotion. `persons.json 96→96` — alle 11 HH-Bootstrap-Author-Slugs waren bereits aus dem W40K-Korpus präsent.

**Phase 4b Verify-Digest** (Read-only gegen Supabase — die live-DB matcht den Apply-Digest exakt): Smoke-Slugs beide präsent (`tales-of-heresy` HH-0010 mit 7f/2l/9c, `the-primarchs` HH-0020 mit 6f/1l/5c; `in_coll=0` auf beiden, weil die Anthology→Constituent-Edges out-of-range für diese Welle sind — landen auf Folge-Wellen). Rating-Coverage 20/20 für HH-0001..HH-0020, alle `rating_source=goodreads`. NEW-Range Audit HH-0001..HH-0020: `total_works=20`, `drift_works=15` (erwartet — Cross-Era-Alias-Resolving wirkt: `raw_name="Luna Wolves"` zeigt auf canonical `sons_of_horus`, `raw_name="Kharn"` auf `kharn_the_betrayer` etc.; das ist die ADR-Disziplin in Aktion), `gap_works=2` (Data-Findings, dünne Metadata-Achsen — kein 4a-Fehler; gehören in den Audit-Cockpit-Drift-Sweep), `content_in_collection=0` (konsistent mit `work_collections +0` aus dem Apply-Digest). OLD-Range HH-0001..HH-0000 invertierter Boundary (per Verify-Config "kein prior HH-Baseline"; das Empty-Result beweist, dass die Comparison-Baseline die leere Menge ist, nicht stale W40K).

**Out-of-range materialisiert auf Folge-Wellen.** Die 20 HH-Anthology→Constituent-Edges sind als deferred edges geparkt: `applyCollections` re-evaluiert die Kante idempotent, sobald die spätere Welle den Constituent in die kumulative Apply-Range bringt — keine Roster-Maintenance-Edits, kein `collection-gaps.json`-Eintrag nötig (das ist exakt das Erfolgs-Pattern der W40K-Anthology-Forward-Refs aus Pass 6: Sanctus Reach, Damocles, Shield of Baal). `work_collections` tickt entsprechend in den Folge-HH-Wellen hoch, sobald HH-0117..HH-0120 / HH-0150..HH-0156 / HH-0157..HH-0165 appliziert sind.

**Cross-Era-ADR validated.** Die ADR [`./decisions/cross-era-identities.md`](./decisions/cross-era-identities.md) (Brief 100) hat in der ersten HH-Welle operativ getragen — 15/20 `drift_works` sind Surface-Forms, die auf eine kanonische W40K-Row resolved haben (Faction-Renames, Character-Honor-Title-Splits, Primarchen-Pattern); `factionsSkippedRedundant`-Bucket bleibt für Cross-Era-Hits leer (Aliases resolven, kein Skip). Keine echte Identitäts-Disambig getriggert → kein `## Needs decision`-Stop in der Phase-Statusdatei.

**Was die Folge-HH-Wellen jetzt zeigen.** Phase-3-Token-Budget der Bootstrap-Welle hielt sich im Bootstrap-Korridor; die regulären 50/60-Wellen sind tagweise leichter pro Welle, größer cross-Welle. Beobachtungs-Watch: jede HH-Welle, die `out-of-range`-Konstituenten in den Re-Apply zieht, sollte `work_collections` proportional ticken lassen (idempotente Aktivierung der deferred edges).

## Latest pipeline state (post-100, Resolver-Loop zwei-domänen-fähig — HH-Bootstrap-ready)

Brief 100 (HH-Resolver-Domain-Öffnung) ist implementiert + gemergt. Die Maschinerie ist HH-ready, der erste scharfe HH-Trial-Lauf wartet operativ. Detail in [Brief 100](../../sessions/2026-05-26-100-arch-resolver-hh.md) + [100-impl](../../sessions/2026-05-26-100-impl-resolver-hh.md); zugehörige ADR [`./decisions/cross-era-identities.md`](./decisions/cross-era-identities.md) (Modellierungs-Disziplin: eine Canonical-Row pro Identität, Era-Surface-Forms als Aliases).

**Detektor + Loop-Wrapper + Auto-Config zwei-domänen.** `scripts/resolver-loop-detect.ts` trägt `Domain = "w40k" | "hh"` + `DOMAIN_ORDER = ["w40k", "hh"]`; `CrystallizedBatch.domain`, `WaveDescriptor.{domain, label}`, `DetectInput.{w40kRosterCount, hhRosterCount, w40kProgressBatch, hhProgressBatch}`, `ApplyRange.domain` erweitert. `parseResolverLoopLog` matcht `ssot-(w40k|hh)-AAA..BBB` und liefert per-Domain-Progress-Counter. `loadInputs` matcht `manual-overrides-ssot-(w40k|hh)-(\d+)\.json`. Drei externe Terminal-Zustände **`open-wave | idle | all-complete`** (kein `w40k-complete` mehr); der W40K→HH-Übergang ist interner Branch-Point, materialisiert extern als `open-wave` mit der ersten HH-Welle. `scripts/run-resolver-loop.sh` domain-agnostisch — `w40k-complete)`-Switch-Case durch `all-complete)` ersetzt, der `WAVE="ssot-w40k-${WAVE}"`-Zusammenbau raus, der Wrapper konsumiert `wave.label` 1:1 aus dem Detektor-Output. Pass-Counter läuft global fortlaufend (HH startet bei Pass 10).

**HH-Bootstrap-Wave-Sizing.** Konstanten `HH_BOOTSTRAP_WAVE_TARGET=20` / `HH_BOOTSTRAP_WAVE_HARD_CAP=25` greifen **nur** bei `hhProgressBatch === 0`. Mit Batch-Größe 10 ist der Cap das bindende Limit: Wave 1 = `ssot-hh-001..002` (20 Bücher), Batch 003 würde gegen die Cap overflowen. Spätere HH-Wellen nutzen die regulären `WAVE_TARGET=50` / `WAVE_HARD_CAP=60` — partitionieren grob als `003..008 (60) / 009..014 (60) / 015..020 (60) / 021..025 (50) / 026..030 (44)`. Gesamt HH-Domäne: **~6 Wellen** (1 Bootstrap + 5 regulär), nicht ~12. Pure `partitionWaves(restBooks, batchSize=10, hardCap=hhProgressBatch===0 ? 25 : 60)` — die ganze Domain-Logik ist eine Cap-Konstante, kein neuer Algorithmus.

**Verify-Trias zwei-domänen (Variant A — statische Tupel).** `scripts/apply-override-dry.ts`, `scripts/test-resolver-coverage.ts`, `scripts/test-resolver-data-integrity.ts` lesen ihre Batch-Range jetzt aus `{domain, n}`-Tupeln (`BATCHES` mit `as const satisfies ReadonlyArray<…>`) statt aus einer hardgecodeten `["001"..."057"]`-Liste; Pfad-Template `manual-overrides-ssot-${b.domain}-${b.n}.json`. HH-Tupel sind beim Implementierungs-Zeitpunkt **noch nicht** pre-added — der Phase-4a-Trigger jeder zukünftigen HH-Welle fordert CC auf, die `{domain: "hh", n: "NNN"}`-Tupel der Welle zu appenden (idempotent-applyRange-Voraussetzung). Materielle HH-Verify-Abdeckung greift damit erst nach dem ersten HH-Pass.

**EXPECTED_RANGES angehoben.** `apply-override-dry.ts`: `factions.max 2100→2500`, `locations.max 800→1100`, `characters.max 1400→2200`. Minima unverändert (W40K-Floor schützt gegen accidental zero-apply). Begründung: faction-Wachstum bleibt überschaubar (~30–50 HH-Inserts laut Brief-100-Forecast), location-Wachstum mittel (~60–120), character-Wachstum dominant (~500–800 — Foundational Ten + Mournival + Primarchen-Cast). Pro-Welle-Adjustierung nicht nötig, Ranges sind globale Sanity-Caps. Nachjustierung erst bei einem Konsolidierungs-Pass mit großer Merge-Bewegung oder bei einem `## Needs decision`-Stop nach Cap-Riss in einem HH-Pass.

**Cross-Era-Identitäten-Disziplin (Brief 100 § Baustein 3 + Runbook §4 + ADR).** Eine kanonische Identität = eine Canonical-Row, Era-spezifische Bezeichnungen wandern in `*-aliases.json`. Konkret: Faction-Renames (`Luna Wolves` → `sons_of_horus`), Character-Honor-Title-Splits (`Kharn` → `kharn_the_betrayer`, `Abaddon` → `abaddon_the_despoiler`, `Magnus` → `magnus_the_red`, `Lucius` → `lucius_the_eternal`), Primarchen mit Honor-Title als Anker und HH-Surface-Forms als Aliases. Pre-Heresy-Charaktere ohne W40K-Pendant (Garviel Loken, Nathaniel Garro, Tarik Torgaddon, …) werden frische Canonical-Rows. Ausnahme: echte Identitäts-Disambig → `## Needs decision`-Stop. Surface-Form-Treue bleibt — die Override-Datei trägt die Lore-korrekte Surface-Form, das Resolving zur Canonical-ID via Alias passiert in Phase 4a. Spec im `resolver-pass-runbook.md` §4 (Subsektion „Cross-Era-Identitäten") — selbst-enthalten, ohne Brief-Verweis im operativen Body.

**W40K-Hardcode-Sweep negativ.** Brief 100 § OQ Frage „weitere W40K-Hardcodes im Pass-Ökosystem" ist beantwortet — `scripts/aggregate-surface-forms.ts` (wave-parametrisiert, liest aus `resolver-pass.config.json`), `scripts/run-phase4-apply.sh` (nutzt `r.domain` dynamisch aus `applyRange`), `scripts/verify-pass.ts` (SQL-Lex-String-Comparisons, funktionieren für `HH-NNNN` genauso wie `W40K-NNNN`), `scripts/run-resolver-pass.sh` (per-Welle-Driver, Config-getrieben) tragen alle KEINE W40K-Konstanten. Kein Folge-Cleanup nötig.

**Tests.** `test:resolver-loop-detect` 36 Cases (von 22 prä-100), alle grün — 6× `partitionWaves` (inkl. HH-Bootstrap, HH-Regular), 11× `detectNextWave` (W40K open/idle, `w40k-complete-hh-pending`-Boundary, HH-30/30, w40k-complete-zero-HH-idle, HH-Regular-wave-2, HH-Final-Restwave), 9× `buildWaveConfig`, 9× `parseResolverLoopLog`, 2× `resolver-loop-log-update` (unverändert). `test:resolver` 287/287, `test:resolver-data` ok, `test:apply-override-dry` `[apply-override-dry] ok`, `test:loop-next` 9/9, `brain:lint -- --no-write` 0 blocking + 16 pre-existing warnings.

**Was erst der Trial-Lauf zeigt.** Phase 3 in der ersten HH-Welle ist der Bootstrap-Knubbel (150–200 neue Characters: Foundational Ten + Mournival + erste Primarchen) — wenn die 20er-Welle Phase 3 sauber unter ~150k Token hält, ist die Architektur richtig dimensioniert; wenn nicht, ist das das Signal, das in Brief 094 deferrede Phase-3-Achsen-Slicing als Folge-Brief nachzuziehen. Cross-Era-Alias-Resolving (`Luna Wolves` → `sons_of_horus`, `Kharn` → `kharn_the_betrayer`, etc.) muss in Phase 1/3 sauber laufen; `factionsSkippedRedundant`-Bucket bleibt für Cross-Era-Hits **leer** (Aliases resolven, kein Skip). Wenn eine Achse die angehobenen Caps reißt, ist das ein `## Needs decision`-Stop und Datenpunkt für eine zweite Anhebung.

## Latest pipeline state (post-095, Resolver-Pass 7 applied — 450 W40K-Bücher + Resolver-Loop entkoppelt + Rollup-Ownership)

Drei zusammenhängende Stränge seit dem post-092-Stand: Resolver-Pass 7 hat die DB auf 450 gebracht, Brief 094 hat die Resolver-Maschinerie headless gemacht, Brief 095 die Rollup-Ownership geklärt. Alle drei sind implementiert + gemerged (Maintainer-bestätigt).

**Resolver-Pass 7 — `ssot-w40k-036..045`** ([Brief 093](../../sessions/archive/2026-05/2026-05-22-093-arch-resolver-pass-7.md) rationale-only, [impl-report](../../sessions/resolver-dossiers/resolver-pass-7-impl-report.md), PR #90). Die siebte Welle (W40K-0351..0450, 100 Bücher) in den Authority-Layer und nach Postgres — **450/450 W40K-Bücher applied**. Erster echter Lauf auf der von Brief 091 gesplitteten 6-Phasen-Maschinerie (0/1/2/3/4a/4b, `/clear`-getrennt, 4b nur aus 4a-Statusdatei + Apply-Digest); brief-frei + runbook-getrieben, supervised/axis-sliced gefahren (nicht über den Driver). Counts PRE→POST (idempotenter Re-Apply `001..045`): `work_factions 1424→1659` (+235), `work_locations 543→638` (+95), `work_characters 844→1074` (+230), `work_collections 109→142` (+33), `work_persons 325→424` (+99), `work_facets 7227→9087` (+1860). Reference-Rows `factions 162→166`, `locations 189→201`, `characters 237→297`; Aliases faction 41→48, location 15→15, character 34→39. `facet_values 86→86` — keine Facet-Promotion (Welle vokabular-sauber). Dry-Run-Prediction == DB-POST-APPLY exakt; Trias + lint + typecheck grün, kein `## Needs decision`. Ein wave-spezifischer Call: `EXPECTED_RANGES` in `apply-override-dry.ts` angehoben (factions 1500→1900, locations 600→800, characters 1200→1400) — Headroom-Sizing, kein Guard-Relax. Der range-aware forward-ref-Guard (Brief 091) blieb über den ganzen kumulativen Sweep grün (15 in-range forward refs); die fünf Collection-Kanten aus OQ (14) / PR #85 (Architect of Fate W40K-0286 + War for Armageddon Omnibus W40K-0307) landeten beim Re-Apply idempotent. `collection-gaps.json` wuchs um einen Eintrag (W40K-0434 *Space Wolves (Legends)*, drei confident Constituents); vier weitere Spot-Checks bewusst deferred (Maintainer-/Roster-Arbeit, kein Resolver-Job).

**Brief 094 — Resolver-Loop entkoppelt + headless** ([Brief 094](../../sessions/archive/2026-05/2026-05-23-094-arch-resolver-loop.md), [094-impl](../../sessions/archive/2026-05/2026-05-23-094-impl-resolver-loop.md)). Der Resolver war bis Pass 7 an die SSOT-Loop-Cadence gekoppelt (Pause alle 100 Bücher, dann supervised Resolver-Pass). Brief 094 entkoppelt beide und automatisiert den Resolver headless — vier Bausteine: (1) Entkopplung — Resolver-Pause-Cadence aus `run-ssot-loop.sh` + `loop-next-batch.ts` raus, der SSOT-Loop läuft cadence-frei bis 859 durch. (2) Wellen-Detektor `scripts/resolver-loop-detect.ts` (pure-core) — greedy ≤60-Buch-Partition offener kristallisierter Batches + mechanische `resolver-pass.config.json`-Auto-Generierung pro Welle. (3) Brief-freies Runbook — `brief`-Feld aus der Config raus, `sessions/resolver-loop-log.md` als append-only Fortschritts-Marker (Bootstrap: Pässe 1..7 / `001..045`). (4) Headless Loop-Driver `scripts/run-resolver-loop.sh` mit Resume + Halt-Disziplin + State-File. Tests: `test:resolver-loop-detect` 22/22, Trias grün, Live-Smoke `idle` (erwartet — `046+` noch nicht kristallisiert). Erster scharfer headless Lauf = die 115 W40K-Reste in zwei Wellen (`046..051` + `052..057`); operativ von Philipp getriggert. Der Detektor ist W40K-only — am W40K-complete-Meilenstein wechselt er auf `w40k-complete` und der Loop verlässt; HH braucht später eine Detektor-Erweiterung. Ein **Konsolidierungs-Pass** (eigener Pass-Typ, erster Lauf am W40K-complete-Meilenstein, doubles as Maschinerie-Test vor HH) ist als eigener Folge-Brief abgegrenzt — noch nicht geschrieben.

**Brief 095 — Rollup-Ownership** ([Brief 095](../../sessions/archive/2026-05/2026-05-23-095-arch-rollup-ownership.md), [095-impl](../../sessions/archive/2026-05/2026-05-23-095-impl-rollup-ownership.md)). Reine Workflow-/Doku-Änderung, die die zwei Implementer-Stränge (Product/UI, Batches/Ingestion) konfliktfrei macht: die Rollup-/Index-Dateien `sessions/README.md` + `brain/**` werden ab jetzt **ausschließlich im Koordinations-Worktree** geschrieben, nie in den Strang-Worktrees. Strang-Reports tragen substantielle System-Fakten in „What I did" / „For next session"; Cowork zieht sie im Post-Merge-Koordinations-Pass nach (genau dieser Pass ist ein solcher). Zusätzlich gehärtet: eine Worktree-Selbstprüfung am Session-Start (Ansage von Worktree/Strang/Branch + Halt-mit-Rückfrage bei Strang-Mismatch). Verankert in `CLAUDE.md`, `AGENTS.md`, `brain/wiki/workflows/cc-session.md` + `cowork-session.md`, mit Konsistenz-Sweep in `sessions-format.md` + `brain/CLAUDE.md`. Kein Code, kein Schema, kein DB-Touch.

## Latest pipeline state (post-091, Resolver-Pass 6 applied — 350 W40K-Bücher + Phase-4-Split-Brief)

Resolver-Pass 6 (`ssot-w40k-026..035` / W40K-0251..0350) ist gemerged (PR #83, `72c8788`) — die sechste Resolver-Welle, erstmals 100 Bücher, und der erste Lauf unter dem Brief-090-lean-Kontrakt (brief-frei + runbook-getrieben, fünf Phasen-Subsessions, **supervised/axis-sliced gefahren**, nicht über den Headless-Driver). 350/350 W40K-Bücher in Postgres. Die Dry-Run-Vorhersage traf die DB-POST-APPLY-Counts exakt (`work_factions 1424` / `work_locations 543` / `work_characters 844`), alle Trias-Checks (`test:resolver` 206/0) + `lint` + `typecheck` grün, kein `## Needs decision` in irgendeiner Phase. Inhaltlich ein sauberer Lauf — Detail in [`sessions/resolver-dossiers/resolver-pass-6-impl-report.md`](../../sessions/resolver-dossiers/resolver-pass-6-impl-report.md).

Counts PRE→POST (idempotenter Re-Apply `001..035`): `work_factions 1153→1424`, `work_locations 455→543`, `work_characters 701→844`, `work_collections 79→109`, `work_persons 232→325`, `work_facets 5404→7227`. Reference-Rows: `factions 154→162` (+8), `locations 169→189` (+20), `characters 199→237` (+38); Aliases faction 37→41, location 13→15, character 28→34. `facet_values 86→86` — keine Facet-Promotion (die Welle ist vokabular-sauber, `seed-facets` inserted 0). DB-Rating-Coverage 346/350 (die 100 neuen Bücher alle Goodreads-gerated). `persons.json` 63→73 (10 Wave-6-Autoren auto-created).

Pass 6 hat **eine** Architektur-Entscheidung produziert, von Phase 4 korrekt geflaggt: die Welle ist die erste mit *forward-referencing collection edges* — drei Anthologien (Sanctus Reach W40K-0296, Damocles W40K-0294, Shield of Baal W40K-0304) liegen in früheren Batches als ihre Novellen. Der harte `forwardRefs === []`-Assert im `apply-override-dry` fiel mit 10 legitimen Refs; Phase 4 hat ihn auf report-only entschärft und end-to-end verifiziert (10/10 Edges landen in `work_collections`; `applyCollections` ist für cross-batch-Refs gebaut). Zwei deferred collection-gaps (Architect of Fate W40K-0286, War for Armageddon Omnibus W40K-0307 — Constituent-Works existieren, nur die Roster-`collections[]`-Kanten fehlen) sind als Excel-SSOT-Arbeit zurückgestellt → OQ (14).

**Brief 091 — Resolver-Pass Phase-4-Split** ([`sessions/archive/2026-05/2026-05-22-091-arch-resolver-phase4-split.md`](../../sessions/archive/2026-05/2026-05-22-091-arch-resolver-phase4-split.md), `status: implemented`, gemerged). Phase 4 des Resolver-Passes ging in Pass 6 mit dem Kontextfenster Richtung ~300k Token (gegen ein ~120k-Per-Phase-Budget) — Brief 090 hatte den *statischen* Lese-Scope schlank gemacht, nicht die *agentische* Arbeitslast: Phase 4 erledigt fünf Jobs plus etwaiges ungeplantes Debugging in einem `/clear`. Brief 091 splittet Phase 4 in **4a Integration/Apply** + **4b Verify/Report** (`/clear`-getrennt, Handoff über eine 4a-Statusdatei) und härtet den forward-ref-Guard von report-only auf **range-aware** (Fehler nur, wenn die Konstituente außerhalb der applyRange liegt). Reine Resolver-Pass-Maschinerie — kein DB-Touch, vorwärtsgerichtet für Pass 7+. **Implementiert + gemerged** ([`091-impl`](../../sessions/archive/2026-05/2026-05-22-091-impl-resolver-phase4-split.md)): `resolver-pass-runbook.md` + `resolver-pass.config.json` tragen die 6-Phasen-Struktur (0/1/2/3/4a/4b), der forward-ref-Guard ist range-aware (neues Modul `apply-override-collections.ts` + `npm run test:collection-refs`). Erster produktiver Lauf darauf ist Resolver-Pass 7 (Brief 093).

## Latest pipeline state (post-090, Resolver-Pass lean + erster 100er-Loop-Lauf — 350 Override-Files, DB bei 250)

Zwei zusammenhängende Sessions seit dem post-089-Stand: Brief 090 hat die Resolver-Pass-Maschinerie schlank gemacht, der erste 100er-Loop-Lauf hat W40K-0251..0350 kristallisiert.

**Brief 090 — Resolver-Pass lean** (PR #80). Ein Mess-Gate bestätigte die beiden dominanten Token-Treiber pro Resolver-Phase (universelle Vorab-Dokumentenlast Brief 076 + per-pass Brief ≈ 22k tok/Phase + Phase-4-Korpus-Skalierung durch den rohen Apply-Log), danach wurde der Pass umgebaut: (1) neues schlankes [`sessions/resolver-pass-runbook.md`](../../sessions/resolver-pass-runbook.md) — ausführbare Spec für genau eine Phase, gespiegelt an `ssot-loop-runbook.md` (pro Phase „Lies NUR …"-Regel + benanntes Achs-Paket; Brief 076 + der per-pass Brief werden nicht mehr gelesen, um eine Phase zu fahren). (2) **Brief-freier Driver**: `runbook` ist das required Config-Feld, `brief` optional/rationale-only; `run-resolver-pass.sh` injiziert den Runbook-Pointer statt eines Briefs. `CLAUDE.md` + `AGENTS.md` tragen die Resolver-Pass-Phase-Ausnahme von der Session-Start-Leseroutine. (3) **Phase 4 korpus-entkoppelt**: `run-phase4-apply.sh` schreibt einen fix-großen Digest (`ingest/.last-run/phase4-digest.md` ≈ 1k tok), die rohe Per-Batch-Ausgabe geht in ein gitignored `*-verbose.log`; `verify-pass.ts` emittiert den Verify-Digest. Live gegen die 250-Bücher-DB validiert — der idempotente Re-Apply liefert byte-stabile Counts (works 250, work_factions 1153, work_locations 455, work_characters 701, work_facets 5404). (4) **Konsolidierung**: stabile, wave-parametrisierte Tools (`aggregate-surface-forms.ts`, `db-counts.ts`, `seed-facets.ts`, `run-phase4-apply.sh`, `verify-pass.ts` + geteilter `resolver-pass-config.ts`-Loader), die `-089`-Klone gelöscht — ein künftiger Pass erzeugt keine neuen `-NNN`-Klone. (5) **Cadence 50→100**: `loop-next-batch.ts` pausiert bei kumulativ ≡ 50 mod 100 (250/350/450/550, nicht mehr bei 300), neues `Decision`-Feld `nextResolverPauseAt`; `run-ssot-loop.sh`-Default `ITERATIONS` 5→10. Volle grüne Suite (`test:resolver` 173, `test:loop-next` 17). Brief 076 trägt einen Rationale-only-Banner; die operative Spec lebt im Runbook.

**Erster 100er-Loop-Lauf `ssot-w40k-026..035`** (PR #81). Der Standing-Loop (Brief 061) lief erstmals als eine volle 100er-Welle: 100 Bücher W40K-0251..0350 in zehn Iterationen, zehn Override-JSONs + Loop-Log-Appends, diff-only, kein DB-Touch. Alle vier Loop-Disziplinen aktiv. Kumulativ **350 Override-Files**; die DB bleibt bei 250 applied, bis Resolver-Pass 6 läuft. Der selbst-erkennende `⏸`-Resolver-Pause-Block steht bei 350 im Loop-Log. Die Welle ist inhaltlich Space-Marine-Battles-/Chapter-lastig (Salamanders Tome of Fire, Word Bearers, Rogue Trader, Bastion Wars, Sisters of Battle, Night Lords je als Trilogie + Omnibus) und content-clean — kein Architektur-Call für Resolver-Pass 6. Content-Flags fürs Roster (OQ (14)): Format-`data_conflict` W40K-0297 *Flesh Tearers* + W40K-0334 *Lords of Caliban* (Roster sagt novel/novella, real Single-Author-Collection), Titel-Mistags W40K-0259 *The Rose in Anger* + W40K-0330 *The Hunt for Magnus* (Roster-Titel weicht ab, Slug roster-treu belassen). `plot_type=duel` ist ein Vokabular-Watch-Item aus W40K-0340 (freq < 2, vorerst auf `war_story`+`journey` gemappt).

**Resolver-Pass 6 startklar.** `scripts/resolver-pass.config.json` ist in der Cowork-Session 2026-05-22 von Pass 5 auf Pass 6 gesetzt: `pass=6`, `wave=ssot-w40k-026..035`, `brief`-Feld entfernt (brief-frei), Dossier-/Phase-Report-Pfade pass-6-gekeyt, `aggregator.batches` 026..035, `applyRange` 1..35, Verify-Ranges (`newRange` W40K-0251..0350, `oldRange` ..0250) + zehn neue `smokeSlugs`, Phase-4-Trigger ohne Facet-Promotion (die Welle ist vokabular-sauber). Gefahren wird Pass 6 als fünf Phasen-Subsessions nach Runbook + Config oder optional headless über `run-resolver-pass.sh`.

## Latest pipeline state (post-089, Resolver-Pass 5 applied — 250 W40K-Bücher)

Drei zusammenhängende Sessions seit dem post-086-Stand haben die Pipeline auf 250 applied W40K-Bücher gebracht und die Goodreads-Rating-Beschaffung fest in den Standing-Loop verankert.

**Brief 087 — Goodreads-Rating-Pipeline-Integration** (PR #74/#75). Goodreads-Rating ist die vierte forward-only Loop-Disziplin: `overrides.rating` als nested object (`status:"rated"` mit `value`/`count`/`evidenceUrl`, oder `status:"unrated"` mit `reason`), neuer Pure-Helper `scripts/apply-override-rating.ts`, `apply-override.ts` schreibt `book_details.rating`/`rating_count`/`rating_source` nur bei vorhandenem Feld, `apply-override-dry.ts` simuliert + trägt `--file=<path>` für Single-Fixture-Dry-Runs. `WebFetch` ist in `run-ssot-loop.sh` `--allowedTools`. Hard rule: Rating von der Goodreads-Buchseite parsen (Page-Read), nie aus dem Snippet (086-Phase-4-Verdikt). `unrated` schreibt `rating=NULL` + `rating_source='goodreads'`, keine Migration. Forward-only ab `ssot-w40k-021`; `001..020` bleiben rating-no-op.

**Brief 088 — SSOT-Loop lean** (PR #76, `71fb4f1`). Eine Loop-Iteration liest jetzt genau drei Dateien (`sessions/ssot-loop-runbook.md` + `npm run loop:next`-Output + `facet-catalog.json`) — ~6k statt ~55k+ Tokens. Neuer Read-only-Detection-Helper `scripts/loop-next-batch.ts` (`npm run loop:next`, JSON mit nächstem Batch/Slice/Resolver-Pause-Status) + 13-Case-Test, neue `sessions/ssot-loop-runbook.md` (143 Zeilen, selbst-enthaltene Iter-Spec). Die Resolver-Pause ist selbst-erkennend — `--skip-initial-resolver-pause` ist vollständig entfernt; der committete `⏸`-Block ist sein eigener advisory Marker, ein Re-Run resumed automatisch. `CLAUDE.md` + `AGENTS.md` tragen den Carve-out: Loop-Iterationen überspringen die Session-Start-Leseroutine. Brief 061 ist auf Design-Rationale reduziert (operative Spec lebt im Runbook).

**Loop-Lauf `ssot-w40k-021..025`** (PR #77, `c2f53e7`). 50 Bücher W40K-0201..0250 in fünf Iterationen, fünf Override-JSONs + Loop-Log-Appends, diff-only, kein DB-Touch. Alle vier Loop-Disziplinen aktiv. Kumulativ 250 Bücher; selbst-erkennender 250er-Resolver-Pause-Block geschrieben. Content-Flags fürs Resolver-Brief: W40K-0244-`seriesHint`-Mistag (`data_conflict`), `value_outside_vocabulary` (`commissar`/`tank_crew`/`penal_legion`), diverse `low_confidence`-Flags.

**Brief 089 — Resolver-Pass 5** (PR #78, `d46bf6a`). Fünfte 50er-Welle axis-sliced kristallisiert + alle 250 W40K-Bücher in Postgres applied, 250er-Resolver-Pause geräumt. Manuell/supervised gefahren (nicht über `run-resolver-pass.sh` — Maintainer-Wahl stop-before-push; die Driver-Config ist befüllt + committed und bereit für einen späteren Pass). Reference-Rows: factions 146→154, locations 157→169, characters 169→199; Aliases faction 36→37, character 26→28 (Location-Aliases unverändert 13). `facet_values` 85→86 — neuer `commissar`-`protagonist_class`-Wert. DB-Junctions post-Re-Apply `001..025`: `work_factions=1153`, `work_locations=455`, `work_characters=701`, `work_collections=79`, `work_persons=232`. Apply: `001..020` = 0 inserts / 10 updates (Drift-Cleanup), `021..025` = 10 inserts / 0 updates. Drei Wave-Calls gelandet: **Call 1** (Regimenter — der deterministische Aggregat-Befund schlägt die Brief-Schätzung: nur Mordian Iron Guard erreicht freq≥2; +8 Faction-Rows inkl. `blood_ravens` freq6 + `deathwatch`). **Call 2** (`commissar`-Facet hinzugefügt; Retag W40K-0237 `inquisitor→commissar` und W40K-0247 `guardsman→guardsman+commissar`; neuer `scripts/seed-facets-089.ts` weil `validateFacetIds` gegen die DB-`facet_values`-Tabelle prüft, nicht den Catalog — `tank_crew`/`penal_legion` bewusst nicht promoted). **Call 3** (Author-Backfill W40K-0206 → Steven B. Fischer, W40K-0244 → Mike Vincent — erledigt + verifiziert). Offen geblieben (Maintainer-Hand-off): der W40K-0244-`seriesHint`-Mistag (`Imperial Guard` → real Night Lords/Chaos) — die Faction-Achse resolved bereits korrekt zu `night_lords`, der `seriesHint`-Fix ist eine Excel-SSOT-Maintainer-Edit (siehe `open-questions.md` OQ (14)).

## Latest pipeline state (post-086, Hardcover→Goodreads rating pivot — DB-Rating-Coverage 197/200)

Brief 086 (PR #73, `af7d90c`, 2026-05-20) lief in vier Phasen und hat die Rating-Linie abgeschlossen.

**Phasen 1–3 — Hardcover-Hit-Rate Pass 2.** Vier kombinierte Hebel (6-Stage-Match-Cascade, Author-Normalisierung, `graphql_error`-Retry-Pass, CC-Direct-WebSearch-Overrides) hoben die Hardcover-Hit-Rate von 79/200 (Brief 085) auf **116/200 = 58.0 %** — unter dem 70-%-Ziel, klassifiziert „< 60 % hard signal". 086-impl-Befund: ≈ 42 der 62 recherchierten Misses sind present-but-unrated oder gar-nicht-auf-Hardcover — eine strukturelle Katalog-Lücke; kein Matching-Layer konjuriert fehlende Daten herbei. Brief 085 (PR #72) wurde ohne Merge geschlossen, durch 086 supersedet.

**Phase 4 — Goodreads-Pivot.** Pro nach Phase 3 noch rating-losem Buch eine Einzel-Websuche für das Goodreads-Rating. Auf den 81 härtesten Residual-Büchern löste das **78 (96.3 %)** und hob die DB-Rating-Coverage auf **197/200 (98.5 %)** — 119 `ratingSource='hardcover'` + 78 `goodreads`. Die 3 offenen (`W40K-0145 Demolisher`, `W40K-0147 The Green Tide`, `W40K-0160 Krakenblood`) sind alle 2025/26-Releases ohne aggregierte Wertung.

**Verdikt — der Design-Constraint für alles Weitere.** Der 086-Closing-Report: die Websuche trägt, **aber nur als Lokator, nicht als Quelle.** Google-Rich-Snippets lieferten ~20 % keine und ~4 % eine plausibel-falsche Zahl. Jeder produktive Goodreads-Konsument MUSS das Rating von der Goodreads-Buchseite parsen (Page-Read), nicht aus dem Snippet.

**ADR + Folge-Brief.** Der Pivot ist als ADR [`./decisions/hardcover-to-goodreads-pivot.md`](./decisions/hardcover-to-goodreads-pivot.md) kodifiziert (amendet `no-goodreads.md` für das Rating-Feld; OL-Fallback + Slug/ID-Stage-6-Folgebrief final gestrichen). **Brief 087** (`sessions/archive/2026-05/2026-05-20-087-arch-goodreads-rating-pipeline.md`; inzwischen implementiert + gemerged, PR #74/#75 — siehe „Latest pipeline state (post-089)" oben) baut die Goodreads-Rating-Beschaffung als vierte Loop-Disziplin fest in Brief 061 ein — ab `ssot-w40k-021` holt jeder Batch sein Rating selbst (WebSearch zum Auffinden, Page-Read der Goodreads-Seite, neues optionales `overrides.rating`-Feld, Apply-Layer schreibt `book_details.rating`/`ratingCount`/`ratingSource`; Bücher ohne Wertung bekommen einen „geprüft, noch keine Wertung"-Marker). OQ (10) + OQ (12) sind damit geschlossen.

## Latest pipeline state (post-080+081, Public-Synopsis-Forward-Guard + Backfill `005..019` complete)

Brief 080 (PR #67, `9b9a839`, 2026-05-17) hat den **Apply-Layer-Forward-Guard** für Public-Synopsen scharfgeschaltet, Brief 081 (PR #69, `8d265be`, 2026-05-19) hat die ~3 359 Pre-080-Pattern-Treffer aus den 15 Batches `005..019` (123 Bücher) durch sequentiellen `/clear`-Loop-Rewrite auf Public-Reader-Prose gebracht. Endstand: alle 200 Public-Synopsen `001..020` sauber gegen die 32 Banned-Patterns (`test:apply-override-dry` zeigt „by label: none").

**Brief 080 — Track B (Guard).** Drei neue Artefakte plus Wiring: (a) `scripts/seed-data/synopsis-banned-patterns.json` — 32 Pattern-Einträge gegen Markdown-Emphasis (`**bold**`, `*italic*`, `_under_`), SSOT-IDs (`W40K-NNNN`/`HH-NNNN`), Batch-/Brief-Refs (`ssot-w40k-NNN`, `[Bb]rief[- ]NNN`), Authority-Layer-Tails, Resolver-/Curation-Vokabular (surface form, canonical entity, direct match, alias lookup, Resolver-Pass), Audit-Markers (data_conflict, low_confidence, historical_canon_layer, value_outside_vocabulary, Inquisition-consistency triggered, omnibus-constraint, aggregation per Brief), Footnote-Markers (`See note:`, sentence-start `cf.`, bracket refs); (b) `scripts/apply-override-synopsis-lint.ts` — Pure Helper im Brief-077-Skip-Helper-Shape (DI-Signatur, kein DB-/FS-Zugriff zur Call-Time, exportiert `lintSynopsis()` + `loadBannedPatterns()` + `formatLintError()`, Snippet-Trimming, Case-Insensitive, Zero-Width-Regex-Safety); (c) `scripts/apply-override.ts` `validateSynopses()` als Batch-Level-Pre-Pass in `main()` parallel zu `validateFacetIds`/`validateEntityRoles`, aggregiert Treffer über alle Bücher im Batch, wirft einmal mit `externalBookId + slug + batch + per-hit (label, position, 40-char snippet)` für jeden Offender (Halt-before-mutation). `scripts/apply-override-dry.ts` läuft denselben Lint als Report-Only-Pre-Pass (exit 0 auch bei Hits — Brief-080-P1-Semantik: Inspector liefert Per-Batch-Telemetrie als Brief-081-Baseline statt Build-Fail). Tests: `scripts/test-synopsis-lint.ts` mit 14 Cases (Pure-Helper-Smoke, muss durch 081-Iterationen unverändert 14/14 grün bleiben).

**Brief 080 — Track A (Pilot).** Rewrite der 10 Synopsen in `manual-overrides-ssot-w40k-020.json` (höchster Pollution-Score aus der Brief-080-impl-Baseline) auf Public-Reader-Copy: 412–556 chars (mean 475), kein Markdown, keine SSOT-IDs/Brief-Refs/„authority layer"-Vokabular, kein WebSearch. Voice = Design-Freedom, Premise-erst vs. Setup-erst-Mix 6/10 : 4/10, fachfremde Begriffe einmalig erklärt, Tonality-Marker als Prose statt Audit-Tag. Re-Apply Batch 020 grün (10 Updates, 0 Inserts, alle 10 Synopsen validiert), 3 Slugs DB-direkt verifiziert (`legacy` / `phalanx` / `crossfire` als Voice-Review-Anker). Plus Voice-Notes für Folge-Loop im `sessions/ssot-loop-log.md`-Pilot-Block hinterlegt.

**Brief 081 — Standing-Loop-Backfill.** 15 Iterationen `005 → 019` (klein-zu-groß-Reihenfolge per Brief-081-Detection-Algorithmus, Welle-2 `005..009` als Voice-Pacing-Kalibrierung, Welle-3 `010..015` + Welle-4 `016..019` als Hauptmasse), je ein Commit pro Iteration auf `codex/ingest-batches-synopsis-005-019` (Pattern `SSOT-Synopsis-Rewrite ssot-w40k-{NNN} (Brief 081)`). Touch-Set pro Batch deterministisch via Mini-tsx-Helper-Aufruf (`lintSynopsis()` pro Buch im Ziel-File, statt Dry-Run-Per-Batch-Total-Aggregat); clean Books unangetastet (123 / 150 hit-bearing in Welle 2..4). Pro Iteration nur `overrides.synopsis`-Edits, kein Touch von `facetIds`/`factions[]`/`locations[]`/`characters[]`/`flags[]`/Top-Level. Längen-Korridor (post-080-revision, einen Tick über Pilot): Standalone-Romane / Novellas / Einzel-POV ~500–620 chars, Omnibi / Multi-POV ~620–750 chars, Soft-Floor 420 chars, Soft-Cap 800 chars (nur begründet). Empirischer Endstand über die 123 Bücher: min/mean/max `502 / 605.3 / 783` chars (pre-Rewrite `505 / 1536.3 / 2458` chars — durchschnittlich ~61 % Längen-Reduktion bei Reader-Info-Erhaltung). Per-Welle-Voice-Praxis: Welle-2 brauchte Elastizität für dünne/aggregat-shaped Frühwerke; Welle-3 setzte sich auf ~570–625 für Single-Spine-Romane; Welle-4 Necromunda blieb knappiger und straßiger; Welle-4 Closing-Batch 019 spannte zwischen Penal-Legion-Memoir, Naval-Campaign und Chapter-Tragödie. Token-Schätzung: kleine Welle-2-Iterationen ~20–45k Tokens, voll-getroffene Welle-3/4-Iterationen ~45–80k Tokens (mit Windows-Quoting- und Closing-Bookkeeping-Overhead). **Kein WebSearch in irgendeiner 081-Iteration.**

**Erhaltene Reader-Information (Brief-081-Konvention, post-Codex-Datenprüfung):** `works.synopsis` wird heute nur gerendert/angezeigt (Buch-Detail-Seite, Katalog-Row, Timeline-Cards, Audit-Cockpit, Atlas-Regen) — strukturierte Daten kommen aus `facetIds`/Junctions, nicht aus der Synopsis. Kürzen verliert keine Filter-/Resolver-/JSON-LD-Funktion. Erhalten geblieben sind beim Rewrite: Protagonist (Name + Rolle), Setting (Welt/Sektor/Schiff/Hive), zentraler Konflikt, entscheidende Faction/Antagonist (sofern in der alten Synopsis benannt), Tonality (war_story vs. political_thriller vs. body_horror als Prose-Marker). Plot-Twists, die schon Premise-Level Public-Kontext in der alten Synopsis waren, bleiben (Kage's apparente Tod / Burned-Man-Identität, Gothic-War-dritte-Novelle, Soul-Drinkers Excommunicate-Traitoris/Mutation-Setup, Daenyathos-Catechisms-Long-Game). Period-treue Faction-Naming (Tau/Imperial Guard für frühe Last-Chancers-Copy, Battlefleet Gothic/Imperial Navy für Rennie's Naval-Shelf, Eldar-Era-Wording in älteren Watson-Materialien).

**Verifikation am 019er-End:** `git status` clean post-Apply, Mini-tsx-Touch-Set 10 hits / 384 hits → 0 nach Rewrite, Non-Synopsis-Comparison zeigt nur `overrides.synopsis`-Diffs, `npm.cmd run test:synopsis-lint` 14/0, `npm.cmd run test:apply-override-dry -- --batch=ssot-w40k-019` pass + alle Batches `001..020` clean („by label: none"), `npm.cmd run db:apply-override -- --batch=ssot-w40k-019` pass (10 Updates, 0 Inserts, 10 Synopsen validiert), `npm.cmd run lint` pass (bekannte `@next/next/no-page-custom-font`-Warning in `src/app/layout.tsx`), `npm.cmd run typecheck` pass, `npm.cmd run brain:lint -- --no-write` pass (0 blocking / 10 warnings).

**Hand-off zur Loop-Disziplin ab `ssot-w40k-021`:** Vom Apply-Layer-Guard war Forward-Discipline aus Brief 061 §"Public-Synopsis-Discipline" (verankert in Brief 076) bisher nur Loop-Heredoc-Convention; ab Brief 080 ist sie zusätzlich Apply-Layer-enforced — jede künftige Loop-Iteration MUSS den Guard passieren, sonst wirft `db:apply-override` hart. Zusammen mit der Faction-Granularity-Discipline aus Brief 077 (Verboten-Liste Imperium/Imperium of Man/Imperium of Mankind/generic Chaos/Xenos/Aliens als raw_name, im Trigger-Heredoc) und dem Skip-Helper auf der Faction-Axis greift jetzt für jede neue Subsession eine Drei-fach-Disziplin (Synopsis-Sauberkeit + Faction-Granularität + Grand-Alignment-Skip), ohne weitere Maintainer-Action.

081-impl-For-next-session-Hand-off (kurz): (a) Cowork-Wiki-Hygiene + Active-Threads-Pruning — durch PR #69 auf `main` gelandet. (b) Danach folgen Brief 084 (Locations-Axis-Hygiene), Brief 085 (Hardcover-Hit-Rate-Härtung) und erst dann der Loop-Re-Trigger `ssot-w40k-021..025` über Brief 061 mit `bash scripts/run-ssot-loop.sh 5` — der Loop selbst braucht keinen eigenen Architektur-Brief, kein Skip-Flag, vier Disciplines greifen nach Brief 084 automatisch. (c) Carry-over-Shape post-Brief-076 (200er-Resolver-Pause-Reset) intakt; keine neuen synopsis-spezifischen Policy-Blocker aus den 15 Iterationen.

## Latest pipeline state (post-077, Grand-Alignment-Junction-Hygiene implementiert)

Brief 077 (Commits `9b8d593..c7ada5d` auf `session-077-grand-alignment-junction-hygiene`, 2026-05-16/17, PR-ready) schließt die Pipeline-Compliance-Lücke aus Brief 070, durch die Grand-Alignment-Tags (`Imperium of Man`, `Imperium`, `Imperium of Mankind`, `Chaos`) als reguläre `work_factions`-Junctions in die DB gerutscht waren, obwohl die Policy aus 070 sie explizit als Tree-Root-/Umbrella-Konzepte ausgewiesen hatte (gehören in `factions.alignment`, nicht als Filter-Surface). Architektur: **Skip-Logik im Apply-Layer**, nicht im Resolver-Modul (Codex-Erratum-Punkt 1 — Resolver bleibt Pure-Surface-Form-zu-ID; ob eine ID redundant ist, ist Block-Context-Information und damit Apply-Layer-Wissen). Implementation in zwei neuen Dateien plus Wiring: (a) `src/lib/seed/alignment.ts` — Alignment-Type + `inferAlignmentFromTree` + `normalizeAlignment` + `FactionAlignmentInput`, aus `scripts/seed-resolver-extensions.ts:125–141` extrahiert und in beiden Pfaden (apply-override + seed-resolver-extensions) konsistent benutzt; (b) `scripts/apply-override-skip.ts` — `decideFactionSkips()` Pure-Helper mit DI-Signatur (`resolveFaction` als Parameter), drei-Bedingungen-Skip aus Brief § Constraints, `skippedSurfaceForms` bewahrt alle Alias-Surface-Forms via Re-Resolve über `original`-Block. Wiring in `scripts/apply-override.ts`: neuer `loadSkipContext()` lädt `faction-policy.json` + `factions.json` und baut `{redundantIds, alignmentById}` mit Startup-Validation (wirft, wenn `redundantWhenSubPresent`-Eintrag entweder nicht in `factions.json` existiert oder `alignment === "neutral"` ist — Guard gegen stille Policy-Drift); `applyBook` reordered so Faction-Resolution + `decideFactionSkips()` VOR `buildSurfaceFormsBlock` laufen, der den neuen `factionsSkippedRedundant`-Bare-String-Array-Bucket im `book_details.notes`-`---surfaceForms---`-Block emittiert (nur bei non-empty — Diff bleibt minimal für Bücher ohne Skip). Parallel-Edit: `scripts/apply-override-dry.ts` simuliert den Skip ebenfalls (Dry-Run-Totals matchen DB-Realität, neue Report-Zeile „skipped surface forms: N across M books, by name: …").

Skip-Bedingung (drei Punkte, alle müssen halten): (1) Die resolved `faction_id` steht in `faction-policy.json` → `redundantWhenSubPresent` (heute: `["imperium", "chaos"]`). (2) Im selben Override-Block ist mindestens eine weitere `faction_id` resolved, deren `alignment` gleich dem `alignment` der Grand-Alignment-Row ist. (3) Diese andere `faction_id` ist nicht selbst die Grand-Alignment-Row. Erfüllen alle drei → Junction wird NICHT geschrieben, Surface-Form landet im Audit-Bucket. Erhaltungs-Pfad: trägt das Buch im Override-Block nur die Grand-Alignment-Row (keine alignment-gleiche Sub-Faction), bleibt die Junction stehen — sehr seltener Worldbuilding-/Galaxy-Wide-Survey-Fall.

Counts-Delta (`scripts/db-counts-077.ts` Pre/Post-Probe): `works=200` invariant. `work_factions=1185 → 1020` (−165). Davon `work_factions(imperium|chaos)=214 → 49` (−165). Imperium-Breakdown: `81 → 6` (−75 redundante Junctions). Chaos-Breakdown: `133 → 43` (−90 redundante Junctions). Andere Axes invariant (`work_locations=417`, `work_characters=633`, `work_collections=56` — Brief 077 berührt nur die Faction-Axis). Residual `imperium=6`/`chaos=43` sind die Erhaltungs-Pfad-Bücher (kein alignment-gleicher Sub im Override). Coverage post-077 mit Skip-Anwendung: `work_factions=1020/1301 = 78.4 %` (pre-Skip-Form-Treue `91.1 %`); Coverage-Test-Output trägt jetzt die Sidecar-Metrik `(post-Brief-077-skip, 165 grand-alignment surface forms suppressed)`. `test:resolver` 122/0 (war 116 prä-077; sechs neue Cases für `decideFactionSkips`).

Smoke (`scripts/smoke-slugs-077.ts`, alignment-aware Probe — Initial-Form war hart auf „keine imperium/chaos-Junction" geprüft und hat 4 von 6 Slugs als FAIL gemeldet; korrigierte Form prüft die Brief-Acceptance „kein imperium/chaos *sofern alignment-gleiche Sub-Tags da sind*"): 0 Skip-Rule-Violations über alle 6 Slugs. Per-Slug-Endstand: `space-wolf` (imperium skipped wegen space_wolves + adeptus_astartes, chaos preserved — keine chaos-aligned sub), `the-anarch` (beide skipped — astra_militarum + tanith_first für imperium, blood_pact + sons_of_sek für chaos), `inquisitor-draco` (imperium skipped wegen callidus_temple + inquisition, chaos preserved — Tyranids/Genestealer sind xenos nicht chaos), `the-green-tide` (imperium skipped wegen astra_militarum + catachan_jungle_fighters, chaos preserved — Orks sind xenos), `armageddon-saint` (imperium skipped wegen astra_militarum + ecclesiarchy + last_chancers, chaos preserved — Orks sind xenos), `13th-legion` (imperium skipped wegen astra_militarum + last_chancers; chaos n/a — nicht im Block). Alle 6 tragen `factionsSkippedRedundant` im `book_details.notes`-Bucket.

Loop-Discipline: neuer Constraint-Block „Faction-Granularity-Discipline (ab `ssot-w40k-021` / `W40K-0201`)" in [`sessions/2026-05-11-061-arch-ssot-loop.md`](../../sessions/2026-05-11-061-arch-ssot-loop.md) direkt nach dem Public-Synopsis-Block, mit Verboten-Liste (`Imperium` / `Imperium of Man` / `Imperium of Mankind`, generic `Chaos` wenn spezifische Chaos-Sub lore-konsistent wäre, `Xenos` / `Aliens`) + Erhaltungs-Pfad-Klärung + Begründung. `scripts/run-ssot-loop.sh`-`base_trigger`-Heredoc bekommt den Append nach der Public-Synopsis-Discipline-Zeile — jede `claude -p`-Loop-Subsession ab nächstem Lauf bekommt beide Disciplines mit, ohne weitere Maintainer-Action.

ADR-Update: [`./decisions/faction-policy.md`](./decisions/faction-policy.md) trägt die neue h2 „Grand-Alignment-Junction-Skip" zwischen „Sonderfälle" und „Was wir bewusst NICHT entscheiden", mit Skip-Liste-Location, Drei-Bedingungen-Regel, Audit-Bucket-Location, Semantik-Note (Alignment-Equality funktioniert nur für imperium/chaos, weil dort alignment = tree-root; bei künftigen xenos-Splits wäre Parent-Chain nötig — `tau` ist `xenos` wie `eldar`, aber kein Sub davon), Backfill-Path, Forward-Discipline. Neuer Revisit-Trigger für Aeldari-Sub-Splits-Aktivierung dokumentiert die Algorithmus-Umstellung (Alignment-Equality → Parent-Chain) als Vorbedingung.

077-impl-For-next-session-Hand-off: (a) **Neue OQ (11) „Locations-Axis-Hygiene-Sister-Pass"** — `test:resolver-coverage` zeigt `Imperium x20` als unresolved Location (Bücher taggen `Imperium` als Filter-Surface auch auf der Locations-Axis); strukturell analog zur 077-Faction-Lücke, eigener kleiner Sister-Brief zwischen Loop-Re-Trigger und 5er-Resolver-Pass denkbar. (b) HH-Domain Pre-Heresy `alpha_legion`-Cabal-Twist: nicht in 077 adressiert (AL hat post-070 `alignment=chaos`, würde imperium also NICHT skippen — Verhalten OK fürs Apply, aber dokumentations-würdig wenn HH-Domain aktiv wird; in `faction-policy.md` § Revisit-Trigger nicht hinzugefügt, weil HH Brief-070-Out-of-Scope-Position). (c) `smoke-slugs-077.ts` als CI-Smoke (`test:smoke-db`-Script) lohnt sich beim UI-Rollup-Brief. (d) `test:resolver-coverage` Two-Line-Output (`pre-skip: 1185/1301 (91%)` + `post-skip: 1020/1301 (78%)`) wäre informativer als die kombinierte `(post-skip, N suppressed)`-Form. (e) `brain:lint` Size-Budget-Warning auf `faction-policy.md` (108/100) tolerabel; bei wachsenden Sektionen (Aeldari-Splits / HH-Edge-Case) lohnt sich Refactor-Move von Operational-Details nach `pipeline-state.md` / Workflow-Pages.

## Latest pipeline state (post-076, axis-sliced Resolver-Pass 4 + 200 W40K-Bücher applied)

Brief 076 (PR #62, `cfb0957`, 2026-05-16) hat die vierte 50er-Welle (`ssot-w40k-016..020` / `W40K-0151..W40K-0200`, 50 Bücher) als **axis-sliced** Resolver-Pass geschlossen. Strukturell anders als Pässe 1–3: statt ein monolithischer Resolver-Brief mit allen drei Achsen in einer CC-Session sind die Achsen in **fünf manuell gefahrene Subsessions** geteilt — Phase 0 Preflight/Dossier, Phase 1 Factions only, Phase 2 Locations only, Phase 3 Characters only, Phase 4 Integration/Tests/Re-Apply/Counts/Smoke (plus Mini-Phase 5 Public-Synopsis-Discipline-Verankerung). Begründung: bei 50 Büchern wird der Cross-Axis-Cognitive-Load hoch genug, dass per-Achse-Fokus die Decision-Quality hebt. Per-Phase-Statusdateien unter [`sessions/resolver-dossiers/`](../../sessions/resolver-dossiers/) (Dossier + Phase-1/2/3-Reports + Aggregator-Helper-Script `scripts/aggregate-surface-forms-076.ts` als deterministischer Re-Run-Anker). Brief und Impl-Report bündeln aggregiert.

Reference-Schicht-Erweiterung: factions 126 → 146 (+20), locations 132 → 157 (+25), characters 129 → 169 (+40); faction-aliases 36 unverändert (alle 19 promoted Surface-Forms direct-matchen via canonical `name`), location-aliases 11 → 13 (+2: `the Phalanx` lowercase-the, `Tau Empire space` Frame-Surface-Form), character-aliases 23 → 26 (+3: `D'onne Ulanti` → `mad_donna`, `Ragnar Thunderfist` → `ragnar_blackmane`, `the Burned Man` → `lieutenant_kage`).

Vier cross-batch Alias-Consolidation-Cases entschieden, vier nach Cowork-Default + einer mit explizitem Split: (D1) **Mad Donna / D'onne Ulanti** single-row `mad_donna`, `primaryFactionId='house_escher'`; (D2) **Kal Jerico Multi-Era** single-row, kein Alias (Direct-Match identisch über Classic + Modern); (D3) **Lord Helmawr Split** — zwei Rows, classic `gerontius_helmawr` „Lord Gerontius Helmawr" vs modern `lord_helmawr` „Lord Helmawr" (Surface-Form-Disambig nur via first-name dokumentiert); (D4) **Lieutenant Kage / the Burned Man** single-row `lieutenant_kage` (rank-inclusive), Surface-Form `the Burned Man` als Alias; (D5) **Soul Drinkers Firstborn/Primaris** single-row mit neuer `tone='primaris_reboot_coexistent'` (Single-Token-Konvention analog Squats `historical_canon_layer`). Plus weitere Phase-spezifische Decisions: Necromunda-Houses unter neuem Mid-Knoten `necromunda` (parallel zu `astra_militarum`/`commissariat`/`inquisition`), Last Chancers / Howling Griffons parent-Wahl, neuer `tone='underhive'`, sub-tribal nicht promoted, ID-Konvention für „The X"-Locations folgt `great_rift`-Precedent (id droppt „the_", name behält „The"), Necromunda-Hive-Sub-Locations mit `sector=null` (kein neuer Sector, kein `underhive`-Browse-Root), Hydraphur unter existierendem `pacificus`-Segmentum, Named Vessels (`Lord Solar Macharius`/`Planet Killer`/`Brokenback`) bewusst nicht promoted (bleiben Plot-Erwähnungen), `Imperium`-als-Location documented unresolved + Cross-Axis-Guard-Test (Faction-Seite resolve, Location-Seite bewusst nicht — Pre-cursor für OQ (11)), Space-Wolves-Saga selektives `sector="obscurus"` (nur `fang` + `asaheim`), Aeronautica-Cross-Batch via Cawl-Pattern, Obispal-Skip (074-historical-canon-layer-Konsistenz), Soul-Drinkers / Calpurnia / Lord-Medell Faction-Tagging-Begründungen, Rank-inclusive ID-Konvention.

Counts post-Re-Apply 001..020 (pre-077; das ist die Pre-Skip-Basislinie für den 077-Backfill): `work_factions=1185`, `work_locations=417`, `work_characters=633`, `work_collections=56`, `works=200`. Per-Batch-Cumulative-Tabelle lückenlos im 076-impl-Report dokumentiert (Disziplin-Lesson aus 072 eingehalten). Coverage: `factions=1185/1301 = 91.1 %`, `locations=417/493 = 84.6 %`, `characters=633/811 = 78.1 %`. 13 Smoke-Slugs probed — 3 Regression-Checks (`the-anarch` 9/3/11, `inquisitor-draco` 11/0/4, `the-green-tide` 6/1/0) bit-identisch zu 074-impl (kein Re-Apply-Drift in den ersten 150 Büchern), plus 10 neue Slugs der vierten Welle (`space-wolf`, `krakenblood`, `lasgun-wedding`, `wanted-dead`, `13th-legion`, `armageddon-saint`, `soul-drinker`, `phalanx`, `crossfire`, `legacy`). Alle 7 Omnibi der Welle (Last-Chancers / Gothic-War / Soul-Drinkers / Soul-Drinkers-Annihilation-Second / Space-Wolf / Space-Wolf-Second / Inquisition-War) haben volle Roster-Constituent-Coverage → kein neuer `collection-gaps.json`-Eintrag, Datei bleibt bei einem Eintrag (Green Tide aus 074).

Cockpit-SQL-Replica (`scripts/audit-cockpit-sql-076.ts`): Welle 001..0150 `drift=106`, `gap=60`, `ssot=150`, `collections=8`; Welle 0151..0200 `drift=50/50`, `gap=15`, `ssot=50`, `collections=0`. Drift-Bucket der neuen Welle bei 50/50 = 100 % ist erwartet (Surface-Form-vs-canonical-name-Drift); wenn der Drift-Tie-Group-Sub-Sortierungs-Brief (075-impl-Hand-off als Note an OQ (10)) merged ist, wird die Cockpit-Drift-Pille-UX-Erfahrung mit der neuen Welle messbar besser.

13 unbekannte facetIds aus `manual-overrides-ssot-w40k-018.json` gestrippt (`coming_of_age`/`loss`/`vengeance`/`hopeful` × 6 Bücher modern-Necromunda-Cluster — LLM-Loop-Catalog-Typos gegen `scripts/seed-data/facet-catalog.json`, analog 074-impl-Pattern). Surface-Forms im Synopsis bleiben unangetastet; nur der `facetIds[]`-Eintrag wird gestrippt (Brief-konformer Override-Edit).

**Driver-Deliverable** (076-impl-Wahl: gebaut statt nur spec'd, Cowork-Präferenz übernommen). `scripts/run-resolver-pass.sh` (~520 Zeilen Bash, modelliert auf `scripts/run-ssot-loop.sh` aus Brief 071) + `scripts/resolver-pass.config.json` als Pass-5-Template. CLI-Form `claude -p` (symmetrisch zum Loop-Driver). Phase-Definition extern als JSON-Config statt hartcodiert. Halt-Check-Matrix: (1) `claude -p` Exit-Status == 0; (2) Worktree clean post-phase; (3) HEAD ist vorgerückt (kein no-op-Vertragsknick — diese erste Driver-Version akzeptiert keine `no-op: nothing to add`-Statusdatei); (4) `git diff --name-only <PHASE_START> HEAD` ⊆ Phase-Write-Scope (Set-Subset via `comm -23`, Brief-Erratum-Punkt 5; `scripts/test-resolver.ts` ist phasenübergreifend shared); (5) Alle in der Phase berührten `.json`-Dateien sind via `JSON.parse` valid; (6) `## Needs decision`-H2 in Per-Phase-Statusdatei → `needs_decision`-Stop, sauberer Driver-Exit ohne Folge-Phasen (Brief-Erratum-Punkt 4: Statusdatei ist die Wahrheit, nicht stdout). Bash-Glob-Patterns im Scope-Matching für Phase-4-Template (erlaubt `scripts/seed-data/persons.json`, `scripts/*-NNN.ts`, `scripts/run-phase4-apply-NNN.sh`). PR-Verhalten: bei vollständigem Lauf öffnet der Driver einen PR mit Titel `Resolver-Pass <N> (<wave>, <K>/<N> phases)`; bei Stop wird Suffix ` (needs-decision)` angehängt + Body-Sektion. PR-Idempotenz via `gh pr list --head --json url --jq '.[0].url // ""'` (Review-Fix). Branch-Konvention: Driver verifiziert nur `current branch != main`; Maintainer legt vor Lauf eine `resolver/pass-N-axis-sliced`-Branch an. Bash-syntax-validated (`bash -n`), Pass-5-Config-JSON via `JSON.parse` valid. Funktionaler Smoke-Run nicht erforderlich (Driver soll Pass 5+ steuern, nicht diesen Pass — Erratum-Punkt 7).

**Mini-Phase 5 (Public-Synopsis-Discipline)** mit-committed in Phase 4 (kein eigener Subsession-Commit): Constraint-Block in `sessions/2026-05-11-061-arch-ssot-loop.md` direkt nach den existierenden Disciplines, ab `ssot-w40k-021` greifend; `scripts/run-ssot-loop.sh`-Trigger-Heredoc-Append. Brief 077 hat danach den zweiten Discipline-Block „Faction-Granularity" an derselben Stelle ergänzt — beide Disciplines fließen jetzt in jede Loop-Subsession.

## Latest pipeline state (post-075, Cockpit-Drift-Sort + Hardcover-Rating-Backfill applied)

Brief 075 (Commits `61d4ff5` / `e0c7575` / `7f3f14f`, 2026-05-15/16) hat zwei kleine, orthogonale Refinements gelandet. **Track A — Cockpit:** `src/app/buecher/page.tsx` trägt eine `CatalogueAudit.driftCount: number`-Komponente, die im gleichen Server-Component-Read wie `hasResolvedDrift` mitläuft (kein zweiter DB-Round-Trip, kein N+1). `sortBooks(auditFilters)` rangiert bei aktivem `?audit=drift` primär nach `driftCount desc`, sekundär `Number(audit.confidence ?? "0") desc`, tertiär `updatedAt desc`, final `externalBookId localeCompare asc` als Determinismus-Tie-Break. Visuelle Affordance: `SortPills.overriddenByDrift`-Prop schaltet das Sort-Label auf „Sortieren (Audit überschreibt)"; zusätzlich rendert eine Mikro-Caption „Sortiert nach Drift-Frequenz · Confidence · zuletzt aktualisiert." nur bei `auditFilters.includes('drift') && sorted.length > 0`. `src/app/buch/[slug]/page.tsx` zieht eine siebte parallele `Promise`-Komponente aus `work_collections JOIN works` für `containedIn` und rendert eine slim Komma-Zeile direkt unter dem Meta-Strip (`font-mono text-xs text-frost-400`, Collection-Titel als `<Link>`, kein `displayOrder`/`confidence`/`basis` — Public-Lean-Constraint gewahrt). Smoke via `scripts/smoke-drift-sort-075.ts` zeigt 19 Top-Bücher auf `drift_count=2 / confidence=1.00` (flat, durch `updatedAt DESC` differenziert) und Rang 20 auf `drift_count=1` (Drop unter die freq=2-Gruppe — Triage-Intuition greift). **Track B — Daten:** `scripts/backfill-hardcover-rating.ts` ist ein Standalone-Drizzle-Script (kein `apply-override.ts`-Touch), Scope-Filter `works.source_kind='ssot' AND works.external_book_id LIKE 'W40K-%' AND works.kind='book'` (Default zusätzlich `bookDetails.rating IS NULL`, `--force` ohne diesen Filter aber im selben SSOT-Scope). `discoverHardcoverClaimV2` hat einen neuen optionalen Parameter `opts.ratingsCountField?: 'users_count' | 'ratings_count'`; Probe gegen Hardcover-Hasura bestätigt `users_count` deterministisch erreichbar — alle 77 Hits haben einen `ratingCount` geschrieben, kein Fallback nötig. Persistenz via `db.insert(bookDetails).onConflictDoUpdate(...)`; `ratingCount`-Spalte wird bei Probe-Erfolg überschrieben und ansonsten via `sql\`${bookDetails.ratingCount}\`` preserved. Author-Missing-Rows (14 Bücher: 5 Sabbat-Anthologien als Editor-only, 5 weitere Editor-only-Anthologien, 4 Roster-Pflege-Lücken inkl. `W40K-0144 Archmagos`) gehen direkt in den Miss-Bucket `no_author`, ohne `discoverHardcoverClaimV2`-Call (Erratum 3 aus Brief 075). Voller Lauf-Endstand: 77 Hits / 14 `no_author` / 40 `null_result_zero_hits` (Hardcover `_eq`-Titel-Mismatch) / 19 `author_mismatch` / 0 `graphql_error` (5 in Pass 1 transient, alle in Pass 2 aufgelöst). NPM-Script: `npm run backfill:hardcover-rating` (idempotent default; `-- --force` für Overwrite).

075-impl-Architektur-Punkt für die V2-LLM-Stage falls sie je reaktiviert wird: `users_count` ist nun ein bekannt-deterministischer Hardcover-Field; der `discoverHardcoverClaimV2(title, author, { ratingsCountField: 'users_count' })`-Call kann von einer reaktivierten Pipeline ohne weitere Schema-Recherche benutzt werden. Konsequenz für OQ6 (siehe `open-questions.md`): geschlossen. Konsequenz für die Promote-Trigger-Frage (OL-Fallback vs. Normalisierung): Hit-Rate 51.3 % < 70 % hat den Trigger aus Brief 075 gefeuert, aber das Miss-Profil ist primär Titel-Normalisierungs-bezogen (40× null_result_zero_hits + 19× author_mismatch) statt Coverage-Lücke — neue OQ (10) Hit-Rate-Härtung als bevorzugter Folgepfad gegenüber OL-Fallback dokumentiert.

## Latest pipeline state (post-074, third resolver wave applied)

Brief 074 (PR #57, `6ac4295`) hat die dritte Resolver-Welle für `ssot-w40k-011..015` geschlossen: 20 neue Factions (Hydra-Cabal als Watson-Trilogy-historical-canon-Knoten unter `inquisition`, Squats-`tone`-Update auf `historical_canon_layer` via Upsert, Sororitas-Order-Sub-Factions, Astartes-Loyalist-Sub-Factions, Aeldari-/Necron-/Navis-Sub-Factions, Goffs, Aeronautica Imperialis), 19 neue Locations (Imperium-Nihilus-Frames mit `era_frame`, Necron-Tomb-Worlds, Watson-Welten mit `historical_canon_layer`-Tag, Named-Vehicles), 26 neue Characters (Belisarius Cawl als 074-Cross-Batch-Anchor + Hadeya Etsul + Aeronautica-Cross-Batch + Watson-Retinue Jaq Draco/Meh'Lindi/Vitali Googol/Grimm mit `historical_canon_layer`-Marker in `notes` + lore-iconic-Singletons). Aliases erweitert (Space Sharks, Dark City, Cawl-Multispelling, Imperium, Chaos Space Marines, …). `triarch_praetorians` alignment=`xenos` (lore-konsistent statt initialer `neutral`-Tendenz). 13 unbekannte facetIds aus 015-Override gestrippt (LLM-Typos der Loop-Subsession gegen veralteten Catalog-Snapshot: `interplanetary`/`freedom`/`discovery`/`duty`/`early_release`). Green Tide (`W40K-0147`) bleibt voll im Resolver-Scope, kein partieller `work_collections`-Eintrag — neuer `scripts/seed-data/collection-gaps.json`-Ledger hält die 4 existierenden + 4 fehlenden Short-Story-Constituents fest.

Brief-Erst-Annahme-Korrektur in 074-impl: Brief 074-arch hatte "150 W40K-Bücher in DB applied" als Pre-State angenommen — Reality vor Apply waren 100, weil der Loop-Driver-PR #54 die 011..015-Override-Files nur committed und nicht via `db:apply-override` in die DB geschrieben hatte. CC hat das gefangen, der 15× Re-Apply war daher gleichzeitig Drift-Cleanup für 001..010 (`work_factions` 650→651, `work_characters` 475→476 nach Review-Follow-up) und First-Apply für 011..015. Wirkung neutral; Wahrheit ist im Post-074-Zustand kodifiziert.

Counts post-Re-Apply `001..015`: `work_factions=912`, `work_locations=287`, `work_characters=522`, `work_collections=35`. Per-Batch-Zwischenstände lückenlos im 074-impl-Report dokumentiert (Disziplin-Lesson aus 072 eingehalten). Coverage `factions=912/1003 = 90.9 %`, `locations=287/342 = 83.9 %`, `characters=522/677 = 77.1 %` direkte Matches; Rest erwartete freq=1-Surface-Forms. Watson-Trilogy junction-spot-check: 49 Rows über `inquisitor-draco`/`harlequin`/`chaos-child`, alle 4 Retinue-Characters × 3 Bücher direkt resolved. Smoke-Slugs (`f/l/c/in-coll`): `honourbound=5/1/1`, `the-infinite-and-the-divine=4/2/2`, `brutal-kunnin=4/1/1`, `krieg=5/1/0`, `archmagos=3/0/1`, `inquisitor-draco=11/0/4`, `voidscarred=4/0/0`, `the-green-tide=6/1/0/0` (collection-side null wie erwartet).

Audit-Cockpit-Tour (SQL-Replica der vier Pillen): post-Apply `0001..0100` hat `drift=72`, `gap=29`, `ssot=100`, `collections=8`, `drift_and_gap=21`; post-Apply `0101..0150` hat `drift=34` (Imperium / Chaos Space Marines bewusst aufgelöst → als raw_name-vs-canonical-Drift sichtbar), `gap=31`, `ssot=50`, `collections=0`, `drift_and_gap=22`. Cockpit-Quality-Feedback aus 074-impl: Drift-Pille braucht freq-/confidence-Sort innerhalb der Liste, damit Maintainer-Triage die freq≥2-Drifts zuerst sieht — heute muss man pro Buch in `/buch/<slug>/audit` einsteigen, um die rohen Surface-Forms zu sehen.

Hand-off zur künftigen Vokabular-Hygiene-Session aus 074-impl: 9 `value_outside_vocabulary`-Tag-Kandidaten aus Loop-Log (`commissar` / `inquisitor` / `squat` / `corsair` / `triarch_praetorian` / `valkyrie_pilot` / `webway_journey` / `omnibus_with_prior_constituents` / `cabal_inquisition` / `rogue_inquisition` / `cw_canon_divergence`) + 5 Catalog-LLM-Typos (siehe oben) + 5 freq=1-Sororitas-Sub-Orders aus *Triumph of Saint Katherine* + 5 `data_conflict`-Author-Missing-Flags (W40K-0141/0142/0143/0146/0147 → Excel-Maintainer-Workflow).

Brief 055 hat den ersten V2-Voll-Lauf über 50 Bücher geliefert (`v2-batch-20260510-1109.diff.json`, slug-window `13th-legion → ascension`, 1.06 web_search/Buch, 4-of-5 Validator-Kinds non-zero). Brief 056 hat die drei strukturellen Schwächen aus dem Voll-Lauf isoliert behoben: per-page Lexicanum-Cache, per-book Diff-Checkpointing, Cost-Recompute auf Cache-Hits, plus Diff-Archivierung.

Brief 057 hat den **Excel-SSOT-Pivot vollzogen** (siehe [`./decisions/why-excel-ssot-not-crawl.md`](./decisions/why-excel-ssot-not-crawl.md)): Maintainer-Excel liegt unter `scripts/seed-data/source/`; Loader `import-ssot-roster.ts` produziert deterministisch `scripts/seed-data/book-roster.json`. Brief 058 hat den V2-SSOT-Modus gebaut; 060/061 haben die ersten fünf W40K-10er-Batches in den Authority-Layer gebracht.

**CC-Erweiterungen in 057-impl ggü. Brief-Acceptance** (Cowork akzeptiert): RosterBook-Schema trägt zusätzlich `editors: string[]` + `editorialNote: "various" | null` (Konsequenz aus den zwei OQ-Antworten "Various Authors" + "(ed.)"-Trailing). `external_book_id` ist `UNIQUE` ohne separaten Index (B-Tree-Backing reicht). `work_collections`-Junction trägt nur einen Sekundär-Index auf `content_work_id` (composite-PK deckt `collection_work_id` als Leading-Column ab; Maintainer kann via 0009-Mini-Migration nachreichen falls gewünscht). Year-Validierung pragmatisch: `null` → warn (1 Treffer: W40K-0307 "War for Armageddon Omnibus"), non-numeric → loud-Error.

Brief 063–069 haben die Resolver-Schicht für die ersten 50 Bücher geschlossen: 0009 applied, resolver reference data seeded, `apply-override` schreibt `work_characters`, normalisiert Rollen und bewahrt Surface-Forms in `raw_name`. 069-Counts nach Apply: `work_factions=318`, `work_locations=129`, `work_characters=363`.

Brief 072 hat die zweite Resolver-Welle für `ssot-w40k-006..010` geschlossen: 54 neue Factions inkl. `heretic_astartes`-Mid-Knoten (parent=`chaos`) + Reparent von 7 Heresy-Traitor-Legionen + optional Alpha Legion auf den Mid-Knoten, Death-Guard / Emperor's-Children-Lücke gefüllt, 45 neue Locations + `great_rift` in-place mit `era_frame`-Tag, 38 neue Characters (16 freq≥2 + 22 lore-iconic), Aeldari-/Drukhari-Aliase, `apply-override.applyCollections` löst Cross-Batch via `works.external_book_id`. Post-Re-Apply `001..010`: `work_factions=650`, `work_locations=239`, `work_characters=475`, `work_collections=35` (Baseline same-batch 17 → Refactor 35); Coverage `factions=650/657`, `locations=239/258`, `characters=475/552`; Smoke `the-anarch=9/3/11`, `calgars-fury=7/3/1`, `the-emperors-gift=8/2/1`, `storm-of-iron=6/1/6`, `celestine=5/1/1`, `spear-of-the-emperor=8/3/2`. Pre-Apply-Counts und per-Batch-Counts wurden im Report nicht enumeriert (Disziplin-Note für nächstes Mal). Character-Junction-Wachstum +112 für 50 Bücher (vs. +363 in erster Welle) — Long-Tail-Surface-Forms, primärer Cockpit-Triage-Datenpunkt.

V2-Pilot (54), V2-Voll-Lauf (55), V2-Pre-Roster-Fixes (56), SSOT-Pivot (57/58), Resolver-Layer (063–069) und Resolver-Pass 2 (072) sind in [`./pipeline-state.md`](./pipeline-state.md) detailliert.

## V2-Pilot-Architektur (post-054)

V2-Pilot (Brief 054) läuft als Parallel-Pfad neben V1. Strukturelle Änderungen gegenüber V1:

- **Discovery-Spine erweitert.** TLBranson reading-order-Seiten als zweite Discovery-Quelle neben Wikipedia (cross-WordPress-Cache, kein Cloudflare). Patcht die Wikipedia-Frische-Lücke ab Dezember 2025 (chem-dog Proof-of-Coverage).
- **Lexicanum infobox-only.** Body-Year-Regex (`extractUniverseYears`) aus dem FIELDS-Pfad entfernt — schreibt nur noch infobox `Setting`/`Date`/`Story Date`. Body-Scan bleibt für Audit-Evidence (`claim.raw.bodyYearCandidates`), wird aber nie zu einem Field promoted ohne Validator-Use-Suggestion.
- **Fünf deterministische Validatoren.** `year_outlier` (Series-Anker-Tabelle für HH/SoT/Eisenhorn/Ravenor/Cain/Dawn-of-Fire), `edition_isbn_conflict`, `pagecount_outlier` (`<30` drop, `>1500` flag), `author_editor_suspicion` (Anthologie-Heuristik), `lexicanum_missing` (Transparenz-only). Validatoren modifizieren Stage-1-Claims nicht direkt; sie produzieren `Validation[]`, das Stage 4 in den finalen Record foldet.
- **Slim-LLM.** Rating und Availability raus aus `PUBLISH_ENRICHMENT_TOOL.input_schema.properties`. `WEB_SEARCH_TOOL.max_uses: 3` (statt 6), 1 obligatorisch (synopsis-context). Structured `factions`/`locations`/`characters` mit `{name, role}` statt flacher Namenslisten. Neuer `PROMPT_VERSION_HASH_V2` invalidiert den V1-Cache.
- **BookV2Record + Provenance pro Feld.** `FieldRecord<T>` = `{ value, source, fetchedAt, override?, evidence? }` pro Feld, mit explizitem Override-Slot für Hand-Korrekturen.
- **Hardcover-Mismatches silent-skipped.** Author-Mismatches landen NICHT mehr in `errors[]`; nur echte Crawler-Fehler (HTTP/GraphQL/Token).

V2-Pilot-Acceptance-Numbers aus `ingest/.archive/v2-pilot/v2-pilot-20260509-1934.diff.json` (5 Bücher; archived in 056) und V2-Voll-Lauf aus `ingest/.last-run/v2-batch-20260510-1109.diff.json` (50 Bücher) sind in [`./pipeline-state.md`](./pipeline-state.md) detailliert. Codex-Verdikt aus [`../raw/reviews/2026-05-09-codex-v2-pilot-review.md`](../raw/reviews/2026-05-09-codex-v2-pilot-review.md) (post-Pilot, pre-Voll-Lauf): „klar die bessere Pipeline-Basis, aber noch nicht die Pipeline, die ich ohne 055-Test direkt auf 700+ Bücher loslassen würde." Post-055-Befund bestätigt das: an 50 Büchern trägt V2 strukturell, aber Latenz + Cost-Telemetrie + Halt-Resilienz brauchen die Verbesserungen aus 056.

## What's open

Top items from [`./open-questions.md`](./open-questions.md), neu sortiert post-PR-112 + post-PR-113 (Post-Merge-Koordinations-Pass, 2026-05-30):

- **Audiobook-Credits — Full-Rebuild-Restore-Wiring — erledigt (Brief 107, gemergt 2026-05-30).** Der Brief-105-Durability-Fix schützte nur die *routinemäßige* Re-Apply; ein voller Rebuild (`db:reset-for-ssot` = TRUNCATE works CASCADE) löschte die 88 Audio-`work_persons`-Rows, und keine Orchestrierung zog `apply:audiobook-narrators` nach. Brief 107 macht den Audio-Apply zum deterministischen **Tail**: neuer confirm-gegateter `db:rebuild`-Orchestrator (`scripts/db-rebuild.sh`: reset → Voll-Korpus-Apply-Wellen via `scripts/db-rebuild.config.json` → Audio-Apply → `--verify`) + `sessions/db-rebuild-runbook.md`; `apply-audiobook-narrators.ts` additiv um `--verify` (exakte `(workId,personId,role)`-Mengen-Gleichheit gegen die Sidecar-Erwartung, Sidecar-abgeleitet, nicht literal). Bestehende Bausteine unverändert, `apply-override.ts` nicht angefasst, `db:seed` off-path bestätigt. Detail siehe Frontmatter-Header oben.
- **Brief-105-UI-Pass (Cluster A, Product) — erledigt (gemergt 2026-05-30, PR #114).** `/buch/[slug]` rendert die Kauf-/Hör-Aktionen über `buildStoreUrl` (server-seitig geo-lokalisiert, affiliate-bereit ohne Tag) + `AudioCredit` (single/duet/ensemble/full-cast) + `RegionSwitcher`; server-seitige Region mit Fallback-Kette (`?store=` > Vercel-Geo > Accept-Language > US); Personen-Query-`orderBy`-Fix (L73) + displayOrder; **ISBN-Select adressiert die Short-Title-Store-Link-Kollisionen** aus dem Daten-Review. Brief 105 damit vollständig `status: implemented` (beide Pässe). Detail siehe Frontmatter-Nachtrag oben.
- **PR #113 (Chronicle/Ask-Redesign) — Cowork-Sichtung + 096-Status offen.** `b60b0fb` hat Brief-096 Phasen G (Chronicle/Timeline bespoke) + H (Ask-Funnel) Material gemergt (lokal-iterativ, kein Impl-Report). Noch nicht Cowork-reviewt; eine eigene Sichtung + die Entscheidung, ob Brief 096 damit `open → implemented` flippt (oder ob G/H-Reste bleiben), stehen aus.
- **Audiobook-Full-Sweep 859 (Langzahn) + Cast-Tiefe + Spelling-Watch.** Brief 105 hat 66 Bücher gecreditet (Major-Reihen + meistgelesene Titel); die ~790 Rest-Bücher sind ein empfohlener Folge-Sweep (gleiche One-Search-pro-Buch-Methode). Zusätzlich: Audio-Drama-Cast-Tiefe (Lexicanum listet teils mehr Performer als die Amazon-Byline) und Spelling-Watch (eine Performerin als „Charlotte Page"/„Charlotte Paige", „Seán Barrett" akzent-variabel — slugifyt jeweils konsistent). Die 4 `no_audiobook`-Audit-Rows sind reale Gaps, keine Fehler. Short-Title-Store-Link-Kollisionen (*Legion*/*Nemesis*/*Censure*/*Krieg*/*Leviathan* können in der Amazon-Such-URL Non-40K-Treffer liefern → ISBN-keyed Links bzw. später stored `external_links`-Override) sind ein UI-Pass-relevanter Daten-Befund.
- **Brief 105 Daten-Pass — erledigt (gemergt 2026-05-29, PR #112).** 66 Bücher Audiobook-Credits in committed Sidecar `scripts/seed-data/audiobook-narrators.json` + durable `persons`/`work_persons` (88 Rows, 29 Performer; 58 single / 5 multi / 3 full-cast), Durability-Fix (gescopter Override-Delete author/editor). Cowork-reviewt: **akkurat** (Zahlen jq-verifiziert + 3 Credits web-gegengeprüft), **stabil** (idempotent, transaktional, einziger work_persons-Delete), **sinnvoll**. Detail siehe Frontmatter-Header oben.
- **Edition-Rename-Alias-Cluster — erledigt (Brief 104, gemergt 2026-06-01, PR #118).** Korrigierte Prämisse: der ursprüngliche „Alias-Eintrag pro Form → ~133 Junctions raus, Batches-Mini-Diff"-Plan war falsch — die Aliase existierten schon in `faction-aliases.json` (korrekt aufgelöst), Drift ist ein reiner `rawName ≠ canonical.name`-Vergleich, das Cockpit las die Maps nicht. Der Hebel saß in der **App-Schicht** (Product). Brief 104 hat das geteilte `src/lib/aliases/`-Modul gebaut (SSOT = die existierenden `*-aliases.json`), den Drift-Test auf drei entity-genaue Zustände (none / known-alias / suspicious) umgestellt und an allen drei Call-Sites durch eine gemeinsame `classifyDrift`/`tallyAxisDrift` geroutet. **Ergebnis: `drift_works` 380 → 0** — *jeder* der 500 Drift-Junctions über 380 Bücher ist ein registrierter Edition-Rename (größer als die ~133-Schätzung = nur Top-3-Cluster). Known-alias rendert als eigene ruhige cyan-Klasse + optionale `?audit=alias`-Pille; der Such-Contract `resolveSurfaceForm` ist exponiert + Test-belegt (15/0), aber noch nicht verdrahtet. Detail siehe Frontmatter-Header oben + `2026-06-01-104-impl-alias-aware-drift.md`.
- **Brief-104-Folgen (next-brief / maintainer-Material, kein Blocker).** Aus dem 104-impl: (a) `?audit=drift` ist auf dem aktuellen Korpus **dauerhaft leer** (drift_works=0) — die Drift-Pille + das Brief-103 `driftScore`/`· drift «X» ×N`-Signal funktionieren weiter, haben aber keine Daten; behalten (leerer Bucket = positives Data-Quality-Signal) / umlabeln / zurückbauen ist eine spätere UI-Entscheidung. (b) Die ruhige Alias-Klasse liegt **always-on auf ~44 % der Bücher** (380/859, Chip + inline-Signal im Cockpit) — ggf. detail-only oder hinter `?audit=alias` falls zu busy. (c) Der Such-Contract `resolveSurfaceForm` (Surface-Form → kanonische Entität, Liste, `matchedVia` alias|canonical-name) ist geliefert + Test-belegt, aber nicht verdrahtet → konsumiert von **Brief 109 Step 3** (universelle Suche); finale Normalisierung (Diakritika, `T'au`) = Such-Session. (d) **Maintainer-Visual-Pass offen**: CC fuhr nur curl-Smoke (4× `?audit=` HTTP 200, Alias-Chips + inline-Signale rendern mit echten Daten); der Desktop- + ≤640px-Sichtcheck (`?audit=alias` ruhig/abgesetzt; `/buch/<slug>/audit` cool known-alias vs. amber drift; No-Regression `?audit=gap`) steht aus.
- **Brief 103 UI-Pass — erledigt (gemergt 2026-05-28, PR #111).** Audio-Drama-Dämpfung im Single-Gap-Modus (`opacity:.4` + kleinerer entsättigter Faction-Dot 10px → 5px + Mono-`Audio`-Tag, volle Opacity auf `:hover`/`[open]`) + URL-getriebene Toggle-Pille `?hideAudio=1` (kein localStorage) + Drift-Tie-Group-Sub-Sortierung nach `driftScore = sum(globalDriftFreq(rawName))` primär + Axis-Triple lex-DESC sekundär + `updatedAt DESC` tertiär. Smoke-Cross-Check `?audit=gap&hideAudio=1` = **325 Rows** = exakt die `audit:gap-candidates`-Helper-Zahl aus dem Daten-Pass. Brief 103 jetzt vollständig `status: implemented`. Detail siehe Frontmatter-Header oben.
- **Brief 096 (Design-Direction) — Phasen A–F gemergt (PR #110), G + H bleiben offen.** PR #110 (147 Files / +21 578 / -1 082) hat Phasen A (globaler Shell mit Cinzel / Cormorant / Plex Mono + SiteBackground + TopNav), B (Hub), C (`/buecher`-Reskin), D (`/buch/[slug]` Bespoke), E (Stub-Shells für `/fraktion` / `/welt` / `/charakter` / `/ask` / `/map`), F (internal-tools Token-Aliasing-Verify) gemerged plus die Map-/Atlas-Inventories (`/lib/galaxy/*`, `/lib/atlas/*`, `/lib/audio-tracks.ts`, `/lib/askPaths.ts`, `src/components/map/*`) und zwei Map-Polish-Followups. Phasen G (Timeline bespoke) + H (Ask 6-path funnel mit canonical six paths) bleiben im Brief 096 deferred — laufen `lokal-iterativ` im `chrono-lexicanum-product`-Worktree, kein neuer Brief. **Update post-PR-113 (2026-05-30):** PR #113 (`b60b0fb`, Chronicle-Redesign + UI-Pass: Timeline-Port + Ask/Buecher/Map) hat dieses G+H-Material aus dem Product-Worktree gemergt (lokal-iterativ, kein Impl-Report) — Cowork-Sichtung + Entscheidung über einen 096-Status-Flip stehen aus (§ What's open Kopf).
- **Sessions-Archive-Sweep + Numbering-Collision-Fix.** Die offene Sessions-Pile wächst: 100/101/102/103 (alle vier impl + arch, vier davon ein-Hälften-Pair, zwei davon Strang-Split) + Pass-11..15-Dossiers (5 reports + 1 final dossier) + consolidation-pass-2-* (sechs Artefakte) — Pile reif für einen Brief-099-Style-Sweep nach `archive/2026-05/`. **Plus zwei Numbering-Collision-Files:** `2026-05-27-098-impl-map-dive-flicker-followup.md` und `2026-05-27-099-impl-map-transition-polish.md` (Product-Strang-Followups aus PR #110) reuse NNN=098/099, schon vom Batches-Strang vergeben. `brain/wiki/workflows/sessions-format.md` spezifiziert NNN als **projektweit monoton**; der Sweep muss die zwei Files umnennen (Vorschlag: nächste freie NNN ab `104` und `105`, mit `parent`-Frontmatter-Anpassung in der zweiten Datei und Pfad-Rewrites in den `links:`-/`sources:`-Listen falls referenziert).
- **`/buch/[slug]/audit`-Detail-Refinement (UI + Daten verzahnen).** Aus dem PR-111-UI-impl For-next-session: die Listenansicht zeigt jetzt Top-Surface-Form + Häufigkeit; die Detailseite könnte analog die volle Drift-Achse + die fehlende Gap-Achse anzeigen („Buch hat `locations=0`; Synopsis nennt: Tallarn, Pelago, …"). Brief 103 ließ es bewusst aus. Folge-Brief-Material, das UI + Daten verzahnt.
- **Audio-Drama-Dämpfung evtl. site-weit übertragbar.** Heute nur im Single-Gap-Cockpit. Wenn die public `/buecher`-Liste (Brief 096) Audio-Dramen je markieren will, ist `book.format === 'audio_drama'` + `.catalogue-row__audio-tag` wiederverwendbar (gleiche Klassen).
- **Cleanup-Pass auf Legacy-Tokens** (aus 096-impl For-next-session). `--color-void / --color-aquila / --color-frost-*` (Tailwind v3-style) sind nach Phasen A–F möglicherweise vollständig unused; ein Code-Review-Sweep über `src/**/*` würde das bestätigen, dann lassen sich die `@theme {}`-Aliases droppen. Out-of-scope für 096; Watch-Item.
- **Brief 103 Daten-Pass — erledigt (gemergt 2026-05-28, PR #109).** `audit:gap-candidates`-Helper liefert deterministische read-only Triage-Liste (325 Kandidaten total, HH=73, W40K=252; Achsen-Breakdown factions=0 → 29, locations=0 → 228, characters=0 → 194); zwei Pilot-Backfills HH-0260 *Hunter's Moon* + HH-0270 *Iron Corpses* haben NEW-Range gap_works 18 → 16 gesenkt. Neue Reference-Anker: `pelago` (location, `tags=["space_wolves"]`) + `koparnos` (character, `primaryFactionId="iron_warriors"`). Brief 103 selbst mit `status: implemented` im Code-PR mit-portiert. **Brief-103-Forecast-Korrektur**: "~10–20 echte fixable" galt für post-Triage-Subset, nicht den Helper-Raw-Output. Detail siehe Frontmatter-Header oben.
- **Maintainer-Excel-Sweep über `audit:gap-candidates`-Restliste — laufende Maintainer-Arbeit, kein Brief.** Per Brief 103 § Out-of-Scope explizit als Maintainer-Workflow markiert: 325 Kandidaten → ~10–20 echte fixable Backfills via Maintainer-Triage. Pro-Buch-Effort laut Brief-103-impl ~5 min Entscheidung + ~1 min Edit + ~3 min Verify (Voraussetzung: Synopsen sind so lore-precise wie HH-0260/HH-0270). Skalier-Schätzung 2–4 h für die echten Fixables. Restliste schmilzt über Wochen mit `git log` über `ingest/.last-run/audit-gap-candidates.md` als Time-Series.
- **Brief 102 / HH-Konsolidierungs-Pass — erledigt (gemergt 2026-05-27, PR #108).** Konsolidierungs-Pass 2 über das datenkomplette Reference-Set (W40K + HH). 23 Cluster → 2 Character-Merges, 21 No-Merges, 0 Flagged; cross-era-anchor-breach Aggregator-Tripwire 0 Treffer über 18 pinned Surface-Forms = positive Brief-100-ADR-Validation. characters 491→489, work_characters 1992→1997, EXPECTED_RANGES.characters.max 2200→2500. `verify-pass.ts`-Out-of-Range-Bolt-on landed (Brief 101 § Open questions gefaltet). Multi-Range-Loader-Normalisierung. **Brief-094-§-Cadence-Bogen geschlossen** — der verpflichtende finale Konsolidierungs-Pass nach HH ist gefahren. Detail siehe „Latest pipeline state (post-PR-108)" oben.
- **Phase-3-Abschluss-Meilenstein erreicht.** **Korpus konsolidiert + datenkomplett** — beide Domänen sealed (565 W40K + 294 HH = 859/859), zwei Konsolidierungs-Pässe gefahren, Cross-Era-Disziplin operativ + post-Konsolidierung gehärtet. Phase 3 (Bulk-Backfill-Pipeline) ist substantiell erledigt. **Offener Architektur-Call: formaler Seal-Brief** (`ROADMAP.md` flippen, Phase 4 / 5 öffnen, was bleibt explizit als Backlog auf den Phase-3-Tail mappen) — Kandidat (b) für den nächsten Architekten-Brief.
- **Audit-Cockpit-Drift/Gap-Sweep — abgesplittet zu Brief 103.** Brief 103 Daten-Pass (PR #109) hat den `audit:gap-candidates`-Helper + 2 Pilot-Backfills (HH-0260/HH-0270, Pass-15-Forecast bestätigt) gemerged. Brief 103 UI-Pass (Audio-Drama-Dämpfung im Gap-Bucket + Drift-Tie-Group-Sub-Sortierung) verbleibt als nächster CC-Job im Product-Worktree (siehe oben). Der ursprüngliche „Data-Quality-Brief-Kandidat" für den nächsten Architekten-Brief ist damit als Brief 103 gefahren.
- **Aggregator-Pass-3-Refinement (langfristig).** Aus 102-impl § For-next-session: false-positive-Klassen aus Pass-2 könnten als auto-no-merge-Regeln im Aggregator codiert werden — `tags:vessel↔planet`-Pattern auf locations (3 false-positives in Pass 2) + `primarch-stem↔captain-named-after-primarch` auf characters (1 false-positive). Bevorzugte Form: `auto-no-merge-rationale`-Annotation im Aggregator-Output (Cluster bleiben sichtbar, als „expected false-positive" gelabelt). Lohnt erst, wenn ein nächster Konsolidierungs-Pass anliegt — heute nicht zeitkritisch.
- **Codex-Review-Sicht auf Pass 2.** Pass 1 hatte einen Codex-Read-Pass-Review post-Brief-Übergabe. Für Pass 2 wäre ein analoger Lesepass auf die Cross-Era-Identity-Handhabung (Merge 1: keeper-wins-on-conflicting-primaryFactionId mit notes-Rewrite) sinnvoll als externe ADR-Anwendungs-Validation. Out-of-Scope für diesen Pass; kann mit dem nächsten Codex-Review-Slot mit-laufen.
- **HH-Domain-Data-Findings — kumulativ ~148 drift_works + ~98 gap_works als Watch-Item.** Über die fünf Pässe 11–15 sind in den NEW-Range Audit-Replicas drift_works und gap_works angefallen (Pass 14: 50 total, 25 drift, 21 gap; Pass 15: 44 total, 25 drift, 18 gap; analoge Verteilungen Pässe 11–13). Die drift_works sind erwartet (Cross-Era-Aliases + Honor-Title-Splits + Primarchen-Pattern: `raw_name ≠ canonical.name` per Disziplin). Die gap_works sind mehrheitlich der strukturelle Audio-Drama-Single-Axis-Shape (HH-0260 *Hunter's Moon* `l=0,c=0`, HH-0270 *Iron Corpses* `c=0`, HH-0290 *Blackshields: The Broken Chain* `l=0` sind Pass-15-Beispiele); ein Audit-Cockpit-Drift-Sweep konfirmiert pro Buch, ob es eine fixable Data-Quality-Lücke oder die erwartete sparse-axis-Form ist. Pass-15-impl-Forecast: HH-0260 + HH-0270 als wahrscheinlichste Backfill-Kandidaten. **Data-Quality-Cycle-Material, nicht Resolver-Pass-Job.**
- **Cockpit-Refinements** (Note an OQ (10), kein eigener OQ-Eintrag — siehe `open-questions.md`-Note): (a) Drift-Tie-Group-Sub-Sortierung innerhalb der freq=2-flat-Gruppe (075-impl-Punkt 3) — Cowork-Session 2026-05-25: an den Product-Strang / Brief 096 angedockt statt eigener Brief; (b) `scripts/smoke-slugs-077.ts` als `npm run test:smoke-db`-Regression-Probe (heute ad-hoc-Script, lohnt wenn UI-Rollup-Brief kommt); (c) `test:resolver-coverage` Two-Line-Output (`pre-skip: N/M` + `post-skip: N/M` statt der kombinierten `(post-skip, N suppressed)`-Form).
- **Hand-Check-Workflow + Override-Schema** (OQ3). Cockpit ist seit Brief 073 verfügbar; Schema-Beschreibung steht noch aus. 074-impl + 075-impl + 076-impl + 077-impl haben echtes Drift-/Rating-/Skip-Material gefüttert — Quality-Feedback bestätigt OQ3-Bedarf (Override-Field-Schema + Triage-Disziplin).
- **Public-Page-Rating-Render — an Brief 096 angedockt.** `bookDetails.rating` liegt für ~556/565 W40K-Bücher in der DB, aber `/buch/[slug]` rendert das Feld nicht. Cowork-Session 2026-05-25: kein eigener UI-Brief — das Rating-Render gehört in die redesignte Buchseite des laufenden Brief-096-Redesigns (ein Parallel-Brief würde im Product-Worktree mit dem in-flight 096-Stand kollidieren). Der Product-Strang zieht es dort mit.
- **Collection-Gap-Ledger-Pflege.** `scripts/seed-data/collection-gaps.json` trägt nach Pass 7 vier Einträge (Green Tide W40K-0147 + drei Pass-7-Fälle, neu W40K-0434 *Space Wolves (Legends)*). Pass 7 hat zudem vier weitere Sammelwerke als Spot-Check-Kandidaten deferred (Ultramarines-Legends W40K-0427, Astra-Militarum-Legends W40K-0429, Shas'o-Legends W40K-0431, Ahriman:Exodus W40K-0372). Schließen lässt sich ein Ledger-Eintrag erst, wenn der Maintainer-Excel-Workflow die fehlenden Constituents als eigene Roster-Works (bzw. Collection-Kanten) modelliert — reine Roster-/SSOT-Loop-Arbeit, kein Resolver-Job. Trigger: Excel-Update durch Maintainer.
- **Slug-/Title-Delta W40K-0259 / W40K-0330 — weiter geparkt (post-Pass-7 bestätigt).** Der Roster-Hygiene-Sweep (OQ (14), PR #85) hat in `book-roster.json` zwei Titel + Slugs korrigiert (W40K-0259 → `the-rose-in-anger`, W40K-0330 → `the-hunt-for-magnus`). Resolver-Pass 7 hat die Batches 026/033 kumulativ re-applied und — wie Brief 093 vorhergesagt — `slug`/`title` **eingefroren** gelassen (der Update-Pfad in `apply-override.ts` schreibt nur `synopsis`/`updatedAt`; ein Identity-Rename „requires an explicit re-import path that brief 060 does not implement"). Das Delta bleibt eine eigene kleine Maintainer-Entscheidung: einen expliziten Slug-/Title-Re-Import-Pfad bauen — oder das kosmetische Delta pre-launch akzeptieren (`external_book_id` ist der stabile Identity-Anker). OQ (14) selbst bleibt geschlossen.
- **`scripts/run-ssot-loop.sh`-Refinements** (post-071-impl). per-iter timeout, shellcheck-Lokal-Setup, workflow-page `brain/wiki/workflows/ssot-loop-driver.md`. Nicht load-bearing, nach 2–3 produktiven Driver-Läufen.
- **Vokabular-Hygiene-Session.** 074-impl + 076-impl haben den Hand-off-Stack vergrößert: 9 Loop-Log-Tag-Kandidaten + 5 Catalog-LLM-Typos (074) + 4 facet-Values aus 076 (in 076 gestrippt statt promoted). Brief 089 hat den ersten Wert davon promoted — `commissar` als `protagonist_class` (Call 2) — und zwei Folgepunkte hinterlassen: (a) **Backfill-Retag früherer `guardsman`-Workaround-Commissar-POVs** (Cain, Gaunt, Minka Lesk, …) auf `commissar`, jetzt wo das Vokabular existiert (089 hat nur `021..025` umgetaggt); (b) offene Vokabular-Watch-Items: `protagonist_class` `tank_crew`/`penal_legion`/`psyker`/`sanctioned_psyker`, `plot_type=duel` (W40K-0340 *Hunter's Snare*, `026..035`-Welle) und neu aus der `036..045`-Welle ein `length_tier` unterhalb `novella` für echte Short Stories (W40K-0432/0433 — vorerst `length_tier` weggelassen statt überstuft) sowie `protagonist_class.knight_noble` für Imperial-Knight-Piloten (W40K-0445/0446 *Kingsblade*/*Knightsblade* — `protagonist_class` weggelassen statt mis-getaggt). Cockpit-Triage statt einmaliger Architektur-Brief (Maintainer-Punkt 2026-05-13), aber wenn der Stack zu zehn+ Cases anwächst lohnt sich ein gebündelter Mini-Brief.
- **Resolver-Loop — headless-Mehr-Wellen-Lauf wird der HH-Trial.** Pass 8 (`046..051`) + Pass 9 (`052..057`) liefen supervised über `run-resolver-pass.sh`; der erste echte headless Mehr-Wellen-Lauf des `run-resolver-loop.sh`-Drivers ist post-Brief-100 der HH-Trial-Lauf (siehe „HH-Resolver-Trial" oben). Brief 097 hat den `readonly STATE_FILE`-Crash im Loop-Driver gefixt. Die `resolver-loop-log.md`-Marker-Drift war durch Brief 098 Teil 1 verifiziert erledigt (Detektor meldet sauber, jetzt `all-complete` für W40K-resolved-zero-HH-pending bzw. `open-wave` für W40K-complete-HH-pending).

## Recently shipped (session-level)

| Date | Session | Status | Topic |
|---|---|---|---|
| 2026-06-01 | Brief 104 (alias-aware-drift) + PR #118 (`1142c82`) | merged | **Alias-aware Drift — `drift_works` 380 → 0.** Geteiltes `src/lib/aliases/index.ts` (SSOT = `scripts/seed-data/*-aliases.json` + Entity-JSONs als statische Imports, Resolver-Pattern, keine Kopie). Drift-Test boolean → drei entity-genaue Zustände (none / known-alias / suspicious) via `classifyDrift`/`tallyAxisDrift`, an allen drei Call-Sites (`queries.ts`, `buecher/page.tsx`, `buch/[slug]/audit/page.tsx`) — dedupliziert die drei `countResolvedDrift`/`isDrift`-Kopien. Known-alias = ruhige cyan-Klasse (vs. warm/amber drift): `ALIAS n`-Chip + inline `· alias «Surface → Canonical»` + optionale `?audit=alias`-Pille (URL-getrieben; via geteilter `AuditPills` auch auf `/atlas/werke`). Such-Contract `resolveSurfaceForm(query) → AliasResolution[]` exponiert (normalisiert, achsen-übergreifend, Liste = ambiguitäts-ehrlich, `matchedVia` alias|canonical-name), **nicht** verdrahtet, Test `scripts/test-aliases.ts` 15/0. **drift_works 380 → 0**: alle 500 Drift-Junctions über 380 Bücher sind registrierte Edition-Renames (größer als ~133-Schätzung = nur Top-3-Cluster; volle Maps × 3 Achsen ≈ 500). Kein Schema/DB/Resolver/Override-Touch (+166/−79, 8 Files). Cowork-Review: sauber, Befund gemessen + logisch zwingend (Resolver speichert nur `(rawName,id)` mit `alias→id`). CC hat `status: open → implemented` im Code-PR geflippt. |
| 2026-05-30 | Brief 107 (Full-Rebuild-Restore-Wiring) + PR #115 (`21072ab`) | merged | **`apply:audiobook-narrators` als deterministischer Tail der SSOT-Reset-Sequenz (Batches) — Cluster-A-Abschluss.** Neuer confirm-gegateter `db:rebuild`-Orchestrator (`scripts/db-rebuild.sh`) verkettet `db:reset-for-ssot --confirm` → `run-phase4-apply.sh scripts/db-rebuild.config.json` (Voll-Korpus W40K 1..57 + HH 1..30 = 859) → `apply:audiobook-narrators` (finaler Tail) → `--verify` (read-only). Schließt den Brief-105-Review-Watch-Item: ein voller Rebuild stellt die 88 Audio-`work_persons`-Rows jetzt deterministisch wieder her (vorher still gelöscht). `apply-audiobook-narrators.ts` additiv um `--verify` erweitert — **exakte `(workId,personId,role)`-Mengen-Gleichheit** gegen die Sidecar-Erwartung (Codex-Härtung gegen Total-only-False-Positive, Negativ-Test bewiesen), Sidecar-abgeleitet (88 = 63/12/13), nicht literal. Eigene Voll-Korpus-Config (`scripts/db-rebuild.config.json`, nicht `consolidation-pass-2` zweckentfremdet), `sessions/db-rebuild-runbook.md`, `package.json` +`db:rebuild`; `apply-override.ts`/`run-phase4-apply.sh`/`db-reset-for-ssot.ts` Internals unverändert; `db:seed` off-path. Destruktiver E2E-Lauf maintainer-owned (DB steht im Ziel-Stand); Komponenten einzeln non-destruktiv grün (Confirm-Gate exit 1, Verify read-only exit 0, Masking-Negativtest exit 1, Trias/lint/typecheck/brain:lint grün). Brief `status: implemented` (CC im Code-PR geflippt). |
| 2026-05-30 | Brief 105 UI-Pass (Buy/Listen + Hörbuch-Credits-Render) + PR #114 | merged | **Cluster A komplett — UI-Pass gemergt, Brief 105 `implemented`.** `src/lib/store-links.ts` (geo-lokalisierendes affiliate-bereites `buildStoreUrl`, kein Tag — `STORE_AFFILIATE_TAGS` aktivierbar ohne Call-Site-Änderung), `src/lib/store-region.ts` (server-Region `?store=` > Vercel-Geo > Accept-Language > US), `src/components/book/` (BuyListenActions + AudioCredit single/duet/ensemble/full-cast + RegionSwitcher-Client-Island), `/buch/[slug]` orderBy-Fix L73 + displayOrder + ISBN-Select + Rail, `.env.example` `STORE_AFFILIATE_TAGS` (leer), Impl-Report `2026-05-29-105-impl-product-buy-listen-links.md`. **ISBN-Select adressiert den Short-Title-Store-Link-Befund** aus dem Daten-Review. CC hat **Brief 105 `status: open → implemented`** im PR-114-Commit geflippt. commit `c4d019c`. |
| 2026-05-30 | Post-Merge-Koordinations-Pass (post-PR-112 + post-PR-113 / Brief 105 Daten-Pass + Brief 096 G+H) | complete | PR #112 (Brief 105 Daten-Pass, Hörbuch-Credits) + PR #113 (Chronicle/Ask-Redesign, Brief 096 G+H) in die Rollup-Dateien eingearbeitet (`project-state` / `open-questions` / `index` / `log` / `sessions/README`). **Cowork-Review von PR #112: akkurat** (Coverage-Zahlen jq-verifiziert + 3 Credits web-gegengeprüft — For-the-Emperor/Butcher's-Nails/Nightbringer exakt), **stabil** (Durability-Fix = einziger `work_persons`-Delete in apply-override, idempotent + transaktional, Validierung lehnt doppelte (person,role) ab), **sinnvoll**. Counts: `work_persons` 785 → 873 (+88 Audio-Rollen), `persons` +29 Performer; keine Schema-Migration, keine `external_links`. Neue Watch-Items in § What's open: **Full-Rebuild-Restore-Wiring** (Review-Befund — `apply:audiobook-narrators` gehört in die Reset-/Rebuild-Sequenz, sonst killt `db:reset-for-ssot`/`db:seed` die Audio-Credits), Brief-105-UI-Pass (Product, offen), PR-#113-Sichtung + 096-Status, 859er-Audiobook-Sweep, Cast-Tiefe/Spelling-Watch, Short-Title-Store-Kollisionen. Keine OQ-Schließung. Kein Code-/Schema-/DB-Touch — doc-only auf `main` direkt. |
| 2026-05-29 | Brief 105 Daten-Pass (Hörbuch-Credits) + PR #112 | merged | **Cluster A Daten-Hälfte — Audiobook-Credits live.** 66 Bücher (33 HH / 33 W40K) in committed Sidecar `scripts/seed-data/audiobook-narrators.json` (keyed by `externalBookId`, je Credit name/role/sourceUrl/checkedAt/confidence + optional note) → durable auf `persons` + `work_persons`: **88 Audio-Rollen-Rows** (narrator 63 / co_narrator 12 / full_cast 13), 29 neue Performer; 58 single-narrator / 5 multi / 3 full-cast; 4 `no_audiobook`-Audit-Einträge (alle pre-2008). **P1-Durability-Fix:** Override-Apply-Delete auf `inArray(role, ["author","editor"])` gescopt → Re-Apply lässt narrator/co_narrator/full_cast unangetastet (live verifiziert, `{author:9,narrator:10,co_narrator:4}` identisch). Neue Skripte: `apply-audiobook-narrators.ts` (idempotent, `--dry-run`/`--file`, per-Buch scoped Delete-then-Insert in Tx, Auflösung über `external_book_id` UNIQUE), `src/lib/seed/persons.ts` (`slugifyPerson`/`deriveNameSort` extrahiert, Brief-077-`alignment.ts`-Pattern), `test-audiobook-narrators.ts` (12/12 DB-frei). Quelle: per-Buch-Websuche → Amazon/Audible-Byline (Goodreads-Muster; Spot-Check ~18 vor Bulk). Keine Schema-Migration, keine `external_links` (Storefront-Links UI-derived). UI-Pass (Kauf-/Hör-Aktionen) noch offen. Tests grün (typecheck/lint/brain:lint; `test:resolver` 473/473 bestätigt Slug-Extraktion verhaltensgleich). Branch `codex/ingest-batches-audiobook-credits`, commit `c9b7c55`. |
| 2026-05-29 | Brief 096 Phasen G+H (Chronicle/Ask-Redesign) + PR #113 | merged — Cowork-Sichtung offen | **Chronicle-Redesign + UI-Pass (Timeline-Port, Ask/Buecher/Map).** Product-Worktree, Brief-096 Phasen G (Chronicle/Timeline bespoke) + H (Ask-Funnel), **lokal-iterativ ohne eigenen Impl-Report** (096-Konvention). Neue Chronicle-Komponenten-Suite `src/components/timeline/chronicle/*` (AccordionShell/BookNode/BookTooltip/ChronicleClient/LayoutLanes/LayoutSequence/OverviewRibbon/SegmentView/ZoomInShell/context), `src/lib/chronicle/roster.ts` + `src/lib/timeline.ts` + `src/lib/timelineAdapter.ts`, `src/app/timeline/page.tsx`-Rewrite (`loading.tsx` entfernt), Ask-Redesign `src/components/ask/*` + `src/app/ask/page.tsx`, `src/app/buecher/ScrollScrim.tsx`, Lab-Prototyp `public/lab/timeline-prototype/*`, `globals.css` +928. Noch nicht Cowork-reviewt; 096-Status-Flip (open → implemented?) + eigene Sichtung stehen aus. Commit `b60b0fb`. |
| 2026-05-28 | Post-Merge-Koordinations-Pass (post-PR-110 + post-PR-111 / Brief 096 Phasen A–F + Brief 103 UI-Pass) | complete | PR #110 (Product-Strang Design-Direction Phasen A–F + Map/Atlas-Inventories + zwei Map-Polish-Followups, 147 Files) und PR #111 (Brief 103 UI-Pass, 5 Files) in die Rollup-Dateien eingearbeitet (`project-state` / `open-questions` / `index` / `log` / `sessions/README`). Korpus bleibt 859/859 datenkomplett + konsolidiert. Brief 103 jetzt vollständig `status: implemented` (beide Hälften durch — Daten via PR #109, UI via PR #111); Brief 096 bleibt `status: open` (Phasen G + H deferred, lokal-iterativ im Product-Worktree). Edition-Rename-Alias-Cluster-Brief als Top-Empfehlung für den nächsten Architekten-Brief identifiziert (UI-impl OQ-2-Befund: Imperial Guard ×77 + Eldar ×34 + Dark Eldar ×22 + Mechanicum ×19 + Horus Lupercal ×19 + Lucius ×11 = ~133 Junctions in einem Alias-Mini-Diff fixable, kein ADR-Verletzung). Disziplin-Befund: NNN=098/099-Collision der zwei Map-Polish-Files (Product-Strang reuse von Batches-Strang-NNN) für nächsten Sessions-Archive-Sweep markiert. Watch-Items: `/buch/[slug]/audit`-Detail-Refinement, site-weite Audio-Drama-Dämpfung, Legacy-Token-Cleanup, Brief-096 G+H, Sessions-Archive-Sweep + NNN-Fix. Keine OQ-Schließung; kein Code-/Schema-/DB-Touch — doc-only auf `main` direkt. |
| 2026-05-28 | Brief 103 UI-Pass + PR #111 | merged | **Audit-Cockpit Drift/Gap-Sweep — UI-Pass landed.** Im Single-Gap-Modus (`?audit=gap` als *einziger* Filter, AND-Semantik `auditFilters.length === 1 && auditFilters[0] === 'gap'` explizit, nicht naives `.includes('gap')`) werden Audio-Dramen auf `opacity:.4` gedämpft + mit kleinerem entsättigtem Faction-Dot (10px → 5px) + Mono-`Audio`-Tag im `__main` markiert (überlebt Mobile-Spaltenkollaps, volle Opacity auf `:hover`/`[open]`). URL-getriebene Toggle-Pille `?hideAudio=1` (kein localStorage, separater Param statt `audit=`-Pillen-Erweiterung) versteckt sie hart. Drift-Tie-Group flach (drift_count=2 / confidence=1.00) jetzt sub-sortiert nach `driftScore = sum(globalDriftFreq(rawName))` über resolved drift-rawNames primär + Axis-Triple lex-DESC `(faction, location, character)` sekundär + `updatedAt DESC` tertiär; pro Row rendert ein sichtbares `· drift «Surface-Form» ×N`-Signal die Top-Häufigkeits-Surface-Form. Tight Diff: `src/app/buecher/page.tsx` (Drift-Score-Wiring, AND-Semantik, hideAudio-Param-Plumbing, topDriftSignal()-Per-Row-Anzeige, BookRow-dimmed+driftSignal-Props), `src/app/buecher/GapAudioToggle.tsx` (neues Client-Island, 43 Zeilen, useSearchParams + router.replace), `src/app/globals.css` (gescopter `.is-audio-dim`-Block hinter `.catalogue-row[open]`, `__audio-tag` + `__drift-signal` Mono-Klassen). Smoke-Cross-Check: `?audit=gap&hideAudio=1` = **325 Rows** = die `audit:gap-candidates`-Helper-Zahl aus dem Daten-Pass (Daten-Pass und UI-Filter teilen dasselbe Prädikat). Brief 103 jetzt vollständig `status: implemented`. Wichtigster UI-impl-Befund (OQ 2): die globale rawName-Häufigkeit zeigt Edition-Renames (Imperial Guard ×77 → astra_militarum, Eldar ×34 → aeldari, Dark Eldar ×22 → drukhari) als reine Synonyme, die in einem kleinen Batches-Pass via Alias-Einträgen ~133 Junctions aus dem Drift-Bucket nehmen würden. Tests grün (typecheck, lint, brain:lint, 4 URL-Smoke-Varianten). |
| 2026-05-28 | Brief 096 Phasen A–F + Brief-096-out-of-scope Map-/Atlas-Inventories + zwei Map-Polish-Followups + PR #110 | merged | **Product-Strang Design-Direction Phasen A–F gemergt.** Drei zusammen geschnürte Linien in einem PR (147 Files / +21 578 / -1 082): (a) **Brief 096 Phasen A–F** (impl `2026-05-23-096-impl-design-direction.md`, vier Commits d37c590 / 492b352 / ed611c1 / a55679a) — globaler Shell + Hub (A+B), `/buecher`-Reskin (C), `/buch/[slug]` Bespoke (D), Stub-Shells für `/fraktion` / `/welt` / `/charakter` / `/ask` / `/map` (E), internal-tools Token-Aliasing-Verify (F); Cinzel / Cormorant Garamond / IBM Plex Mono via `next/font/google`, SiteBackground + TopNav (5-Link-Nav mit DATASTREAM-STABLE-Indikator + Suspense-EraToggle), zwölf neue Chrono-Primitives unter `src/components/chrono/*` (MainAuspex, AuspexSweep, CornerAuspex, FloatingCoord, ScanLine, Typewriter, LiveTelemetry, ProcessingDots, GhostReadout, LetterField, WordField, BottomConsole), `lib/useReducedMotion.ts` (SSR-safe), `lib/faction-colors.ts` (6 kanonische Dot-Farben), neue WebP-Backdrops unter `public/img/`. (b) **Map-/Atlas-Inventories** — `/lib/galaxy/*` (coords/data/eras/share/stars/storage/themes/types — strukturierte Galaxy-Domäne aus dem Lab-Cartographer-Prototyp Brief 079), `/lib/atlas/*` (auth/decks/format/queries/types — Atlas-Inventory-Anbindung), `/lib/audio-tracks.ts` + `/lib/askPaths.ts`, `src/components/map/disc/*` + `src/components/map/editor/*` + `src/components/map/tweaks/*` + `src/proxy.ts`. (c) **Zwei Map-Polish-Followups** — `2026-05-27-098-impl-map-dive-flicker-followup.md` (Dive-Curtain + delayed-view-switch + capped Preview-Zoom + Mechanicus-`SELECTING SEGMENTUM...`-Copy gegen Chrome-Flicker) und `2026-05-27-099-impl-map-transition-polish.md` (Reticle-Entfernung + 180→560ms settle-Window + Tint/Scanlines durch transitions stabil + `animsOn`-Gating für SMIL-Hazard-Zones in dived Segmentum). Brief 096 bleibt `status: open` — Phasen G (Timeline bespoke) + H (Ask 6-path funnel) sind im 096-impl deferred, das Brief-Container-Vehikel trägt sie weiter `lokal-iterativ` im Product-Worktree. DB-Schema unverändert. `/buecher` URL-Kontrakt erhalten. `robots: index:false` erhalten. Tests grün (lint clean, tsc clean, build mit DATABASE_URL placeholder green, alle 11 Routes built). |
| 2026-05-28 | Post-Merge-Koordinations-Pass (post-PR-109 / Brief 103 Daten-Pass) | complete | PR #109 (Brief 103 Daten-Pass) in die Rollup-Dateien eingearbeitet (`project-state` / `pipeline-state` / `open-questions` / `index` / `log` / `sessions/README`). Korpus bleibt 859/859 datenkomplett + konsolidiert. `audit:gap-candidates`-Helper als neues Tooling notiert, NEW-Range gap_works 18→16 (HH-0260 + HH-0270 raus), zwei neue Reference-Anker (`pelago` location, `koparnos` character). Brief 103 selbst trägt `status: implemented` (CC hat geflippt); UI-Pass-Rest in Active-Threads + § What's open ausgewiesen. Brief-103-Forecast-Korrektur dokumentiert (Helper-Raw-Output 325 Kandidaten vs. Brief-Forecast ~10–20 post-Triage). Keine OQ-Schließung (Daten-Pass adressiert keine numerierte OQ); Watch-Items in § What's open (Pelago-Tags-Review, Audit-Helper-Snapshot-Persistenz, UI-Pass-Spec, Maintainer-Excel-Sweep). Kein Code-/Schema-/DB-Touch — doc-only auf `main` direkt. |
| 2026-05-28 | Brief 103 Daten-Pass + PR #109 | merged | **Audit-Cockpit Drift/Gap-Sweep — Daten-Pass landed.** `npm run audit:gap-candidates` (`scripts/audit-gap-candidates.ts`, 246 Zeilen, read-only SQL über `works ⨝ book_details` mit `kind='book' AND format <> 'audio_drama' AND (factions=0 OR locations=0 OR characters=0)`, deterministischer Konsolen-Output + Markdown-Snapshot unter `ingest/.last-run/audit-gap-candidates.md`, idempotent verifiziert). Zwei Pilot-Backfills: HH-0260 *Hunter's Moon* (Russ + Alpharius als `mentioned` im Override, neuer `pelago`-Anchor in `locations.json` mit `tags=["space_wolves"]`, Post-State `f=2/l=1/c=2`); HH-0270 *Iron Corpses* (neuer `koparnos`-Anchor in `characters.json` mit `primaryFactionId="iron_warriors"`, Override-File unverändert — Resolver greift den bestehenden POV-Eintrag, Post-State `f=1/l=1/c=1`). NEW-Range gap_works `18 → 16` (-2, exakt der Brief-Forecast). Story-Chars Tidon/Ven/Sareo/Felbjorn bewusst NICHT promoted (Halluzinations-Schutz / Single-Mention-Bloat-Vermeidung — Brief 103 § Notes). Brief 103 § Notes idempotent-Konvention: CC hat `status: open → implemented` im Code-PR mit-portiert; UI-Pass-Teil läuft als separater Pass im Product-Worktree. Branch `codex/ingest-batches-audit-pilot`. Alle Tests grün (typecheck, lint, brain:lint, test:resolver 473/0, test:resolver-data 10/0 inkl. neuer coverage-smoke-Check `w40k-001..057 + hh-001..030`, test:apply-override-dry `out-of-range=0/unknown-work=0`, test:collection-refs 10/0). |
| 2026-05-27 | Post-Merge-Koordinations-Pass (post-PR-108 / Brief 102 HH-Konsolidierungs-Pass) | complete | PR #108 (Brief 102 / HH-Konsolidierungs-Pass) in die Rollup-Dateien eingearbeitet (`project-state` / `pipeline-state` / `open-questions` / `index` / `log` / `sessions/README`). Korpus jetzt 859/859 datenkomplett *und* über beide Domänen konsolidiert. Cross-Era-ADR um „HH-domain post-consolidation hardened: 2026-05-27"-Halbsatz ergänzt (positive Brief-100-ADR-Validation: cross-era-anchor-breach Tripwire 0 Treffer über 18 pinned Surface-Forms). Brief-094-§-Cadence-Bogen geschlossen — verpflichtender finaler Pass nach HH ✓. Keine OQ-Schließung (Pass 2 ist Maschinerie-Lauf ohne Architektur-Call); drei Watch-Items in § What's open (Audit-Cockpit-Drift/Gap-Sweep, Aggregator-Pass-3-Refinement, Codex-Review-Sicht auf Pass 2). Kein Code-/Schema-/DB-Touch — doc-only auf `main` direkt. |
| 2026-05-27 | Brief 102 (HH-Konsolidierungs-Pass) + PR #108 | merged | **Korpus konsolidiert — 859/859 + 2 Character-Merges.** Konsolidierungs-Pass 2 (Full-Corpus W40K 1..57 + HH 1..30) clean durchgelaufen: 23 Kandidaten-Cluster → 2 Merges (`merir_astelan→astelan` cross-era lore-deep + `sharrowkyn→nykona_sharrowkyn` same-era mechanical), 21 No-Merges, 0 Flagged. **Cross-era-anchor-breach Aggregator-Tripwire = 0 Treffer** über 18 pinned Cross-Era-Surface-Forms → positive Brief-100-ADR-Validation. Drei HH-aware Aggregator-Klassen scharf (Slug-edit-distance / Cross-era-anchor-breach-Re-Annotation / Primarch-stem); 1 false-positive locations + 1 false-positive characters, 0 true-positives in den HH-Layern (alle 2 Merges via Pass-1-Base-Heuristiken). `verify-pass.ts`-Out-of-Range-Bolt-on landed (`if (verify)`-guarded, config-agnostisch), Pass-2-Lauf = 0 (positive Tripwire am datenkomplettem Korpus, Brief 101 § Open questions gefaltet). Counts: characters `491→489` (-2 mergees), character-aliases `64→66` (+2), work_factions `2752→2754`, work_locations `1144→1145`, work_characters `1992→1997` (+5 re-pointed via neue Aliases). `EXPECTED_RANGES.characters.max 2200→2500` (~25% headroom). Multi-Range-Loader-Normalisierung (`applyRanges: ApplyRange[]`, Singleton-Backward-Kompat, Hard-Error bei beidem). Pass-1-Maschinerie/Artefakte unangetastet. **Brief-094-§-Cadence-Bogen geschlossen** — verpflichtender finaler Pass nach HH ✓; künftige Konsolidierungs-Pässe ad-hoc, nicht mehr im Cadence-Default. 7 Commits c01a795..037b618 auf branch `codex/ingest-batches-hh-consolidation-pass`. |
| 2026-05-27 | Post-Merge-Koordinations-Pass (post-PR-107 / Resolver-Loop 5 Wellen) | complete | PR #107 (Resolver-Loop 5 Wellen HH `ssot-hh-003..030`, Pässe 11–15) in die Rollup-Dateien eingearbeitet (`project-state` / `pipeline-state` / `open-questions` / `index` / `log` / `sessions/README`). DB-Stand jetzt 859/859 — Korpus datenkomplett (565 W40K + 294 HH). Cross-Era-ADR über den vollen HH-Korpus operativ gehärtet (Halbsatz „HH-domain hardened: 2026-05-27" ergänzt). Keine OQ-Schließung (Pässe sind Daten-Wellen ohne Architektur-Call); HH-Restwellen-Punkt aus What's-open entfernt (durch PR #107 selbst erledigt). Pass-15-impl-For-next-session-Watch-Items (Audit-Cockpit-Drift/Gap-Follow-up, characters-cap Re-Tune) in § What's open verlinkt. Kein Code-/Schema-/DB-Touch — doc-only auf `main` direkt. |
| 2026-05-27 | Resolver-Loop 5 Wellen (Pässe 11–15, `ssot-hh-003..030`) + PR #107 | merged | **HH-Domäne datenkomplett — DB 585 → 859** (+274 HH-Bücher HH-0021..HH-0294). Erster echter headless Mehr-Wellen-Lauf des `run-resolver-loop.sh`-Drivers; fünf clean Two-Domain-Pässe in Folge (Pass 11: 60 / Pass 12: 60 / Pass 13: 60 / Pass 14: 50 / Pass 15: 44 — exakt der Brief-100-Forecast 60/60/60/50/44), null `## Needs decision`-Stops, null facetId-Strips. EXPECTED_RANGES.locations.max 1100 → 1500 (Pass 15, Pass-14-forecasted Re-Tune; ~24% post-bump headroom). Counts post-Pass-15-Re-Apply: `works=859`, `work_factions=2752` (+771 seit Pass 10), `work_locations=1144` (+368), `work_characters=1992` (+667), `work_collections=196` (+49 — deferred Pass-10-Anthology-Constituent-Edges idempotent materialisiert + neue HH-Anthology-Edges), `work_persons=785` (+244), `work_facets=16845` (+5173). Reference factions `179 → 202` / locations `234 → 288` / characters `404 → 491`. Cross-Era-ADR operativ gehärtet (drei Pass-15-Cases: `Aenoid Thiel → aeonid_thiel` Typo, `Bjorn the One-Handed → bjorn` Pre-Dreadnought-Honor-Title, `Lord Cypher → cypher` Cross-Era Heresy-era → post-Heresy Fallen-Lord-of-the-Fallen). Verify-Digest Pass 15 clean: 5 Smoke-Slugs (HH-0260/0270/0280/0290/0294), 44/44 Goodreads-rated für die Pass-15-NEW-Range, OLD-Range HH-0001..HH-0250 cumulative `content_in_collection=49` (Brief-101-deferred-edge-Pattern trägt). Resolver-Loop emittiert jetzt `all-complete`. |
| 2026-05-26 | Post-Merge-Koordinations-Pass (post-101 + Pass 10) | complete | Brief 101 (PR #106) + Resolver-Pass 10 (PR #105) in die Rollup-Dateien eingearbeitet (`project-state` / `pipeline-state` / `open-questions` / `index` / `log` / `sessions/README`). DB-Stand 585/859, HH-Bootstrap in Postgres, 20 out-of-range Constituent-Edges deferred auf Folge-Wellen. Cross-Era-ADR um „first wave validated"-Halbsatz ergänzt. Keine OQ-Schließung (Brief 101 = Maschinerie-Fix, Pass 10 = Daten-Welle); 2 gap_works als Watch-Item in § What's open. Kein Code-/Schema-/DB-Touch — doc-only auf `main` direkt. |
| 2026-05-26 | Resolver-Pass 10 (`ssot-hh-001..002`) + PR #105 | merged | **HH-Bootstrap-Apply landed — DB 565 → 585** (+20 HH-Bücher HH-0001..HH-0020). Sechs Phasen completed: Phase 1-3 JSON-Reference-Layer +6 factions / +10 locations / +60 characters / +9 aliases (Cross-Era-Disziplin sauber — Luna Wolves→`sons_of_horus`, Mechanicum→`mechanicus`, Imperial Army→`astra_militarum`, Lucius→`lucius_the_eternal`, Ezekyle Abaddon→`abaddon_the_despoiler`). Phase 4a halt-disziplin-konform auf `## Needs decision` gestoppt (20 HH-Anthology→Constituent-Edges aus den drei HH-Anthologien — exakt der Pass-10-Dossier-§7d-Stop-Trigger); Brief 101 hat den Guard gefixt; Re-Run commit `42a6fad` clean (`test:apply-override-dry` `out-of-range=20, unknown-work=0`). Counts post-Apply: `work_factions 1903→1981`, `work_locations 733→776`, `work_characters 1220→1325`, `work_collections 147 (unverändert — Constituent-Edges out-of-range, materialisieren auf Folge-Wellen)`, `work_persons 524→541`, `work_facets 11291→11672`. Reference factions 173→179 / locations 224→234 / characters 344→404. Phase 4b Verify-Digest clean: Smoke-Slugs `tales-of-heresy` (7f/2l/9c) + `the-primarchs` (6f/1l/5c), Rating-Coverage 20/20 Goodreads, NEW-Range Audit 20 total / 15 drift_works (Cross-Era-Alias-Resolving wirkt erwartungsgemäß) / 2 gap_works (Data-Findings) / 0 content_in_collection. |
| 2026-05-26 | Brief 101 (HH-Bootstrap Guard-Reason-Split) + PR #106 | merged | **`apply-override-dry.ts` Assertion reason-split.** Cowork-Maintainer-Wahl: Option 1 aus drei Optionen — Assertion auf `reason === "unknown-work"` eingeschränkt, `out-of-range` informativ im Console-Reason-Breakdown sichtbar (`by reason: out-of-range=N1, unknown-work=N2`, auch bei beiden=0), `unknown-work` bleibt scharfer Tripwire. Drei Test-Cases (a/b/c) in `test-apply-override-collections.ts` (10/0); volle Resolver-Trias grün. `apply-override-collections.ts` unangetastet (Reason-Klassifikation bereits korrekt). Optionen 2 (domain-aware seal) und 3 (allowlist) explizit verworfen. Brief 101 selbst mit `status: open → implemented` im Code-PR mit-portiert. |
| 2026-05-26 | Brief 100 (HH-Resolver-Domain-Öffnung) + PR | merged | **Resolver-Loop zwei-domänen-fähig.** Detektor + Wrapper + Auto-Config + Verify-Trias auf W40K + HH erweitert (drei externe Terminal-Zustände `open-wave | idle | all-complete`); W40K→HH interner Branch-Point. HH-Bootstrap-Welle `ssot-hh-001..002` (20 Bücher, Pass 10) mechanisch; spätere HH-Wellen regulär 50/60. Pass-Counter global fortlaufend. Verify-Trias Variant A (`{domain, n}`-Tupel mit `as const satisfies`); HH-Tupel pro Welle via Phase-4a-Trigger appended. EXPECTED_RANGES angehoben (factions/locations/characters max 2500/1100/2200; Minima unverändert). Runbook §4 erweitert um Cross-Era-Identitäten-Subsektion (Faction-Rename, Character-Honor-Title-Split, Primarchen-Pattern, Disambig-Ausnahme, Surface-Form-Treue). 36 Detektor-Tests grün (von 22 prä-100); resolver/data/coverage/dry/loop-next/brain-lint alle grün; Live-Smoke liefert die erwartete Bootstrap-Welle. W40K-Hardcode-Sweep über das übrige Pass-Ökosystem negativ. |
| 2026-05-26 | HH-SSOT-Loop-Lauf `ssot-hh-001..030` + PR(s) | merged | Standing-Loop (Brief 061), 30 Iterationen — alle 294 HH-Bücher kristallisiert (`ssot-hh-001..030`-Override-Files committed). Diff-only, kein DB-Touch. Roster damit voll kristallisiert: 565 W40K + 294 HH = 859/859 Override-Files. |
| 2026-05-25 | Post-#099-Koordinations-Pass | complete | Brief 099 (Sessions-Archiv-Sweep) gemergt → in die Rollup-Dateien eingearbeitet (`project-state` / `open-questions` / `index` / `log` / `sessions/README`). Kein Code-/Schema-/DB-Touch. |
| 2026-05-25 | Brief 099 (Sessions-Archiv-Sweep) + PR | merged | **`sessions/`-Root aufgeräumt.** 53 geschlossene Session-Files (062–097) nach `sessions/archive/2026-05/` verschoben, 188 relative Pfad-Referenzen in 12 `brain/wiki/**`-Files umgeschrieben. `brain:lint` 0 blocking, `lint` 0 errors, `typecheck` clean. Reine Repo-Hygiene, kein Code-/DB-Touch. |
| 2026-05-25 | Post-Merge-Koordinations-Pass (#98) + Brief 099 | complete | Brief 098 in die Rollup-Dateien eingearbeitet (`project-state` / `pipeline-state` / `open-questions` / `index` / `log` / `sessions/README`); Brief 099 (Sessions-Archiv-Sweep) geschrieben. Zwei UI-Posten (Rating-Render, Cockpit-Drift-Sub-Sort) an den Product-Strang / Brief 096 angedockt statt eigener Brief. Kein Code-/Schema-/DB-Touch. |
| 2026-05-25 | Brief 098 (W40K-Konsolidierungs-Pass) + PR | merged | **W40K konsolidiert.** Neuer Pass-Typ für Cross-Wave-Canonical-Row-Dedup, gebaut + erstmals über das W40K-Entitäten-Set (~743 Reference-Rows) gefahren. 15 Kandidaten → 2 Merges (`vigilus_s`→`vigilus`, `magister_sek`→`anakwanar_sek`), 13 no-merges, 0 geflaggt. Reference `locations 225→224`, `characters 345→344`, `factions 173` unverändert; alle Junction-Counts invariant. Neue `consolidation-*`-Skripte + `consolidation-pass-runbook.md` + Session-Start-Callouts; Maintainer-Review-Gate eingehalten. Teil 1 (`resolver-loop-log.md`-Verifikation): kein Drift. |
| 2026-05-25 | Post-Merge-Koordinations-Pass (post-094/095/096) + Brief 098 | complete | #94 / #95 / #96 in die Rollup-Dateien eingearbeitet; Brief 098 (W40K-Konsolidierungs-Pass) geschrieben; Brief 096 vom Altbranch `codex/brain-coordination-post-095` ins Repo zurückgeholt. Kein Code-/Schema-/DB-Touch. |
| 2026-05-24 | Resolver-Pass 9 (`ssot-w40k-052..057`) + PR #96 | merged | **W40K complete — 565/565 Bücher.** Finale Welle W40K-0511..0565 → DB 510 → 565. Counts `work_factions 1795→1903`, `work_locations 683→733`, `work_characters 1170→1220`, `work_collections 145→147`. `EXPECTED_RANGES.factions.max` 1900→2100. Dry-Prediction == DB-POST-APPLY exakt, Trias grün, kein needs-decision. |
| 2026-05-23 | Resolver-Pass 8 (`ssot-w40k-046..051`) + Brief 097 + PR #95 | merged | Achte Welle W40K-0451..0510 → DB 450 → 510. Plus Brief-097-Hotfix: `readonly STATE_FILE`-Crash in `run-resolver-loop.sh` gefixt + Pass-8-Loop-Log-Backfill. Supervised über `run-resolver-pass.sh` gefahren. |
| 2026-05-23 | SSOT-Loop-Lauf `ssot-w40k-046..057` + PR #94 | merged | Standing-Loop (Brief 061), 12 Iterationen — die 115 W40K-Reste kristallisiert. Diff-only, kein DB-Touch. W40K-Roster damit voll kristallisiert (565 Override-Files). |
| 2026-05-23 | Post-Merge-Koordinations-Pass (Pass 7 + 094 + 095) | complete | Brain-Ingest für die drei seit dem post-092-Stand gemergten Stränge: `project-state.md` auf 450 W40K-Bücher + neue „Latest pipeline state (post-095)"-Sektion, `pipeline-state.md` (Pass-7-Counts + Resolver-Loop-Konvention), `open-questions.md` (Header), `index.md`/`log.md`, `sessions/README.md` Active-Threads. Kein Code-/Schema-/DB-Touch. Erster Pass unter der Brief-095-Rollup-Ownership-Regel (Cowork als alleiniger Schreiber von `sessions/README.md` + `brain/**`). |
| 2026-05-23 | 095-impl + merge | complete | **Rollup-Ownership.** Worktree-gebundene Regel: `sessions/README.md` + `brain/**` werden nur noch im Koordinations-Worktree geschrieben, nie in den Strang-Worktrees; Strang-Reports tragen System-Fakten, Cowork zieht sie im Post-Merge-Pass nach. Plus Worktree-Selbstprüfung am Session-Start (Ansage + Halt-mit-Rückfrage bei Strang-Mismatch). `CLAUDE.md` + `AGENTS.md` + `cc-session.md` + `cowork-session.md` + Sweep `sessions-format.md`/`brain/CLAUDE.md`. Reine Doku, kein Code. |
| 2026-05-23 | 095-arch | implemented | Brief Rollup-Ownership — Konfliktquelle als die Rollup-/Index-Dateien identifiziert (nicht die Worktrees); UI-Arbeit behält den Brief↔Report-Flow, nur die Rollups werden single-writer. |
| 2026-05-23 | 094-impl + merge | complete | **Resolver-Loop entkoppelt + headless.** Vier Bausteine: SSOT-Loop ↔ Resolver entkoppelt (Cadence raus, Loop läuft bis 859 durch), pure-core Wellen-Detektor `resolver-loop-detect.ts` (≤60-Buch-Wellen + Auto-Config), brief-freies Runbook (`brief`-Feld raus, `resolver-loop-log.md` als Fortschritts-Marker), headless Loop-Driver `run-resolver-loop.sh` (Resume + Halt-Disziplin + State-File). `test:resolver-loop-detect` 22/22, Trias grün, Live-Smoke `idle`. |
| 2026-05-23 | 094-arch | implemented | Brief Resolver-Loop — Resolver vom SSOT-Loop entkoppeln + brief-frei headless automatisieren; Wellengröße 50–60, Konsolidierungs-Pass als eigener Folge-Brief abgegrenzt (erster Lauf am W40K-complete-Meilenstein). |
| 2026-05-23 | Resolver-Pass 7 (`ssot-w40k-036..045`) + PR #90 | merged | **Resolver-Pass 7 — W40K-0351..0450, 450/450 W40K-Bücher applied.** Erster echter Lauf auf der Brief-091-6-Phasen-4a/4b-Maschinerie; supervised/axis-sliced. Counts PRE→POST `work_factions 1424→1659`, `work_locations 543→638`, `work_characters 844→1074`, `work_collections 109→142`, `work_persons 325→424`. Reference factions 162→166 / locations 189→201 / characters 237→297. `EXPECTED_RANGES` angehoben (Headroom-Sizing). Dry-Prediction == DB-POST-APPLY exakt, Trias grün, kein needs-decision. |
| 2026-05-22 | Resolver-Pass-7-Setup (Brief 093 + Config-Re-Key) | complete | Per-pass Brief [`093`](../../sessions/archive/2026-05/2026-05-22-093-arch-resolver-pass-7.md) für Resolver-Pass 7 geschrieben; `scripts/resolver-pass.config.json` von Pass 6 auf Pass 7 re-keyed (wave `036..045`, `applyRange` 1..45, Verify-Ranges W40K-0351..0450, `clusters` + `smokeSlugs` aus den Override-Files, `brief`-Feld auf Brief 093). Slug-/Title-Delta-Befund korrigiert (`apply-override` friert `slug`/`title` on update ein → out of scope für Pass 7). `sessions/README.md` + diese Seite nachgezogen. |
| 2026-05-22 | SSOT-Loop-Lauf `ssot-w40k-036..045` + PR #87 | merged | Standing-Loop (Brief 061), zweite volle 100er-Welle: 100 Bücher W40K-0351..0450 in zehn Iterationen, zehn Override-JSONs + Loop-Log-Appends, diff-only, kein DB-Touch. Kumulativ 450 Override-Files, selbst-erkennender `⏸ Resolver-Pause bei 450 Büchern`-Block. |
| 2026-05-22 | 091-impl + merge | complete | **Resolver-Pass Phase-4-Split.** Phase 4 → 4a (Integration/Apply) + 4b (Verify/Report), `/clear`-getrennt mit selbst-enthaltener 4a-Statusdatei; forward-ref-Collection-Guard in `apply-override-dry.ts` von report-only auf range-aware gehärtet (neues Pure-Modul `apply-override-collections.ts` + `test:collection-refs`, 7 Cases). `resolver-pass-runbook.md` + `resolver-pass.config.json` auf 6 Phasen; Driver comment-only. Trias + lint + typecheck grün. |
| 2026-05-22 | Brain-Hygiene-Pass (post-092) | complete | OQ (14) Roster-Excel-Hygiene-Sweep nach PR #85 / Session 092 in die Wiki eingearbeitet: `open-questions.md` (OQ (14) strike-through-closed + Frontmatter-Header + Migration-history), `project-state.md` (Roster 191→196 Collections, Slug-Delta-Watch-Item in § What's open), `pipeline-state.md` (191→196), `index.md`/`log.md`, `sessions/README.md`. Kein Code-/Schema-/DB-Touch; kein Archivierungs-Move. |
| 2026-05-22 | Session 092 — Roster-Excel-Hygiene-Sweep + PR #85 | merged | **OQ (14) — alle fünf Gruppen (a)–(e).** Excel-SSOT-Edits + Loader-Regen: `seriesHint`-Mistag (W40K-0244), sechs fehlende Autoren, zwei Format-`data_conflict` (W40K-0297/0334), zwei Titel-Mistags (W40K-0259/0330), fünf fehlende Collection-Kanten (W40K-0286/0307). `book-roster.json` 191→196 Collections, `books` 859. Kein DB-Touch; Slug-Delta (W40K-0259/0330) für Resolver-Pass 7 vermerkt. |
| 2026-05-22 | Pass-6-Review + Brief 091 (Phase-4-Split) + Brain-Hygiene-Pass | complete | Resolver-Pass 6 reviewed (sauberer Lauf — 350/350 Bücher, Dry-Prediction == DB-POST-APPLY exakt, Trias grün, kein needs-decision). Brief 091 geschrieben (Resolver-Pass-Phase-4-Split 4a/4b + range-aware forward-ref-Guard). Brain-Hygiene-Pass post-091 (`project-state`/`pipeline-state`/`open-questions`/`index`/`log` auf 350 Bücher); `sessions/README.md` Active-Threads. |
| 2026-05-22 | Resolver-Pass 6 (`ssot-w40k-026..035`) + PR #83 | merged (`72c8788`) | **Resolver-Pass 6 — erste 100er-Welle, W40K-0251..0350.** Erster Lauf unter dem Brief-090-lean-Kontrakt (brief-frei + runbook-getrieben), supervised/axis-sliced gefahren. 350/350 W40K-Bücher applied; Reference factions 154→162 / locations 169→189 / characters 199→237; DB-Junctions `work_factions=1424`, `work_locations=543`, `work_characters=844`, `work_collections=109`, `work_persons=325`. Forward-ref-Collection-Guard auf report-only entschärft (erste Welle mit cross-batch edges). |
| 2026-05-22 | Resolver-Pass-6-Setup + Brain-Hygiene-Pass | complete | `scripts/resolver-pass.config.json` von Pass 5 auf Pass 6 gesetzt (`ssot-w40k-026..035`, brief-frei, `applyRange` 1..35, Verify-Ranges, Phase-4 ohne Facet-Promotion); Brain-Hygiene-Pass auf 350 Override-Files / DB 250 + Brief-090-Maschinerie + 100er-Loop (`project-state`/`pipeline-state`/`open-questions`/`index`/`log`); `sessions/README.md` Active-Threads; Git-Sandbox-Prevention-Regel in `CLAUDE.md` § Git verankert. |
| 2026-05-21 | 100er-Loop-Lauf `ssot-w40k-026..035` + PR #81 | merged | Standing-Loop (Brief 061), erste volle 100er-Welle: 100 Bücher W40K-0251..0350 in zehn Iterationen, zehn Override-JSONs + Loop-Log-Appends, diff-only, kein DB-Touch. Vier Loop-Disziplinen aktiv. Kumulativ 350 Override-Files, selbst-erkennender 350er-Resolver-Pause-Block. Content-clean (SMB-/Chapter-lastig); Roster-Flags fürs OQ (14). |
| 2026-05-21 | 090-impl + PR #80 merge | complete | **Resolver-Pass lean.** Mess-Gate bestätigt (Vorab-Dokumentenlast ~22k/Phase + Phase-4-Korpus-Skalierung), dann Umbau: schlankes `resolver-pass-runbook.md`, brief-freier Driver, Phase-4 digest-only (live gegen 250 Bücher idempotent-validiert), stabile wave-parametrisierte Tools (keine `-NNN`-Klone), Resolver-Cadence 50→100. Volle grüne Suite. |
| 2026-05-21 | 090-arch | implemented | Brief Resolver-Pass lean — Token-Budget pro Phase, Mess-Gate-Abzweigung, fünf Bausteine (schlankes Runbook, brief-freier Driver, Phase-0-Token-Dichte, Phase-4-Digest, Konsolidierung + 100er-Cadence). |
| 2026-05-21 | Wiki-Hygiene-Pass (post-087/088/089) | complete | Brain-Update für Brief 087 + 088 + Loop-Lauf `021..025` + Brief 089. `project-state.md` auf 250 W40K-Bücher + neue „Latest pipeline state (post-089)"-Sektion, `pipeline-state.md` Resolver-Counts + Loop-lean, `open-questions.md` (OQ (14) Roster-Excel-Hygiene neu; Brief 087 / Loop `021..025` / Resolver-Pass 5 als erledigt), `index.md`/`log.md` nachgezogen. Session-Archivierung: geschlossene Session-Files nach `archive/2026-05/`. Coordination-Worktree am 2026-05-21 auf `origin/main` resynct. |
| 2026-05-21 | 089-impl + PR #78 merge | complete (`d46bf6a`) | **Resolver-Pass 5 (`ssot-w40k-021..025` / W40K-0201..0250).** Axis-sliced, manuell/supervised gefahren (nicht über den Driver — stop-before-push). Reference factions 146→154 / locations 157→169 / characters 169→199; `facet_values` 85→86 (`commissar`). DB-Junctions post-Re-Apply `001..025`: `work_factions=1153`, `work_locations=455`, `work_characters=701`, `work_collections=79`, `work_persons=232`. Drei Wave-Calls (Regimenter / `commissar`-Facet + Retag / Author-Backfill). 250er-Resolver-Pause geräumt. Alle Checks grün. |
| 2026-05-21 | 089-arch | implemented | Brief Resolver-Pass 5 — fünfte 50er-Welle axis-sliced (Fünf-Phasen-Vertrag analog 076), drei Wave-Calls. Vorab-Roster-Backfill W40K-0206/W40K-0244-Autoren im Session-Branch erledigt. |
| 2026-05-21 | Loop-Lauf `ssot-w40k-021..025` + PR #77 | merged (`c2f53e7`) | Standing-Loop (Brief 061) Iterationen 021..025 — 50 Bücher W40K-0201..0250, fünf Override-JSONs + Loop-Log-Appends, diff-only, kein DB-Touch. Vier Loop-Disziplinen aktiv. Kumulativ 250, selbst-erkennender Resolver-Pause-Block. |
| 2026-05-21 | 088-impl + PR #76 merge | complete (`71fb4f1`) | **SSOT-Loop lean.** Loop-Iteration liest jetzt drei Dateien (~6k statt ~55k+ Tokens): `ssot-loop-runbook.md` + `loop:next`-Detection-Helper (`scripts/loop-next-batch.ts`, 13-Case-Test) + `facet-catalog.json`. Resolver-Pause selbst-erkennend, `--skip-initial-resolver-pause` entfernt. `CLAUDE.md`/`AGENTS.md`-Carve-out; Brief 061 auf Design-Rationale reduziert. |
| 2026-05-21 | 088-arch | implemented | Brief SSOT-Loop lean — schlanke selbst-erklärende Loop-Iteration: kurzes Runbook + Detection-Helper statt Brief-061-Voll-Lesekette. |
| 2026-05-20 | 087-impl + PR #74/#75 merge | complete | **Goodreads-Rating-Pipeline-Integration.** Goodreads-Rating als vierte forward-only Loop-Disziplin: `overrides.rating` (nested object), Pure-Helper `apply-override-rating.ts`, Apply-Layer schreibt `book_details.rating`/`rating_count`/`rating_source`, `apply-override-dry.ts --file=`-Modus, `test:resolver` +3 Cases (154/0). `WebFetch` in `--allowedTools`. Page-Read der Goodreads-Seite, nie Snippet. Forward-only ab `ssot-w40k-021`. |
| 2026-05-20 | Wiki-Pass (post-086-impl) | complete | Brain-Update für Brief 086: OQ (10) + OQ (12) geschlossen, ADR `decisions/hardcover-to-goodreads-pivot.md` gezogen (amendet `no-goodreads.md` fürs Rating-Feld), `project-state.md` auf 197/200 Rating-Coverage + neue „Latest pipeline state (post-086)"-Sektion, Brief 087 (Goodreads-Rating-Pipeline-Integration) geschrieben, neue OQ (13) Crawl-Simplification-Sichtung, Refresh-Button in den Roadmap-Ideas-Backlog. `index.md`/`log.md`/`cowork-session.md`/`sessions/README.md` nachgezogen. Cowork-Befund: Coordination-Worktree mit reparierter `.git/config` (NUL-Korruption) + tree-weitem CRLF-Flip — Resync-Empfehlung in der Branch-Sektion. |
| 2026-05-20 | 087-arch | implemented | Brief Goodreads-Rating-Pipeline-Integration — Goodreads-Rating als vierte Loop-Disziplin in Brief 061, Override-Schema-Erweiterung + Apply-Layer, Page-Read statt Snippet. Steht für CC bereit. |
| 2026-05-20 | 086-impl + PR #73 merge | complete (`af7d90c`) | **Hardcover-Hit-Rate Pass 2 + Goodreads-Rating-Validierung (vier Phasen).** Phasen 1–3: vier Hebel (6-Stage-Cascade, Author-Normalize, Pass-2-Retry, CC-Direct-WebSearch-Overrides) → 116/200 = 58 %, unter dem 70-%-Ziel, „hard signal" (strukturelle Hardcover-Katalog-Lücke). Phase 4: Goodreads-Rating per Einzel-Websuche, 78/81 = 96.3 %, DB-Rating-Coverage 197/200 (98.5 %). Verdikt: Websuche nur als Lokator, nicht als Quelle — Page-Read Pflicht. Brief 085 (PR #72) supersedet, ohne Merge geschlossen. |
| 2026-05-20 | 086-arch | implemented | Brief Hardcover-Hit-Rate Pass 2 (vier Hebel, Drei-Phasen-Workflow) + Phase-4-Nachtrag (Goodreads-Rating-Validierungslauf). Supersedet Brief 085. |
| 2026-05-19 | 084-impl + PR #71 merge | complete (`cdca651`) | **Locations-Axis-Hygiene** — Sister-Pass zu Brief 077 für die Locations-Achse. Allowlist-Skip für Umbrella-Surface-Forms (`location-policy.json` + `apply-override-location-skip.ts`), `locationsSkippedRedundant`-Audit-Bucket, ADR `decisions/location-policy.md`, Re-Apply `001..020` (`locationsUnresolved.Imperium 20→6`, `locationsSkippedRedundant 0→14`, `work_locations` invariant bei 417). |
| 2026-05-19 | 084-arch | implemented | Brief Locations-Axis-Hygiene-Sister-Pass. Allowlist-Skip mit Erhaltungs-Pfad, eigene `location-policy.json`, ADR `decisions/location-policy.md` mit HH-Domain-Revisit-Trigger. |
| 2026-05-19 | Wiki-Hygiene-Pass (post-079/080/081/082) | complete (PR #69 `8d265be`) | Brain-Update für die vier zusammenhängenden Sessions seit dem post-076/077-Wiki-Hygiene-Pass. `project-state.md` auf 2026-05-19, alle 200 Public-Synopsen `001..020` clean, neue „Latest pipeline state (post-080+081)"-Sektion (Apply-Layer-Forward-Guard + Backfill mit 123 Bücher / 502/605/783 chars), Branch-Sektion auf post-PR-69-Stand + Worktree-Layout dokumentiert (inkl. ~150er-Line-Ending-Noise auf Coordination-Worktree als bekanntes Artefakt), What's running um Brief-082-Worktree-Konvention + Brief-079-Lab-Cartographer + Guard-Pre-Pass ergänzt. `open-questions.md` und `index.md`/`log.md` mit-aktualisiert; `sessions/README.md` Maintainer-Bedienung-Pointer post-Wiki-Hygiene gesetzt. Keine Code-Änderungen. Workflow-Abweichung: pro Philipp-Entscheidung als Wiki-Edits direkt auf den 081-codex-Branch gepackt statt separater `brain/hygiene-post-081`-Branch; Hygiene-Diff und 081-Backfill-Diff teilen sich PR #69. |
| 2026-05-17 | 083-impl + PR #68 merge | complete (`f551a13`) | **Parallel-Worktree-Bootstrap.** `chrono-lexicanum-product` (Branch `worktree/product-bootstrap`) + `chrono-lexicanum-batches` (Branch `worktree/batches-bootstrap`) als dauerhafte agent-neutrale Bootstrap-Worktrees angelegt, Strang-Disziplin (Product/UI vs. Batch/Ingestion vs. Coordination, `main` read-only) in `CLAUDE.md` + `AGENTS.md` § „Parallel worktrees" verankert. Keine Foundation-Branches, kein Feature-Code, kein Touch der Altlast `chrono-batches-011-015`. Vor-Migration: Workspace-Layout bereitet die parallelen Brief-081-Backfill-Loop + spätere Brief-079-Cartographer-Phase-2-Arbeit ohne serialisierte Single-Worktree-Konflikte vor. |
| 2026-05-17 | 082-arch | implemented | Brief Parallel-Worktree-Bootstrap. Spec für die zwei agent-neutralen Bootstrap-Branches + `git`-Lifecycle (Branch-Erzeugung pro Task ab Bootstrap, `main` read-only, PR-Erstellung im Worktree-Pfad, Aufräum-Sequenz bei merged PRs). Workflow-Wiki-Page bewusst deferred — wenn die Praxis sich bewährt, später nach `brain/wiki/workflows/`. |
| 2026-05-17/18 | 081-impl (`codex/ingest-batches-synopsis-005-019`, 16 Commits) | complete + merged (PR #69 `8d265be`) | **Public-Synopsis-Backfill `005..019`.** 123 polluted Synopsen über 15 Standing-`/clear`-Loop-Iterationen rewritten (klein-zu-groß-Reihenfolge `005 → 019`, 1 Commit pro Iteration plus Closing-Report). Längen pre `505/1536.3/2458` (min/mean/max) → post `502/605.3/783` chars; Welle-2 (005..009) Voice-Pacing-Kalibrierung mit Welle-3/4-Volllauf danach. Voice-Praxis: Premise-erst vs. Setup-erst-Mix grob 6/10:4/10, In-Universe-Vokabular einmalig erklärt, Period-treue Faction-Naming (Tau/Imperial Guard / Battlefleet Gothic / Eldar-Era), Necromunda knappiger als Soul-Drinkers, Last-Chancers härter als Calpurnia. Pro Iteration nur `overrides.synopsis`-Edits, kein Touch von `facetIds`/Junctions/Top-Level. Detection-Algorithmus per Loop-Log-`tail`-`H2`-Marker `## YYYY-MM-DD · 🔄 Synopsis-Rewrite ssot-w40k-{NNN} (Brief 081)`. Kein WebSearch in irgendeiner Iteration. Endstand-Verifikation alle grün: `test:synopsis-lint` 14/0, `test:apply-override-dry` alle Batches `001..020` clean, `db:apply-override` Batch 019 pass (10/0 Updates), `lint`/`typecheck` pass, `brain:lint --no-write` 0 blocking. Token-Schätzung Welle-2 ~20–45k / Welle-3+4 ~45–80k pro Iteration. |
| 2026-05-17 | 081-arch (Commit `189960a` auf `codex/ingest-batches-synopsis-005-019`) | implemented | Brief Standing-`/clear`-Loop für `ssot-w40k-005..019` als 15-Iter-Backfill. Längen-Korridor-Spec (500–620 standalone, 620–750 omnibi, soft-floor 420, soft-cap 800), Reader-Info-Erhaltungs-Regel (Protagonist/Setting/Konflikt/Faction/Tonality halten, Audit-/Curation-/Cross-Ref-Tails weglassen), Touch-Set per Mini-tsx-Helper, Per-Iteration-Verifikation, Closing-Impl-File-Pflicht. Pro Codex-Datenprüfung: `works.synopsis` nur gerendert, strukturierte Daten kommen aus Junctions — Kürzen verliert keine Filter-/Resolver-/JSON-LD-Funktion. |
| 2026-05-17 | 080-impl + PR #67 merge | complete (`9b9a839`) | **Public-Synopsis-Forward-Guard + Pilot-Rewrite Batch 020.** Track B: `scripts/seed-data/synopsis-banned-patterns.json` (32 Patterns), `scripts/apply-override-synopsis-lint.ts` (Pure Helper mit DI-Signatur, analog Brief-077-Skip-Helper), `validateSynopses()` als Batch-Level-Pre-Pass in `scripts/apply-override.ts` (hard-throw vor `works.synopsis`-Write), Report-Only-Pre-Pass in `scripts/apply-override-dry.ts`, `scripts/test-synopsis-lint.ts` 14/14. Track A: 10 Synopsen in `manual-overrides-ssot-w40k-020.json` rewritten auf 412–556 chars (mean 475), Re-Apply grün (10/10 rows, 0 throws, alle 4 Re-Mess-Scans = 0), 3 Slugs DB-direkt verifiziert. Pre-Mess-Telemetrie + Voice-Notes für Folge-Loop 081 im `sessions/ssot-loop-log.md`-Pilot-Block. |
| 2026-05-17 | 080-arch | implemented | Brief Public-Synopsis-Forward-Guard (Track B) + Pilot-Rewrite Batch 020 (Track A). Klein dimensioniert für eine CC-Session (~30–40k Tokens). Voice-Wahl = Design-Freedom, kein WebSearch, Apply-Layer-Pre-Pass-Architektur analog Brief-077-Skip-Helper-Shape. Folge-Brief 081 (Loop für 110+ Bücher in Batches 005..019) wird *nach* Maintainer-Voice-Review geschrieben. |
| 2026-05-17 | 079-impl + PR #66 merge | complete (`67e39dd`) | **Lab-Cartographer-Prototype.** Standalone Claude-Design-Galaxy-Map-Export aus lokalem gitignored `.scratch/map/Warhammer 40k Map/` als isolierte Iframe-Sandbox unter `/lab/cartographer` (Server-Component, ~20 LOC, `robots: { index: false, follow: false }`, full-viewport-Iframe auf `/lab/cartographer-prototype/index.html?standalone=1`). 13 verbatim-Kopien unter `public/lab/cartographer-prototype/` (`index.html`/`app.jsx`/`themes.js`/`eras.js`/`galaxy-data.js`/`galaxy-share.js`/`tweaks-panel.jsx`/`galaxy-parts.jsx`/`galaxy-detail.jsx`/`galaxy-editor.jsx`/`galaxy-add.jsx`/`galaxy-hologram.jsx`/`galaxy-search.jsx`), minimaler `app.jsx`-Patch für `?standalone=1`-Toggle, minimaler Root-Layout-Patch `<body suppressHydrationWarning>` (Browser-Extension-Body-Attribute). Produktive `/map`-Route unverändert, kein DB-Touch, kein TSX-Port (= Phase 2 künftig). Direkt-Maintainer-Anweisung — kein paired Architect-Brief, parallele Brain-Hygiene-/Brief-078-Sessions unangetastet. |
| 2026-05-17 | Wiki-Hygiene-Pass (post-076/077) | complete (PR #64 `3b5c664`) | Post-076-impl + post-077-impl Brain-Update. `project-state.md` auf 200 W40K-Bücher applied + post-077 `work_factions=1020 (−165)` gebracht; neue „Latest pipeline state"-Sektionen für 076 (axis-sliced Resolver-Pass 4 + Driver-Deliverable + Cross-Batch-Alias-Consolidation + Mini-Phase 5) und 077 (Grand-Alignment-Junction-Hygiene + Skip-Helper + Alignment-Util-Extract). `open-questions.md` mit Header-Update: OQ (11) Locations-Axis-Hygiene-Sister-Pass eingeführt (Imperium x20 als unresolved Location), nichts gepruned (alle 076/077-Decisions in den jeweiligen Sessions verankert; OQ (10) bleibt offen für Folge-Brief). `pipeline-state.md` mit Skip-Logik + Driver + axis-sliced Workflow + Resolver-Dossiers ergänzt. `index.md` / `log.md` mit-aktualisiert. `sessions/README.md` Maintainer-Bedienung-Pointer post-Wiki-Hygiene gesetzt. Keine Code-Änderungen, kein CC-Brief. |
| 2026-05-16 | 077-impl (PR-ready) | complete (Commits `9b8d593..c7ada5d`, branch `session-077-grand-alignment-junction-hygiene`) | **Grand-Alignment-Junction-Hygiene.** Pipeline-Compliance-Lücke aus Brief 070 geschlossen: Skip-Logik im Apply-Layer schreibt `imperium`/`chaos` nicht mehr als `work_factions`-Junction, wenn im selben Override-Block eine alignment-gleiche Sub-Faction resolved ist. Drei-Bedingungen-Skip via pure `decideFactionSkips()`-Helper (`scripts/apply-override-skip.ts`, DI-Signatur), Alignment-Util extrahiert nach `src/lib/seed/alignment.ts` und in beiden Pfaden (apply-override + seed-resolver-extensions) konsistent benutzt. Policy-Quelle: `scripts/seed-data/faction-policy.json` neue Top-Level-Field `redundantWhenSubPresent: ["imperium", "chaos"]`. `factions.json` `imperium`-Row trägt explizit `"alignment": "imperium"`. Geskippte Surface-Forms im neuen `factionsSkippedRedundant`-Bucket im `book_details.notes`-`---surfaceForms---`-Block. Re-Apply 001..020 putzt 165 redundante Junctions weg: `work_factions=1185 → 1020`, `imperium 81 → 6`, `chaos 133 → 43`. Tests: 122/0 (+6 Cases), `test:resolver-coverage` zeigt `1020/1301 (post-Brief-077-skip, 165 grand-alignment surface forms suppressed)`. Loop-Discipline „Faction-Granularity" ab `ssot-w40k-021` in 061 + Trigger-Heredoc. ADR-Section in `faction-policy.md` mit Revisit-Trigger für künftige Aeldari-Sub-Splits (Alignment-Equality → Parent-Chain). |
| 2026-05-16 | 077-arch | implemented | Brief Grand-Alignment-Junction-Hygiene mit Codex-Review-Erratum (3 Präzisierungen: Skip-Logik in apply-override nicht Resolver, Surface-Form-Persistenz im `---surfaceForms---`-Block via neuem Bucket, `factions.json`-Patch oder Helper-Extract für imperium-alignment). Drei-Edits-Spec (Skip-Logik + Loop-Constraint + Re-Apply als Backfill). |
| 2026-05-16 | 076-impl + PR #62 | complete + merged (`cfb0957`) | **Resolver-Pass 4 axis-sliced für `ssot-w40k-016..020` / `W40K-0151..0200`.** Fünf manuell gefahrene Subsessions (Preflight/Dossier → Factions only → Locations only → Characters only → Integration/Tests/Re-Apply/Counts/Smoke + Mini-Phase 5). +20 Factions (Necromunda-Houses-Cluster, Last Chancers, Howling Griffons, Soul Drinkers mit `primaris_reboot_coexistent`) / +25 Locations (Necromunda-Hive-Sub-Locations, „The X"-Pattern, Hydraphur unter pacificus) / +40 Characters (cross-batch Alias-Consolidation für Mad Donna/D'onne Ulanti, Ragnar Thunderfist, Kage/the Burned Man single-row + Lord Helmawr split). Re-Apply `001..020` mit Counts `work_factions=1185`, `work_locations=417`, `work_characters=633`, `work_collections=56`, `works=200`. Coverage `factions=1185/1301 = 91.1 %`. 13 Smoke-Slugs probed (3 Regressions bit-identisch zu 074, 10 neue). Cockpit-SQL-Replica für Welle 0151..0200 zeigt `drift=50/50`, `gap=15`, `collections=0` (alle 7 Omnibi voll). 13 unbekannte facetIds aus 018-Override gestrippt. **Driver-Deliverable** `scripts/run-resolver-pass.sh` + `scripts/resolver-pass.config.json` als Pass-5-Template gebaut. Per-Phase-Statusdateien unter `sessions/resolver-dossiers/`. Mini-Phase 5 (Public-Synopsis-Discipline ab `ssot-w40k-021`) im 061 + Trigger-Heredoc verankert. test-resolver 116/0 (war 78). |
| 2026-05-16 | 076-arch | implemented | Brief Resolver-Pass 4 axis-sliced mit Phasen-Workflow-Spec, Driver-Deliverable-Option (gebaut oder spec'd), Mini-Phase-5-Public-Synopsis-Verankerung. Codex-Review-Erratum-Punkte (Set-Subset statt Set-Equality bei Halt-Check, Per-Phase-Statusdatei als Wahrheit für Decision-Detection, JSON-Config statt hardcoded Phasen, no-op-Vertragsknick konservativ ausschließen). |
| 2026-05-15 | 075-impl + PR merge | complete (`61d4ff5`/`e0c7575`/`7f3f14f`) | Brief 075 in zwei orthogonalen Tracks gelandet. Track A — Cockpit: `CatalogueAudit.driftCount` parallel zu `hasResolvedDrift` berechnet (kein N+1), `sortBooks(auditFilters)` mit (drift, conf, updated, externalBookId)-Sortier-Kaskade bei aktivem `?audit=drift`, `SortPills.overriddenByDrift`-Affordance + Mikro-Caption, slim „Auch enthalten in:"-Komma-Zeile als siebte parallele `Promise` auf `/buch/[slug]` (Public-Lean gewahrt). Track B — Daten: `scripts/backfill-hardcover-rating.ts` (W40K-SSOT-eng, idempotent + `--force`, `no_author`-Bucket pro Erratum 3), `discoverHardcoverClaimV2(..., { ratingsCountField })` Probe → `users_count` deterministisch erreichbar, 77/150 Bücher (51.3 %) mit Rating + Count. Smoke `scripts/smoke-drift-sort-075.ts` committed; 2-Pass-Live-Run gegen prod-DB (5 transiente `graphql_error` in Pass 1, alle in Pass 2 aufgelöst). Hit-Rate < 70 % → For-next-session-Promote „OQ Hardcover-OL-Fallback öffnen" bzw. (075-impl-Argumentation:) bevorzugt Titel-Normalisierungs-Layer. |
| 2026-05-15 | 075-arch | implemented | Brief Cockpit-Refinement (Drift-Sort + optional Auch-enthalten-Panel) + OQ6 Hardcover-Rating-Promotion. Codex-Review-Erratum als ersten Block (4 Punkte: `--env-file=.env.local`-Konvention, W40K-SSOT-Scope-Filter, `no_author`-Miss-Bucket, DetailPanel-Surface auf Public-Detail wegen Duplikat-UI in `/buecher`-Row-Details). Sicherheitsventil für Track B explizit, Brief bewusst klein gehalten (zwei orthogonale Tracks in eigenen Commits). |
| 2026-05-15 | Wiki-Hygiene-Pass | complete | Post-074-impl Brain-Update + ADR `decisions/why-cc-direct-curation.md` geschrieben (V2-LLM-Stage als de-facto ausgemustert kodifiziert, OQ1 + OQ2-(c) als moot-post-Pipeline-Shift geschlossen). `project-state.md` / `pipeline-state.md` / `open-questions.md` / `index.md` / `log.md` auf post-074-Stand gebracht; `deferred-questions.md` um OQ2-(c)-Eintrag ergänzt. Keine Code-Änderungen, kein CC-Brief. |
| 2026-05-15 | 074-impl + PR #57 | complete + merged (`6ac4295`) | Resolver-Pass 3 für `ssot-w40k-011..015`. +20 Factions (Hydra-Cabal-Watson-Knoten + Squats-`tone`-Update + Sororitas-Orders + Astartes-Loyalist-Sub-Factions + Aeldari/Necron/Navis + Goffs + Aeronautica Imperialis); +19 Locations (Imperium-Nihilus-Frames + Necron-Tomb-Worlds + Watson-Welten + Named-Vehicles); +26 Characters (Cawl + Hadeya Etsul + Aeronautica-Cross-Batch + Watson-Retinue mit `historical_canon_layer`-Marker). 17 neue Faction-Aliases, 4 Location-Aliases, 17 Character-Aliases. Green Tide bleibt Buch-Scope, neuer `scripts/seed-data/collection-gaps.json`-Ledger. 13 unbekannte facetIds aus 015-Override gestrippt (LLM-Catalog-Typos). Re-Apply `001..015` mit lückenloser Pre-/Per-Batch-/Post-Counts-Tabelle: `work_factions=912`, `work_locations=287`, `work_characters=522`, `work_collections=35`. Watson-Trilogy junction-spot-check: 49 Rows sauber. test-resolver 78 passed (war 51). Cockpit-Tour als SQL-Replica. |
| 2026-05-15 | 074-arch | implemented | Brief Resolver-Pass 3 mit Codex-Review-Erratum (Schema-Tatsachen Squats-`tone`-vs-`tags`, Squats existieren bereits, Cawl-Status-Check, Green-Tide-Roster-Lücke) + Green-Tide-Collection-Gap-Addendum (Buch-Scope + `collection-gaps.json`-Ledger statt partieller Kanten). Implementiert durch 074-impl. |
| 2026-05-15 | 071-impl + PR #54 | complete + merged (`4993e17`) | Loop-Driver-Script `scripts/run-ssot-loop.sh` plus erster produktiver `N=5`-Run: 5 Iterationen, 50 neue Bücher (`ssot-w40k-011..015`), Loop-Log gewachsen, ⏸-Pause bei kumulativ 150. Override-Files auf `main` committed, in DB applied erst durch 074-impl. |
| 2026-05-14 | 073-impl | complete | Maintainer-Audit-Cockpit. `/buch/[slug]/audit` Server-Component mit allen DB-Feldern (`raw_name`, `confidence`, `sourceKind`, `work_collections.basis`, `external_links`, `notes`); `/buch/[slug]` slim-down auf Public-Lean; `/buecher` Audit-Modus mit vier Pillen (`drift`/`gap`/`ssot`/`collections`, AND, URL-State); Default-Sort `updatedAt desc`. Catalog-Filter in-memory (acceptable bis 500 Bücher). `src/lib/book-labels.ts` für konsistente Labels. |
| 2026-05-14 | 072-impl | complete | Resolver-Pass 2 für `ssot-w40k-006..010`. +54 Factions inkl. `heretic_astartes`-Mid-Knoten + Alpha-Legion-Reparent + Death-Guard / Emperor's-Children-Lücken-Fix; +45 Locations + `great_rift` in-place mit `era_frame`; +38 Characters (16 freq≥2 + 22 lore-iconic); Aeldari-/Drukhari-/Khârn-/Czevak-Aliase; `apply-override.applyCollections` löst Cross-Batch via `works.external_book_id`; Re-Apply `001..010` mit Post-Counts `work_factions=650`, `work_locations=239`, `work_characters=475`, `work_collections=35`. Smoke-Slugs grün. Brain-Lint-Fix an `index.md` + `why-sonnet-not-haiku.md` durch CC dokumentiert. |
| 2026-05-13 | 070-impl | complete | Faction-Policy & Hierarchie-Hygiene — Browse-Root vs. Tree-Root als Konzept getrennt, `brain/wiki/decisions/faction-policy.md` + `scripts/seed-data/faction-policy.json` (16 Browse-Roots), `factions.json` audit-patched (Chaos-Rename + 14 Reparents), `seed-resolver-extensions` faction-Insert auf Upsert, Pre-Apply Parent-Hygiene-Check ins Runbook, `brain:lint` neue Kategorie. Junctions unverändert. |
| 2026-05-12 | 069-impl | complete | Resolver Apply Evidence — `db:migrate`, `db:seed-resolver-extensions`, Re-Apply `ssot-w40k-001..005`; DB-Counts nachher `work_factions=318`, `work_locations=129`, `work_characters=363`, Smoke-Slugs matchen Dry-Run. |
| 2026-05-12 | 063–067 | complete | Resolver Layer — Migration `0009`, Resolver-Modul, Alias-/Reference-Daten, `work_characters`-Apply, Role-Normalisierung, Dry-Run/Runbook, Detailpage Locations+Characters. |
| 2026-05-11 | 058–062 | complete | SSOT-Mode + erster Authority-Layer — V2 liest `book-roster.json`, erster 10er-Batch, DB-Apply `ssot-w40k-001`, Standing-Loop 061 bis `ssot-w40k-005`, Author-FK-Fix. |
| 2026-05-10 | 057-impl | complete | Excel-SSOT-Import — Schema-Migration `0008_ssot_schema.sql` (bookFormat-Enum +3, sourceKind +1, `works.external_book_id` UNIQUE, `bookDetails.notes`, `work_collections`-Junction mit Self-M2M), Truncate-Skript `db-reset-for-ssot.ts` mit `--confirm`/ENV-Gate, Loader `import-ssot-roster.ts` produziert deterministisch (SHA256-verifiziert) `book-roster.json` mit 859 Büchern + 191 Collections. Migration-Apply + Truncate-Smoke deferred zu Maintainer-Trigger. |
| 2026-05-10 | 056-impl | complete | V2 Pre-Roster Fixes — per-page Lexicanum-Cache (24h TTL + negative caching), per-book Diff-Checkpointing in `run-batch.ts`, Cost-Recompute auf Cache-Hits ($0.0352/Buch live verifiziert), 9-Datei-Diff-Archivierung nach `ingest/.archive/{v1,v2-pilot,v2-batch}/`. |
| 2026-05-10 | 055-impl | complete | V2 Voll-Lauf 50 Bücher — `genericityScore`-Discovery-Merge mit 11 Unit-Tests, Web-Search-Prompt strict (1.06/Buch), Engine-Refactor (`run-engine.ts`/`run-batch.ts` als shared Stage 0–4), `v2-batch-20260510-1109.diff.json` als canonical Diff. Surface-Form-Top-20 + Resolver-Triage-Notes für 056-Resolver verfügbar. Cost-Telemetrie auf Cache-Hits blind ($0.0199/Buch real, aus 5-Book-Smoke). |
| 2026-05-09 | 054-impl | complete | V2-Pilot — TLBranson + Wikipedia Discovery, infobox-only Lexicanum, 5 deterministische Validatoren, Slim-LLM, BookV2Record mit Provenance pro Feld. 4/5 Acceptance-Bullets clean, $0.062/Buch (vs. V1 $0.114). |
| 2026-05-09 | 053 | complete | Brain Lint — `scripts/brain-lint.ts` + CI-Gate, erste Lint-Report-Datei. |
| 2026-05-09 | 052 | implemented | Ingest-Retention Decision (Option A: alles bleibt committed, Re-evaluate-Trigger gesetzt, Inline-Diff-Quote-Verbot). |
| 2026-05-09 | 051 | implemented | Brain Slim Pass — 22 sessions archiviert, ADRs gekürzt, open-questions.md Split in actionable + deferred + sub-phase backlog. |
| 2026-05-09 | 050 | implemented | Brain Hygiene Pass — Link-Audit, Frontmatter-Sources-Normalisierung, Read-Order-Fix, `.gitattributes`. |
| 2026-05-09 | 049-impl | complete (`1f6da88`/`3f43f4c`/`64b47b5`/`9fa70d8`) | Karpathy-Reset Brain + Atlas — 26 wiki pages + atlas-regen-skript + top-level consolidation. |
| 2026-05-08 | 047 + 048 (Bundle) | implemented (`4da6184`) | Pipeline-Härtung (5 Hebel) + Doku-Refresh (README/ARCHITECTURE/ONBOARDING auf Phase-3-Stand) |
| 2026-05-05 | 044 + 045 | complete | Phase 3e Batch 1 + Sonnet-Quadrant-Vergleich. |
| 2026-05-04 | 040 + 041 + 042 + 043 | complete | Haiku-Switch + Prompt-Härtung + Phase 3.5 Ingestion-Dashboard |
| 2026-05-03 | 035 + 037 + 039 | complete | 3a Skeleton + 3b Aux-Sources + 3c LLM-Schicht |

For older sessions and the full chronological log: [`../raw/historical/sessions-readme-log-pre-2026-05-08.md`](../raw/historical/sessions-readme-log-pre-2026-05-08.md) (pre-049 entries) and `sessions/archive/2026-04/`, `sessions/archive/2026-05/`.

## Next likely brief

Stand 2026-06-01 (post-PR-118 / Brief 104). **Korpus datenkomplett (859/859) + konsolidiert.** Seit dem letzten Stand gemergt: Brief 104 (`alias-aware-drift`, drift_works 380→0, PR #118), Brief 105 (beide Pässe — Daten PR #112 + UI PR #114, **Cluster A komplett**), Brief 107 (Full-Rebuild-Restore-Wiring, PR #115). **Aktive Linie: der Entity-Graph-Arc (Brief 109, Product) + der Podcast-Track (Brief 110, Batches) laufen parallel** — beide `open`, beide auf `main`, beide setzen das gemergte Brief 104 voraus. Cowork-Empfehlung in Reihenfolge:

0. **Brief 109 (Entity-Graph Step 1, Product) + Brief 110 (Podcast Step 1, Batches) — parallel anwerfen, der naheliegendste nächste Schritt.** 109 ersetzt die Stub-Seiten `/charakter` `/fraktion` `/welt` durch echte Entity-Hub-Seiten (geteilte frame-agnostische View, SSG, ownership-split-CSS); 110 ist ein Dry-Run-Pilot-Ingest (*The 40k Lorecast*, ~148 Folgen) + Episode-Tagging über die Resolver/Alias-Bibliothek (Brief 104 `src/lib/aliases/`), committed Artefakt + Quality-Report, **kein** Schema/DB-Write. Verschiedene Worktrees (Product/Batches) → kollisionsfrei parallelisierbar; 110 hängt hart an 104s `src/lib/aliases/` (jetzt auf `main`). Beide Briefs geschrieben + `open`. Daneben offen: **PR-#113-Sichtung** (Chronicle/Ask-Redesign, Brief-096 G+H) + Entscheidung über den 096-Status-Flip.

1. **Brief-104-Folgen (next-brief / maintainer-Material, kein Blocker).** Der Edition-Rename-Alias-Cluster (vorher hier „Top-Empfehlung") ist durch **Brief 104 erledigt** — drift_works 380→0, der vermutete Batches-Mini-Diff stellte sich als App-Schicht-Arbeit heraus. Was bleibt: `?audit=drift` ist jetzt dauerhaft leer → Drift-Pille/Brief-103-Sub-Sort behalten/umlabeln/zurückbauen entscheiden; die ruhige Alias-Klasse liegt always-on auf ~44 % der Bücher → ggf. detail-only/hinter `?audit=alias`; die `resolveSurfaceForm`-Such-Auflösung wird von **Brief 109 Step 3** verdrahtet; der maintainer-Visual-Pass der 104-Alias-UI (Desktop + ≤640px) steht aus. Detail in § What's open.

2. **Phase-3-Seal-Brief — formaler Phase-Abschluss.** `ROADMAP.md` flippt Phase 3 (Bulk-Backfill-Pipeline) auf shipped + öffnet Phase 4 (Discovery-Layer) bzw. Phase 5 (Cartographer + Ask the Archive). Brief würde: (i) den Phase-3-Tail explizit als Backlog-Items dokumentieren (OQ (13) Crawl-Simplification, OQ (3) Hand-Check-Workflow, Aggregator-Pass-3-Refinement, Refresh-Button), (ii) ggf. ROADMAP-Ideen-Backlog vs. Phase-4-Scope sortieren, (iii) den Phase-4-Eingang skizzieren (Submission-Schicht? Public-API? Was *wirklich* als nächstes Wert produziert). Eher klein, doc-only — kann auch parallel zum Alias-Cluster-Brief laufen (kein Konflikt).

3. **Sessions-Archive-Sweep + Numbering-Collision-Fix.** Die offene Sessions-Pile (100/101/102/103/Pass-11..15-Dossiers/consolidation-pass-2-*) ist reif für einen Brief-099-Style-Sweep nach `archive/2026-05/`; **dazu kommen zwei Numbering-Collision-Files** aus PR #110 — `2026-05-27-098-impl-map-dive-flicker-followup.md` und `2026-05-27-099-impl-map-transition-polish.md` reuse NNN=098/099, die schon vom Batches-Strang vergeben sind. Der Sweep muss die zwei Files umnennen (nächste freie NNN ab `104`/`105`, `parent`-Frontmatter-Anpassung in der zweiten Datei, Pfad-Rewrites in `links:`-/`sources:`-Listen). Reine Repo-Hygiene, kein Code-/DB-Touch.

4. **Brief-096 G + H bleiben lokal-iterativ.** Das site-weite Visual-Redesign läuft im `chrono-lexicanum-product`-Worktree weiter — Phasen G (Timeline bespoke) + H (Ask 6-path funnel mit canonical six paths) sind im 096-impl explizit deferred, das Brief-Container-Vehikel trägt sie weiter ohne neuen Cowork-Brief. Philipp committet erst, wenn das meiste sitzt, der nächste PR aus Product kommt auf ausdrückliches `fertig`. Public-Page-Rating-Render (`bookDetails.rating` für ~820/859 in der DB, auf `/buch/[slug]` nicht gerendert) bleibt an dieses Redesign **angedockt** (kein eigener Brief).

Sekundäre Optionen (Maintainer-Wahl, weniger zeitkritisch — gebündelbar oder einschiebbar):

- **Crawl-Simplification-Sichtung / Dead-Code-Retirement** (OQ (13), Cowork-Befund 2026-05-20). Die 086-Erfahrung — simple Websuche schlägt komplexe Crawl-/API-Maschinerie — ist ein Muster; die bypassed-aber-nicht-retired Ingestion-Pfade (V1-Pipeline, V2-LLM-Stage, V2-Pipeline-Rest) sind gegen die „Simplest thing first"-Regel zu sichten, im selben Zug die stale `CLAUDE.md`-Stack-Tabelle zu korrigieren. Eine Kandidaten-Einschätzung liegt in `open-questions.md` OQ (13); daraus entscheidet der Maintainer, was ein eigener Brief wird. Natürlicher Partner-Brief zum Phase-3-Seal-Brief — beide räumen den Phase-3-Tail auf, nur auf verschiedenen Achsen (Docs vs. Code).
- **Aggregator-Pass-3-Refinement** (auto-no-merge-rationale für `tags:vessel↔planet` + `primarch-stem↔captain-named-after-primarch`). Lohnt erst, wenn ein nächster Konsolidierungs-Pass anliegt — heute nicht zeitkritisch.
- **Sessions-Archive-Sweep** für 100/101/102 + Pass-11..15-Dossiers + consolidation-pass-2-*. Pile baut sich auf; reiner Hygiene-Pass (analog Brief 099).
- **Hand-Check-Workflow + Override-Schema** (OQ 3) — Cockpit ist seit Brief 073 da, das Override-Field-Schema + die Triage-Disziplin stehen noch aus; in ihrer ursprünglichen Form auf Aktualität zu prüfen (womöglich superseded durch den `claude -p`-Direct-Curation-Loop).
- **Refresh-Button** (Roadmap-Ideas-Backlog) — per-Buch on-demand Goodreads-Rating-Refresh; eigener Brief, sobald Brief 087 die Page-Read-Mechanik etabliert hat.

Cowork's session-end discipline (post-049): each architect brief and CC report runs through [`./workflows/session-end.md`](./workflows/session-end.md) — update this page, prune `open-questions.md`, write decisions if needed. Dieser Post-Merge-Koordinations-Pass 2026-05-25 (post-094/095/096) ist genau ein solcher Lauf — und seit Brief 095 zugleich der **einzige** Pfad, über den `sessions/README.md` + `brain/**` sich ändern (Cowork als alleiniger Schreiber der Rollup-Dateien aus dem Koordinations-Worktree).
